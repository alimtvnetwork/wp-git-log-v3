# Project Memory

## Core
Spec-only project. Focus EXCLUSIVELY on `spec/` and the supporting toolchain (`linter-scripts/`, `.github/workflows/spec-health.yml`, `.lovable/memory/audit/`). User wants to improve the spec — never the web app, UI, or preview. Do not modify `src/`, `index.html`, `tailwind.config.ts`, `index.css`, or any frontend code unless the user explicitly asks for an app/UI change.
Never touch `.release/`. Bump at least minor version on code changes.
Do NOT append boilerplate "If you have any question..." or "Do you understand?..." blocks.
Always list remaining tasks at end of session; on `next`, find pending work from memory if task list is empty.
Spec edits keep these in lockstep: target file banner + §98 changelog row + §99 health/inventory + `mem://specs/git-logs.md` queued-decisions trail.
File slots are immutable once shipped — never reuse a number; if content moves, rename the slot and add a §99 audit row (precedent: §16 → §37 in v2.8.6; Phase 130 caught a slot-32 collision pre-commit).
When §99 has duplicate inventory blocks, prefer removing the stale one over patching it — single source of truth eliminates a class of drift (precedent: Phase 135 removed bottom block missing 12 rows).
§99 inventory heading must match `(File Inventory|Module Inventory|Top-Level Modules|Document Inventory|Modules)` — bare `## Inventory` silently loses the rubric-v2 inventory credit (precedent: Phase 137 recovered 168/168).
Run `check-tree-health.cjs --strict` periodically — default mode rounds to 100 even when one module slips; strict caught 22-git-logs-v2 at q=2/3 invisible to default gate.
Tree health **168/168 strict-pass**, all 56 modules at full marks. Lockstep 87/87 pass · 0 findings. Trace-map gate **GREEN** — Phase H1 rebaseline 2026-04-28: `{ac_total:1304, ac_traced:74, code_total:48, code_orphan:25}`. CI gate count **15** (Phase H1). RUBRIC v2.24.
**§99 freshness gate (Phase H1) adoption rule**: never stamp a §99 `## Summary` block with `<!-- verified-phase: NNN -->` unless you have just read that Summary end-to-end and reconciled its claims against §97 / §00 / source-of-truth in the same session. Stamping stale prose to silence the gate is exactly the failure mode H1 was built to prevent (precedent: Phase H1-S1 found root `spec/99` Summary materially stale — claimed 78/100 + Warnings:3 from Phase 1 Triage era; freshened + stamped in one edit, never stamped-as-is). Codified in §27 changelog v2.42.1.
**§99 stamp adoption progress**: 1/89 §99 files stamped (root `spec/99` at phase 146, Phase H1-S1 2026-04-28). Gate remains advisory project-wide; per-file strict tracking activates as authors opt-in.
G-series CLOSED Phase 145: cumulative ac_traced 24→69 (+45, +188%), all 14 Phase 117–absorbed scripts bound; remaining ~25 orphans are run.sh/deprecated-v1/helpers/audit-internals — R1 (blocked on Lovable Cloud) is the proper deeper-binding tool. Future single-phase delta >50 ACs or >5 code files MUST be inspected before rebaselining again. **G-series lessons codified**: (1) always `ls linter-scripts/<name>.*` BEFORE authoring `[[trace]]` entries; (2) trace-map regression gate is reliable belt-and-braces but pre-flight saves cycles; (3) AC binding is mechanical 1:1 work — semantic/multi-script bindings need R1. audit-v4's 45/100 baseline is **superseded by audit-v5** (Phase 130).
**Phase 108 CLOSED Phase 108-full 2026-04-28**: O1/O2/O3 migrated to §27 slots 18/19/25 (G1 also bound them in trace-map.toml end-to-end); INV-08 retains for any future orphan; only `test/` rows O4–O8 remain ledger-tracked (out of scope, Phase 102 covers them). Slot-range exception precedent: slots 18/19 are validators in the 10-19 generator band — accepted because slots 01-09 full + renaming would break Phase 97/104 retros + AC-SAG-24 ref; future contributors MUST NOT "correct" the placement (codified in AC-T-22 + AC-T-23 + per-slot `## Slot-range note` subsections).
Session-persistence regression observed twice historically — verify file presence at start of each session before declaring "fixed". Not re-observed in Phases 117–H1-S1.
Stale-prose §99 sweeps: filter out fenced code, Validation History tables, and blockquotes — only `## Summary` narrative claims are actionable (precedent: Phase 136 over-counted 20 modules; Phase 139 found real count was 1).
`spec-folder-refs.allowlist` parser is line-strip-only — inline `# comment` trailers on entry lines corrupt the bucket; comments MUST be on separate full lines ABOVE entries (codified as AC-62-04 in Phase 143).
Test files belong in `linter-scripts/test/`, never `linter-scripts/` root — Phase 112's inventory parity gate treats `linter-scripts/*.{py,cjs,mjs,sh,go,ps1}` as production scripts and will flag any unspecced file there as drift. After creating ANY new file under `linter-scripts/`, run `bash linter-scripts/test/test-overview-inventory-parity.sh` before declaring the phase complete. **Phase F3 addendum:** new self-tests SHOULD be `.sh` (uniform `bash` runtime, `set -euo pipefail` + `assert` contract). Sanctioned `.py` exception: exercising an internal function (not a CLI surface) of a hyphenated `.py` source requiring `importlib`.

## Memories
- [Git Logs spec layout](mem://specs/git-logs) — Folder 22 §97 v3.9.1, §99 v3.9.6, §07 v2.2.0 (Phase 147 closed B1). Q1/Q2/Q3 landed; §39 split-DB live. SQLite root, no JWT, SSH-key Lane B in §31. Section map §00–§08 + §14–§37 + §39 + §97–§99, locked gap §09–§13. **Closed**: B1 §07 App identity — locked decision 12 PERMANENT (Env/Platform/OwnerEmail forbidden; future expansion needs new locked decision).
- [Full-tree audit v5](mem://specs/full-tree-audit-v4) — Phase 130 reconciliation: 3 of 4 audit-v4 critical findings verified resolved, quantitative claims corrected (1 module missing §97/§99 not 13/15; 6 intentional name-dupes not 15 collisions). Open items: R1 (blocked), R2 (monitor). B1 closed Phase 147.
