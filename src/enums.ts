export enum JsType {
    Object = 'object',
    String = 'string',
    Number = 'number',
    Boolean = 'boolean',
    Undefined = 'undefined',
    Function = 'function',
    Symbol = 'symbol',
    BigInt = 'bigint'
}

export enum ThemeType {
    Light = 'light',
    Dark = 'dark',
    System = 'system'
}

export enum OrientationType {
    Horizontal = 'horizontal',
    Vertical = 'vertical'
}

export enum DirectionType {
    Left = 'left',
    Right = 'right',
    Up = 'up',
    Down = 'down'
}
