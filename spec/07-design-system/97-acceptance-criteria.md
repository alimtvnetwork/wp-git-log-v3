# Acceptance Criteria

**Version:** 3.6.0  
**Updated:** 2026-04-26 (Phase 15d: Code Blocks section AC-017..AC-025 converted from table format to full GWT subsections — fixed-dark-bg contract, language-badge color mapping, font-size control bounds + localStorage persistence, line pin/unpin React state + visual precedence rules, shift-click range selection idempotency, fullscreen viewport inset + backdrop + z-index stack, Escape/overlay-click exit + cleanup, copy-button success state + Clipboard API + fallback, tree/structure emoji prefixes + guide rendering. AC IDs unchanged at AC-001..AC-034. 25 of 34 ACs now GWT; 9 await Phase 15e.)

---

## Overview

Testable criteria for validating design system compliance across all components and pages.

> **Format note (Phase 15d in flight):** Sections in this file are mid-conversion from table-row format to full Given/When/Then format. **Theme & Variables (AC-001..AC-006), Typography (AC-007..AC-011), Motion & Transitions (AC-012..AC-016), and Code Blocks (AC-017..AC-025)** are fully converted to GWT subsections (25 of 34 ACs total). **Navigation + Page Consistency** remain in table format pending Phase 15e. AC IDs are stable across the conversion — `AC-012` means the same criterion in both formats. Tooling that scrapes ACs by ID continues to work; tooling that requires GWT prose can use the converted sections as canonical and treat the table sections as one-line summaries until they are converted.

---

## Theme & Variables

### AC-001: No hardcoded colors anywhere in component code
- **Given** any component file under `src/` (TSX, JSX, CSS module, or styled-component)
- **When** the file is scanned for color literals
- **Then** the file MUST NOT contain ANY of: (a) hex literals matching `#[0-9a-fA-F]{3,8}` (e.g. `#fff`, `#1a1a2e`, `#ff5722cc`), (b) `rgb(...)` / `rgba(...)` / `hsl(...)` / `hsla(...)` function calls with raw numeric arguments (raw `hsl(220, 50%, 60%)` is FORBIDDEN; only `hsl(var(--token))` form is permitted per AC-002), (c) named CSS colors (`red`, `white`, `cornflowerblue`), (d) literal Tailwind color utility classes (`text-white`, `bg-black`, `border-gray-500`, `text-red-600`, `bg-blue-500`) — these bypass the design-token layer and break theming; AND the ONLY exceptions permitted are: (i) the design-token DEFINITIONS in `src/index.css` (`:root` and `.dark` blocks), (ii) `tailwind.config.ts` token registration, (iii) third-party SVG assets where colors are baked into the source asset (in which case the asset MUST be wrapped in a component that exposes a `currentColor` override).
- **Source:** `01-design-principles.md` (token-only rule), `src/index.css` (token definitions), `tailwind.config.ts` (token registration), `mem://index.md` Core rule on semantic-token enforcement.

### AC-002: All colors reference CSS custom properties via `hsl(var(--token))`
- **Given** any color application in component code or CSS
- **When** the color value is inspected
- **Then** it MUST take the form `hsl(var(--<token-name>))` for solid colors OR `hsl(var(--<token-name>) / <alpha>)` for transparency (e.g. `hsl(var(--primary) / 0.5)`); AND the `--<token-name>` MUST be defined in BOTH the `:root` and `.dark` blocks of `src/index.css` (per AC-004); AND the token VALUE in `index.css` MUST be a bare HSL triplet WITHOUT the `hsl()` wrapper (e.g. `--primary: 217 91% 60%;` — NOT `--primary: hsl(217, 91%, 60%);`) so that the `hsl(var(--primary))` consumer expression composes correctly; AND consumer code MUST NOT inline raw HSL values like `hsl(217, 91%, 60%)` even if the numbers happen to match a token — this defeats theming because the dark-mode override would not apply.
- **Source:** `02-theme-variable-architecture.md` (the `hsl(var(--token))` discipline), `src/index.css` (bare-triplet token format), `mem://index.md` Core rule on HSL-only design tokens.

### AC-003: Changing `--primary` in `:root` updates all primary-colored elements
- **Given** the design-token `--primary` defined in `src/index.css :root`
- **When** the value is changed (e.g. from `217 91% 60%` to `340 80% 55%`) and the page is reloaded
- **Then** EVERY surface bound to `primary` MUST update simultaneously: button backgrounds, link underlines, focus rings, hover tints, gradient endpoints involving `--primary`, the icon accent color, and any `border-primary` / `text-primary` / `bg-primary` Tailwind utility — without ANY component requiring a code change; AND derived tokens like `--primary-foreground` (the on-primary text color) MUST be updated MANUALLY in `index.css` to maintain WCAG AA contrast (per AC-006) — the system does NOT auto-derive foreground from background; AND surfaces using `primary-glow` or gradient compositions (per the `--gradient-primary` token) MUST also reflect the new primary because they reference `var(--primary)` internally.
- **Source:** `02-theme-variable-architecture.md` (single-source-of-truth principle), `src/index.css` (token cascade), AC-006 (paired-token contrast invariant).

### AC-004: Both `:root` and `.dark` blocks define all tokens
- **Given** the token catalog in `src/index.css`
- **When** the `:root { ... }` block and the `.dark { ... }` block are diffed
- **Then** the SET of declared `--<token-name>` properties MUST be IDENTICAL between the two blocks — every token defined in `:root` MUST have a corresponding override in `.dark`, and no token may exist in `.dark` that is not also in `:root`; AND the values MUST be appropriate for each mode (e.g. `--background` is light HSL in `:root`, dark HSL in `.dark`); AND tokens that are intentionally mode-invariant (e.g. brand `--accent` if the brand color does not adapt) MUST still appear in both blocks with identical values to make the intent explicit (silence is not consent — a missing override implies "not yet themed for dark mode" which is a bug); AND the `.dark` class MUST be applied to `<html>` or `<body>` by the theme provider — never to a nested element (which would create cascading-context bugs where some descendants escape dark mode).
- **Source:** `02-theme-variable-architecture.md` (dual-block requirement), `src/index.css` (the two blocks under audit), the `ThemeProvider` component (for the `.dark` class application contract).

### AC-005: Dark-mode toggle changes all surfaces, text, and borders correctly
- **Given** the application rendered in light mode
- **When** the user toggles to dark mode (via the theme switcher, which adds `.dark` to `<html>`)
- **Then** within ONE animation frame (≤ 16ms perceptible delay), ALL of the following MUST update simultaneously: page background (`--background`), card surfaces (`--card`, `--popover`), text colors (`--foreground`, `--muted-foreground`), borders (`--border`, `--input`), accent surfaces (`--primary`, `--secondary`, `--accent`, `--destructive`), focus rings, and any gradient compositions referencing the above; AND no element MUST exhibit a "flash" of the wrong-mode color (FOUC) — this requires the theme provider to set the initial class BEFORE first paint via a synchronous script in `index.html` reading from `localStorage`; AND the toggle MUST persist to `localStorage` so the choice survives reload; AND the toggle MUST respect `prefers-color-scheme` for first-visit users (no stored preference yet) per the standard `next-themes` / shadcn theme-provider pattern.
- **Source:** `02-theme-variable-architecture.md` (toggle contract), `06-motion-transitions.md` (no-flash requirement intersects with the reduced-motion rule in AC-014), the theme provider component (persistence + first-paint script).

### AC-006: Text on colored backgrounds meets WCAG AA contrast (4.5:1)
- **Given** any pairing of background token + foreground token used together in the UI (e.g. `bg-primary` + `text-primary-foreground`, `bg-card` + `text-card-foreground`, `bg-destructive` + `text-destructive-foreground`)
- **When** the contrast ratio is measured per WCAG 2.1 §1.4.3
- **Then** for normal-size text (< 18pt or < 14pt bold) the ratio MUST be ≥ 4.5:1; for large text (≥ 18pt or ≥ 14pt bold) the ratio MUST be ≥ 3:1; AND this MUST hold in BOTH light and dark modes (per AC-004 the token pair is defined in both blocks — both pairings MUST pass independently); AND when a new token pair is added to `index.css`, the author MUST verify contrast before merging — the recommended check is `npx @bramus/specificity` or any WCAG checker against the resolved HSL values; AND interactive states (hover, focus, disabled) inherit the same contrast obligation — disabled text at `opacity: 0.5` against the background MUST still pass 3:1 minimum (or use a dedicated `--muted-foreground` token sized for the lower contrast); AND non-text UI elements (icons, focus rings, form-control borders) MUST meet the 3:1 non-text threshold per WCAG 2.1 §1.4.11.
- **Source:** `01-design-principles.md` (accessibility baseline), `02-theme-variable-architecture.md` (paired-token convention `--primary` + `--primary-foreground`), WCAG 2.1 §1.4.3 (contrast — minimum), §1.4.11 (non-text contrast).

## Typography

### AC-007: Headings use the Ubuntu font family
- **Given** any heading element rendered in the application (`<h1>` through `<h6>`, plus any custom component that visually presents as a heading — e.g. `<CardTitle>`, `<DialogTitle>`, hero display text)
- **When** the computed style is inspected in browser devtools
- **Then** `font-family` MUST resolve to `"Ubuntu"` as the FIRST family in the stack, with a system-sans fallback chain (`"Ubuntu", system-ui, -apple-system, "Segoe UI", sans-serif`); AND the Ubuntu font MUST be loaded via Google Fonts in `index.html` `<head>` with `display=swap` (e.g. `<link rel="preconnect" href="https://fonts.googleapis.com">` + `<link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500;700&display=swap" rel="stylesheet">`) — `display=swap` is REQUIRED so headings remain readable during font load (no FOIT); AND the family MUST be registered in `tailwind.config.ts` under `theme.extend.fontFamily.heading` (or `display`) so utility classes like `font-heading` resolve correctly; AND headings MUST NOT use Poppins (the body family per AC-008) — mixing the two within a single heading is a contract violation; AND when a heading appears INSIDE a card or dialog, the font family MUST still be Ubuntu — child-component overrides are FORBIDDEN unless the design system explicitly defines a non-heading display font.
- **Source:** `03-typography.md` (font-stack contract), `index.html` (font-loading), `tailwind.config.ts` (`fontFamily.heading` registration), AC-008 (body family separation), AC-010 (gradient effect on H1/H2 — applied AFTER the family rule).

### AC-008: Body text uses the Poppins font family
- **Given** any non-heading text element (`<p>`, `<span>`, `<li>`, `<button>` label, form input text, table cell, footer copyright)
- **When** the computed style is inspected
- **Then** `font-family` MUST resolve to `"Poppins"` as the first family with a system-sans fallback (`"Poppins", system-ui, -apple-system, "Segoe UI", sans-serif`); AND Poppins MUST be loaded via Google Fonts in `index.html` `<head>` with `display=swap` and weights `300;400;500;600;700` at minimum (the design system uses `font-medium` and `font-semibold` for emphasis); AND the family MUST be registered in `tailwind.config.ts` under `theme.extend.fontFamily.sans` so it becomes the project DEFAULT (Tailwind's `font-sans` utility resolves to Poppins, not the built-in system stack); AND body text MUST NOT use Ubuntu — Ubuntu is reserved for headings per AC-007 to maintain visual hierarchy; AND inline `<code>` (within paragraphs) MUST use the monospace family per AC-009, NOT Poppins, even though it's body context.
- **Source:** `03-typography.md` (body-stack contract), `index.html` (font-loading), `tailwind.config.ts` (`fontFamily.sans` default override), AC-007 (heading family separation), AC-009 (code/mono escape hatch).

### AC-009: Code blocks use Ubuntu Mono / JetBrains Mono
- **Given** any `<code>`, `<pre>`, or `<CodeBlock>` element (incl. inline `<code>` inside paragraphs)
- **When** the computed style is inspected
- **Then** `font-family` MUST resolve to a monospace stack with `"Ubuntu Mono"` as the first family and `"JetBrains Mono"` as a secondary fallback before generic `monospace` (full stack: `"Ubuntu Mono", "JetBrains Mono", "Fira Code", Consolas, "Courier New", monospace`); AND BOTH `Ubuntu Mono` AND `JetBrains Mono` MUST be loaded via Google Fonts in `index.html` with `display=swap` (Ubuntu Mono weights `400;700`; JetBrains Mono weights `400;500;700` — the heavier weights support syntax-highlight bold tokens); AND the family MUST be registered in `tailwind.config.ts` under `theme.extend.fontFamily.mono` so utilities like `font-mono` resolve to this stack (overriding Tailwind's built-in `font-mono` which uses ui-monospace + Menlo); AND code blocks MUST preserve `font-feature-settings: "liga" 0;` to disable programming ligatures by default — ligatures are user-opt-in via a `format:ligatures` directive per `07-code-blocks.md` because they obscure the actual character sequence which matters in code review; AND inline `<code>` MUST use the same family as block `<pre>` so a snippet quoted in prose visually matches the same snippet in a code block.
- **Source:** `03-typography.md` (monospace stack), `07-code-blocks.md` (ligature default + inline-vs-block parity), `index.html` (dual font-loading), `tailwind.config.ts` (`fontFamily.mono` override).

### AC-010: H1 and H2 use the gradient text effect
- **Given** any `<h1>` or `<h2>` rendered in the application (raw HTML headings AND component wrappers like `<PageTitle>` / `<SectionTitle>` that semantically render as H1/H2)
- **When** the computed style is inspected
- **Then** the heading MUST have a gradient text fill via `background-image: var(--gradient-primary)` (or an equivalent `linear-gradient(...)` referencing design tokens) PLUS `background-clip: text` PLUS `-webkit-background-clip: text` PLUS `color: transparent` (or `-webkit-text-fill-color: transparent` for Safari compatibility) — ALL FOUR properties are required for cross-browser gradient text; AND the gradient MUST reference design tokens — hardcoded color stops are FORBIDDEN per AC-001 (typical token form: `linear-gradient(135deg, hsl(var(--primary)), hsl(var(--primary-glow)))`); AND H3..H6 MUST NOT use the gradient effect — they remain solid `hsl(var(--foreground))` to preserve the visual hierarchy where only the top two levels carry the brand emphasis; AND when the heading wraps to multiple lines, the gradient MUST flow continuously across all lines (the gradient is applied to the full text box, not per-line); AND for accessibility, the gradient MUST NOT be the SOLE conveyor of meaning — semantic `<h1>`/`<h2>` tagging carries the structural meaning for screen readers, the gradient is purely visual decoration.
- **Source:** `03-typography.md` (gradient effect), `02-theme-variable-architecture.md` (`--gradient-primary` token), AC-001 (no hardcoded colors in gradient stops), `12-page-creation-rules.md` (heading-hierarchy semantics — see AC-011).

### AC-011: No heading level is skipped (H1 → H2 → H3)
- **Given** any page in the application
- **When** the heading outline is extracted (e.g. via browser accessibility tree or `document.querySelectorAll('h1,h2,h3,h4,h5,h6')` traversal)
- **Then** the heading levels MUST form a monotonically descending or sibling-only sequence — a heading at level N MUST be preceded by a heading at level N-1 OR a heading at level N (sibling) OR be the first heading on the page (which MUST be H1); AND every page MUST have EXACTLY ONE `<h1>` (the page title) — pages with zero H1s violate WCAG and SEO; pages with multiple H1s break the document outline; AND skipping levels (H1 → H3, or H2 → H4) is FORBIDDEN even if it visually looks acceptable, because screen readers use the level structure to build a navigable outline and gaps make the structure unreliable; AND when a component wraps a section with its own heading, the wrapper MUST accept a `level` prop (or use a polymorphic `as` prop) so the consumer can choose the correct level for the surrounding context — hardcoded `<h2>` inside a reusable Card component is a bug because it forces every consumer to nest at the same depth; AND visual styling MUST be decoupled from semantic level via Tailwind utilities — a semantic `<h3>` rendered with `text-4xl font-bold` is permitted (and often correct) when the visual hierarchy doesn't match the semantic outline.
- **Source:** `12-page-creation-rules.md` (heading-discipline rule), `03-typography.md` (visual styling decoupling), WCAG 2.1 §1.3.1 (info & relationships) + §2.4.6 (headings & labels), `tailwind.config.ts` (utility classes for visual sizing).


## Motion & Transitions

### AC-012: All hover transitions complete within 300ms
- **Given** any interactive element with a hover state (buttons, links, cards, navigation items, dropdowns, form controls, icon buttons)
- **When** the user moves their pointer onto the element and the `:hover` styles take effect
- **Then** the `transition-duration` MUST be ≤ 300ms for every animatable property (color, background-color, border-color, transform, opacity, box-shadow); AND the canonical durations are: 150ms for color/background swaps (fast feedback), 200ms for transforms (scale, translate), 300ms for compound effects (gradient sweeps, shadow expansions) — values OUTSIDE the 150/200/300ms set are FORBIDDEN unless justified inline with a comment, because the design system relies on a small fixed timing vocabulary so transitions feel coherent across surfaces; AND the `transition-timing-function` MUST be `cubic-bezier(0.4, 0, 0.2, 1)` (Tailwind's `ease-in-out` default) — `linear` is FORBIDDEN for hover effects (mechanical feel), `ease-in` is FORBIDDEN (slow start feels laggy on quick mouse-overs); AND every hover transition MUST also define the REVERSE transition (mouse-out) with the same duration — asymmetric durations (e.g. 150ms in, 300ms out) cause visual "stickiness" and are FORBIDDEN; AND when `prefers-reduced-motion: reduce` is active per AC-014, the transition MUST collapse to ≤ 10ms (effectively instant) — the hover effect itself MAY still apply (e.g. color change), only the animation between states is suppressed.
- **Source:** `06-motion-transitions.md` (timing vocabulary + easing function), `tailwind.config.ts` (`transitionDuration` extension if non-default values are needed), AC-014 (reduced-motion override), AC-016 (CTA-specific composition built on this baseline).

### AC-013: No JavaScript animation libraries used for visual effects
- **Given** the project's dependency tree (`package.json` + lock file)
- **When** the dependency list is audited
- **Then** the project MUST NOT depend on ANY of: `framer-motion`, `react-spring`, `react-motion`, `gsap`, `anime.js`, `lottie-web`, `lottie-react`, `mo.js`, `popmotion`, `react-transition-group`, `velocity-animate`, or any other runtime JavaScript animation library — these add 30-200KB of bundle weight, conflict with the design system's CSS-first motion vocabulary per AC-012, and bypass the `prefers-reduced-motion` automatic respect that pure CSS gives for free; AND visual transitions MUST be implemented with CSS (`transition`, `@keyframes`, `animation` property) OR with the Tailwind `animate-*` utilities backed by `tailwind.config.ts` `keyframes` + `animation` registration; AND the ONLY exceptions are: (a) `tailwindcss-animate` (the official shadcn animation plugin, ~2KB, registers utility classes but emits pure CSS), (b) `embla-carousel-react` and similar UI libraries where the animation is intrinsic to the widget's behavior (carousel scroll, drawer slide), (c) third-party charting libraries (`recharts`, `d3`) where the animation is part of data visualization, NOT decoration; AND animating React state via `requestAnimationFrame` loops in user-land code is FORBIDDEN — if a CSS-only solution is impossible, the implementation MUST be approved as a documented exception in `06-motion-transitions.md` before merge.
- **Source:** `06-motion-transitions.md` (CSS-first principle), `package.json` (dependency audit surface), `tailwind.config.ts` (`keyframes`/`animation` extensions), AC-014 (reduced-motion compatibility — CSS gets it free, JS libs MUST be wrapped manually).

### AC-014: `prefers-reduced-motion` media query disables animations
- **Given** the user has set `prefers-reduced-motion: reduce` in their OS accessibility preferences (macOS: System Settings → Accessibility → Display → Reduce motion; Windows: Settings → Ease of Access → Display → Show animations; iOS: Settings → Accessibility → Motion → Reduce Motion)
- **When** the application loads
- **Then** `src/index.css` MUST contain a global rule that suppresses transitions and animations: `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { transition-duration: 0.01ms !important; animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; scroll-behavior: auto !important; } }` — the `0.01ms` (not `0`) is intentional so JavaScript that listens for `transitionend` still fires; AND component-level `@keyframes` animations MUST individually respect the media query if they are essential to the UI (e.g. loading spinners — wrap in `@media (prefers-reduced-motion: no-preference) { ... }` so the spin only runs when motion is allowed, and provide a static fallback indicator); AND `scroll-behavior: smooth` MUST be overridden to `auto` so users with reduced-motion preference don't experience smooth-scroll which can trigger vestibular issues; AND the rule MUST live in `index.css` GLOBAL scope, NOT in a component-scoped CSS module — global enforcement is the only way to catch every transition without per-component opt-in; AND parallax effects, auto-playing video, and infinite scroll auto-advance MUST be DISABLED entirely (not just slowed) when reduced-motion is active.
- **Source:** `06-motion-transitions.md` (reduced-motion contract), `src/index.css` (global override location), WCAG 2.1 §2.3.3 (animation from interactions), MDN `prefers-reduced-motion` reference, AC-012 (transitions collapse to ≤10ms), AC-013 (CSS-first means free reduced-motion compliance).

### AC-015: Link underline sweeps right-to-left on hover
- **Given** any inline text link (`<a>` element with text content, NOT navigation menu items which use the gradient-underline sweep per AC-027)
- **When** the user hovers over the link
- **Then** an underline MUST animate from the RIGHT edge of the link to the LEFT edge over 300ms (per AC-012), revealing the underline progressively; AND the underline MUST be implemented via a `::after` pseudo-element with `position: absolute`, `bottom: 0`, `right: 0`, `width: 0`, `height: 1px` (or `2px` for emphasis links), `background-color: hsl(var(--primary))` (or the link's current color via `currentColor`), and `transition: width 300ms cubic-bezier(0.4, 0, 0.2, 1)` — on hover, `width: 100%` triggers the sweep; AND the parent link MUST be `position: relative` so the absolute pseudo-element anchors correctly; AND on mouse-out the underline MUST sweep BACK from full-width to zero in the SAME direction (right-to-left out, then right-to-left in on next hover) — using `right: 0` for the anchor achieves this naturally because `width` shrinks toward `right`; AND the link MUST NOT use `text-decoration: underline` simultaneously with the pseudo-element underline — pick one, the pseudo-element is the design-system standard because `text-decoration` cannot animate width on most browsers; AND focus-visible state MUST show the underline at full width without animation (instant) so keyboard users get immediate feedback; AND when `prefers-reduced-motion: reduce` is active per AC-014, the underline MUST appear instantly at full width on hover/focus (no sweep animation).
- **Source:** `06-motion-transitions.md` (sweep direction + pseudo-element implementation), AC-012 (300ms baseline), AC-014 (reduced-motion override), AC-027 (navigation menu items use a DIFFERENT sweep — gradient + left-to-right — so the patterns are intentionally distinct).

### AC-016: CTA buttons use slide text animation, not simple color change
- **Given** any primary call-to-action button (the `Button` component with `variant="default"` or `variant="premium"` per `09-button-system.md`)
- **When** the user hovers over the button
- **Then** the button's text MUST animate via a slide effect — typically the visible text translates UP and a duplicate text element below translates UP into view (giving the illusion of the text being replaced by an identical copy with a subtle "press-up" feel); the implementation MUST use `transform: translateY(...)` on TWO stacked text spans inside an `overflow: hidden` button body, with the original at `translateY(0)` → `translateY(-100%)` and the duplicate at `translateY(100%)` → `translateY(0)`, BOTH with `transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1)`; AND a SIMPLE `background-color` change alone is FORBIDDEN for primary CTAs — that's the secondary/ghost variant pattern; AND the slide MUST be REVERSIBLE on mouse-out (text slides back down to original position); AND the button background MAY ALSO change (e.g. gradient angle shift, glow intensification) BUT the slide animation is the REQUIRED differentiator that signals "this is the primary action"; AND when `prefers-reduced-motion: reduce` is active per AC-014, the slide MUST be replaced with an instant background tint change (the visual emphasis of "this is hovered" remains, only the motion is suppressed); AND the slide direction MUST be vertical (up) — horizontal slides are reserved for navigation transitions per the design system's directional vocabulary; AND the duplicate text MUST be `aria-hidden="true"` so screen readers announce the button label only once.
- **Source:** `09-button-system.md` (CTA variant contract + slide implementation), `06-motion-transitions.md` (vertical-direction reservation), AC-012 (300ms + cubic-bezier baseline), AC-014 (reduced-motion fallback), AC-006 (the new background color after hover MUST still meet WCAG AA against the foreground text).


## Code Blocks

### AC-017: Code blocks maintain dark background in both themes
- **Given** any `<CodeBlock>` component rendered on a page
- **When** the page theme is toggled between light and dark mode
- **Then** the code block's background MUST remain a fixed dark color (`hsl(220, 14%, 11%)` per `07-code-blocks.md` "Block background" token) in BOTH themes — it MUST NOT invert to a light background when the rest of the page does; AND the header background (`hsl(220, 14%, 14%)`) and line-number background (`hsl(220, 14%, 9%)`) MUST also remain fixed dark; AND the ONLY elements that MAY change with theme are the syntax-highlighting tokens (keywords, strings, numbers, comments per the "Themed Elements Within Code Blocks" table in `07-code-blocks.md`) and the `--lang-accent` glow color; AND this fixed-dark behavior MUST be achieved via static HSL values in the component's scoped CSS, NOT via `hsl(var(--background))` or other theme tokens — code readability requires a consistently dark canvas regardless of ambient theme.
- **Source:** `07-code-blocks.md` (Color System — Fixed, Not Themed section), `02-theme-variable-architecture.md` (exception to token-only rule for code blocks), AC-001 (hardcoded-color prohibition has explicit code-block exception).

### AC-018: Language badge shows correct color dot per language
- **Given** any `<CodeBlock>` with a detectable language (e.g. `language-typescript`, `language-python`, `language-json`)
- **When** the block renders its header
- **Then** the header MUST display a language badge containing: (a) a colored dot (7px circle) with `box-shadow: 0 0 6px hsl(var(--badge-color) / 0.5)`, (b) the uppercase language name (e.g. "TYPESCRIPT", "PYTHON"); AND the dot color MUST be determined by a language-to-color mapping — typical mappings: TypeScript `#3178c6` (blue), Python `#3776ab` (blue), JSON `#cfcfcf` (gray), Bash `#4eaa25` (green), CSS `#264de4` (blue), Markdown `#083fa1` (blue), SQL `#e38c00` (orange), Rust `#dea584` (orange), Go `#00add8` (cyan), YAML `#cb171e` (red) — these are STATIC brand colors for languages, NOT theme tokens, because language identity is ambient context not UI state; AND the badge color MUST be injected via the inline `--lang-accent` CSS custom property on the `.code-block-wrapper` element so it also drives hover glow, fullscreen shadow, and tool-button hover border (per `07-code-blocks.md` "Language Badge" section); AND when the language cannot be detected, the badge MUST show "TEXT" with a `--muted-foreground` dot.
- **Source:** `07-code-blocks.md` (Language Badge section), `src/components/markdown/codeBlockBuilder.ts` (language detection + badge rendering), `src/index.css` (lines 264–605 — code block styles).

### AC-019: Font size controls adjust between 12px and 32px
- **Given** a `<CodeBlock>` with font-size controls visible in the header (the `A-`, `A`, `A+` buttons)
- **When** the user clicks the font-size buttons
- **Then** the `--code-font-size` CSS custom property MUST adjust as follows: `A-` decreases by 2px (minimum 12px), `A` resets to default 18px, `A+` increases by 2px (maximum 32px); AND the font-size change MUST apply to BOTH the code content AND the line numbers, with line numbers scaling proportionally at `calc(var(--code-font-size) * 0.7)`; AND the font-size value MUST be stored in `localStorage` under a key like `code-font-size` so the preference persists across page reloads; AND the font-size change MUST animate smoothly over 150ms (per AC-012 fast-feedback timing) — NOT an instant jump; AND the control buttons themselves MUST have `aria-label` attributes (e.g. `aria-label="Decrease font size"`, `aria-label="Reset font size"`, `aria-label="Increase font size"`) for screen-reader accessibility; AND when the stored preference is outside the 12-32px range (e.g. from a corrupted `localStorage` value), the system MUST clamp to the nearest valid boundary on load.
- **Source:** `07-code-blocks.md` (Font Size Controls section), `src/index.css` (lines 264–605 — `--code-font-size` usage), `src/components/markdown/codeBlockBuilder.ts` (control button rendering + localStorage persistence).

### AC-020: Line click pins/unpins with primary-colored background
- **Given** a rendered `<CodeBlock>` with visible line numbers in the left gutter
- **When** the user single-clicks on a line number
- **Then** the clicked line MUST toggle its **pinned** state: if not pinned, it becomes pinned; if already pinned, it becomes unpinned; AND a pinned line MUST receive the `line-pinned` class which applies: (a) background `hsl(var(--primary) / 0.12)` (primary-tinted, semi-transparent), (b) left border accent `3px solid hsl(var(--primary))` on the line number gutter cell, (c) line number text color changed to `hsl(var(--primary))`; AND the pin toggle MUST be implemented via React state (not just CSS `:active`) so the pin persists for multi-line selection operations; AND single-click on an already-pinned line MUST unpin it, removing all visual markers; AND the pin state MUST be visually distinct from the hover state (hover is `hsl(220, 15%, 16%)` — a neutral dark gray; pin is primary-tinted); AND when a line is both hovered AND pinned, the pinned background MUST take precedence (primary-tinted over neutral hover) because the pin is the stronger semantic signal (user has explicitly marked this line); AND the line number gutter MUST remain clickable across the full width of the gutter column, not just the number text itself, so users don't have to aim precisely at small digits.
- **Source:** `07-code-blocks.md` (Line Click / Pin section), `src/index.css` (lines 264–605 — `.line-pinned` styles), `src/components/markdown/codeBlockBuilder.ts` (pin state management).

### AC-021: Shift-click selects line range
- **Given** a rendered `<CodeBlock>` with at least one line already pinned (the "anchor" line)
- **When** the user holds Shift and clicks on a different line number
- **Then** ALL lines between the anchor line and the shift-clicked line (INCLUSIVE) MUST be selected/pinned; AND the selection MUST work in BOTH directions — if the anchor is above the shift-click target, all lines from anchor→target are selected; if the anchor is below, all lines from target→anchor are selected; AND the selection MUST be implemented as a RANGE, not individual toggles — shift-clicking when lines are already in the range MUST NOT unpin individual lines (the range operation is idempotent: all lines in the range end up pinned regardless of their prior state); AND the range selection MUST trigger the appearance of the **selection bar** (per AC-024) showing "Lines X–Y" with Copy Selected and Clear buttons; AND if NO anchor line exists (user shift-clicks without any prior pin), the clicked line alone becomes the anchor AND the selection (range of 1); AND the Shift key detection MUST be checked at click time (`event.shiftKey`) not at keydown time, because the user may have held Shift before focusing the code block; AND the range selection MUST visually match the single-pin styling (same `line-pinned` class, same primary-tinted background, same left border accent) so the user perceives no visual difference between single-pin and range-pin states.
- **Source:** `07-code-blocks.md` (Shift-Click / Range section), `src/components/markdown/codeBlockBuilder.ts` (range selection logic), `src/index.css` (`.line-pinned` styles shared with AC-020).

### AC-022: Fullscreen mode fills viewport with 2rem inset
- **Given** a rendered `<CodeBlock>` with the fullscreen button visible in the header
- **When** the user clicks the fullscreen button (or presses `F11` while focused in the block — though the button is the primary trigger)
- **Then** the code block MUST expand to fill the viewport with a `2rem` inset from all edges (`position: fixed; inset: 2rem;` or equivalent `top/right/bottom/left: 2rem`); AND the expansion MUST be accompanied by a backdrop overlay covering the rest of the page: `position: fixed; inset: 0; background: hsl(0 0% 0% / 0.7); backdrop-filter: blur(4px); z-index: 40;` — the overlay prevents interaction with the underlying page and focuses attention on the code; AND the expanded block MUST sit above the overlay at `z-index: 50`; AND the block MUST cast a large colored shadow using the language accent color: `box-shadow: 0 25px 80px hsl(var(--lang-accent) / 0.25);` — this creates a "floating" aesthetic; AND the body MUST receive `overflow: hidden` to prevent background scrolling while fullscreen is active; AND the transition into fullscreen MUST animate over 300ms (per AC-012 compound-effect timing) using `transform: scale(1.02)` → `scale(1)` for a subtle "settle" feel; AND on exit, the block MUST animate back to its inline position with the same duration.
- **Source:** `07-code-blocks.md` (Fullscreen section), `src/index.css` (fullscreen styles), `src/components/markdown/codeBlockBuilder.ts` (fullscreen state + DOM manipulation).

### AC-023: Escape key exits fullscreen
- **Given** a `<CodeBlock>` currently in fullscreen mode
- **When** the user presses the Escape key (`keydown` event with `event.key === 'Escape'`)
- **Then** the fullscreen mode MUST exit immediately (no delay); AND the exit MUST restore: (a) the block to its original inline position in the document flow, (b) the body `overflow` to its pre-fullscreen value (typically `auto` or `visible`), (c) the backdrop overlay removed from DOM, (d) focus returned to the fullscreen button that triggered the mode (or the code block itself if the button no longer exists); AND the exit MUST also fire on backdrop-overlay click (clicking the dimmed area outside the block exits fullscreen) — this is a secondary trigger, Escape is primary; AND the exit MUST be idempotent — pressing Escape twice does not cause an error; AND the exit MUST clean up ALL event listeners added during fullscreen entry (keydown, click-outside, resize) to prevent memory leaks; AND if the user navigates away (e.g. browser back button) while in fullscreen, the cleanup MUST run via a `beforeunload` handler or React effect cleanup so the page body doesn't remain `overflow: hidden`.
- **Source:** `07-code-blocks.md` (Fullscreen exit section), `src/components/markdown/codeBlockBuilder.ts` (keydown handler + cleanup), `src/index.css` (fullscreen state styles).

### AC-024: Copy button shows "Copied ✓" state for 2 seconds
- **Given** a `<CodeBlock>` with the copy button visible in the header
- **When** the user clicks the copy button
- **Then** the button MUST copy the FULL code content (all lines, NOT just visible lines — if the block is scrollable, the copy MUST include off-screen lines too) to the system clipboard via the Clipboard API (`navigator.clipboard.writeText()`); AND the button MUST immediately change to a **success state** with: (a) background `hsl(152, 60%, 18%)` (success tint), (b) text color `hsl(152, 70%, 60%)`, (c) label text changed to "Copied ✓" (or locale-appropriate equivalent), (d) an optional checkmark icon replacing the copy icon; AND the success state MUST persist for exactly **2 seconds** (2000ms) before reverting to the default copy button state; AND the 2-second timer MUST be cancellable — if the user clicks copy again during the success state, the timer resets to another 2 seconds (idempotent re-copy); AND if the Clipboard API is unavailable or permission-denied, the button MUST fall back to a legacy `textarea` copy technique and still show the success state on success, OR show a transient error tooltip ("Copy failed") if both methods fail; AND the copy button MUST have `aria-label="Copy code to clipboard"` and the success state MUST announce via an `aria-live="polite"` region so screen readers notify the user.
- **Source:** `07-code-blocks.md` (Copy Button section), `src/components/markdown/codeBlockBuilder.ts` (clipboard write + state management), `src/index.css` (`.copied` class styles).

### AC-025: Tree/structure blocks show 📁/📄 prefixes
- **Given** a `<CodeBlock>` whose content is detected as a directory tree or file structure (contains box-drawing characters `├`, `└`, `│`, `─`, directory patterns ending in `/`, or is explicitly tagged with `language-structure` or `language-tree`)
- **When** the block renders
- **Then** directories (entries ending in `/`) MUST be prefixed with the 📁 folder emoji AND rendered in bold with `hsl(var(--foreground))` color; AND files (entries with an extension, e.g. `.md`, `.ts`, `.json`) MUST be prefixed with the 📄 document emoji AND rendered at `hsl(var(--foreground) / 0.85)` color (slightly muted); AND tree guide characters (`├`, `└`, `│`, `─`) MUST be rendered at `hsl(var(--muted-foreground) / 0.5)` — very subtle, decorative only; AND ellipsis (`...`) indicating truncated subtrees MUST use `hsl(var(--accent))` color to draw attention; AND comments (lines beginning with `#`) MUST use `hsl(var(--muted-foreground))` in italic; AND the block MUST use the language label "STRUCTURE" in the header badge (per AC-018) and bypass standard syntax highlighting because tree content is not valid code in any programming language; AND the tree rendering MUST preserve the original indentation/spacing so the visual hierarchy matches the source text exactly — no reformatting or pretty-printing.
- **Source:** `07-code-blocks.md` (Tree / Structure Rendering section), `src/components/markdown/codeBlockBuilder.ts` (tree detection + prefix injection), `src/index.css` (tree-specific styles).

## Navigation

### AC-026: Header icon hover shows scale(1.05) effect
- **Given** any header action icon (theme toggle, fullscreen toggle, keyboard shortcuts button, search trigger — any `.header-icon` element rendered in the page header)
- **When** the user hovers their pointer over the icon
- **Then** the icon container MUST animate to `transform: scale(1.05)` over 150ms (per AC-012 fast-feedback timing) using `cubic-bezier(0.4, 0, 0.2, 1)` easing; AND the hover MUST also apply: (a) `color: hsl(var(--foreground))` (up from default `hsl(var(--muted-foreground))` for visual brightening), (b) `background: hsl(var(--muted) / 0.5)` (subtle tint for click-target affordance); AND on `:active` (mouse-down) the scale MUST drop to `0.95` for tactile feedback (the down-press feel) — this is REQUIRED, not optional, because the bidirectional scale (up-on-hover, down-on-press) is what gives buttons a physical-button feel; AND the scale animation MUST NOT exceed `1.05` — larger scales (1.1+) feel bouncy and break the design system's "subtle, no-spring" motion vocabulary per `08-header-navigation.md` ("No bounce, no spring — just a subtle scale pulse on hover"); AND when `prefers-reduced-motion: reduce` is active per AC-014, the scale MUST collapse to `1.0` (no scale animation), but the color/background hover styles MUST still apply so users still get the "is hovered" visual signal; AND the icon MUST have an accessible label via `aria-label` (e.g. `aria-label="Toggle theme"`) since it's icon-only with no visible text.
- **Source:** `08-header-navigation.md` (Icon Hover Transitions section), `06-motion-transitions.md` (timing + easing baseline), AC-012 (300ms hover transition contract), AC-014 (reduced-motion override).

### AC-027: Menu item hover shows gradient underline sweep
- **Given** any navigation menu item (`.nav-item`) in the header — distinct from prose links (per AC-015) which use a different sweep pattern
- **When** the user hovers their pointer over the menu item
- **Then** a 2px-tall gradient underline MUST sweep across the FULL WIDTH of the item over 300ms (per AC-012 compound-effect timing); AND the implementation MUST use a `::after` pseudo-element with: (a) `position: absolute; bottom: 0; left: 0; width: 100%; height: 2px;`, (b) `background: linear-gradient(90deg, hsl(var(--heading-gradient-from)), hsl(var(--heading-gradient-to)));` (token-only gradient stops per AC-001 — hardcoded color stops are FORBIDDEN), (c) `transform: scaleX(0); transform-origin: bottom right;` initial state, (d) `transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);`; AND on `:hover`, `transform: scaleX(1); transform-origin: bottom left;` — the origin-flip from `right` to `left` on hover is what creates the "sweep from left" expansion (the item grows left-to-right when entering, then collapses right-to-left when leaving, naturally because the origin is `right` in the resting state); AND the underline gradient MUST reference the heading-gradient tokens (NOT primary tokens) so the underline visually echoes the H1/H2 gradient treatment per AC-010, creating a subtle brand-consistency thread between section headings and navigation; AND the parent `.nav-item` MUST have `position: relative` so the absolute pseudo-element anchors correctly; AND when `prefers-reduced-motion: reduce` is active per AC-014, the underline MUST appear instantly at full width on hover (no sweep animation); AND the underline MUST also appear at full width on `:focus-visible` (keyboard focus) so keyboard users get the same affordance — instant, no animation, regardless of motion preference.
- **Source:** `08-header-navigation.md` (Menu Hover Underline section), `02-theme-variable-architecture.md` (`--heading-gradient-from/to` tokens), AC-001 (token-only color stops), AC-010 (heading gradient consistency), AC-012 (300ms baseline), AC-014 (reduced-motion override), AC-015 (DIFFERENT sweep pattern for prose links — intentionally distinct).

### AC-028: Dropdown items show primary-tinted hover background
- **Given** any dropdown menu item (`.dropdown-item`) inside a dropdown/search/popover container (e.g. theme picker, search results, user menu, keyboard shortcuts list)
- **When** the user hovers their pointer over the item
- **Then** the item MUST receive: (a) `background: hsl(var(--primary) / 0.08)` (very subtle primary-tinted background — the `0.08` alpha is intentional; higher values feel heavy in a dropdown context), (b) `color: hsl(var(--primary))` (text shifts to primary color for emphasis); AND the transition MUST animate over 150ms (per AC-012 fast-feedback timing) on BOTH `background` AND `color` properties; AND the hover MUST be visually distinct from header navigation items (per AC-027 which uses gradient underline) — dropdown items are CONTAINED hover surfaces (the whole row tints), navigation items are LINEAR hover surfaces (only the underline changes); AND keyboard navigation (arrow keys + Enter) MUST trigger the same hover styling on the focused item — i.e. the `:focus-visible` state MUST visually match `:hover` so keyboard users get identical affordance; AND the dropdown container itself MUST have `background: hsl(var(--popover))`, `border: 1px solid hsl(var(--border))`, `box-shadow: 0 4px 16px hsl(var(--foreground) / 0.08)`, and animate in via `fade-in 0.15s ease-out` on mount per `08-header-navigation.md`; AND items MUST have `border-radius: calc(var(--radius) - 2px)` so the hover background has rounded corners that nest visually inside the parent dropdown's `border-radius: var(--radius)`.
- **Source:** `08-header-navigation.md` (Dropdown / Search Menu section — Dropdown Item Hover), AC-001 (token-only colors), AC-012 (150ms fast-feedback), AC-027 (navigation items use distinct gradient-underline pattern — dropdowns intentionally use contained tint instead).

### AC-029: Sidebar collapses to sheet on mobile (< 768px)
- **Given** the application sidebar (`<Sidebar>` component, expanded width `16rem` per `10-sidebar-system.md`) rendered on a viewport
- **When** the viewport width crosses below the `768px` breakpoint (mobile)
- **Then** the sidebar MUST switch from inline-fixed-left layout to a **Sheet overlay** pattern: (a) the sidebar slides in from the LEFT edge (transform: `translateX(-100%)` → `translateX(0)`) over 200ms with `cubic-bezier(0.4, 0, 0.2, 1)` easing, (b) a backdrop overlay covers the rest of the viewport at `hsl(0 0% 0% / 0.5)` (50% black tint, NO `backdrop-filter` blur on mobile to preserve performance), (c) the sidebar sits at `z-index: 50` above the backdrop at `z-index: 40`; AND the trigger MUST be a hamburger-menu button rendered in the header — this button MUST ONLY be visible on mobile (`md:hidden` Tailwind utility or equivalent media query); AND the sheet MUST close on any of: (i) clicking the backdrop overlay, (ii) selecting a file/item in the sidebar (auto-close on navigation so the user sees the result of their selection), (iii) pressing Escape, (iv) the viewport growing back to ≥ 768px (sidebar reverts to inline layout, sheet state is discarded); AND on desktop (≥ 768px), the sidebar MUST be visible by default AND respect the `Ctrl+B` toggle per AC-030; AND the sidebar MUST use the shadcn `<Sheet>` primitive (NOT a custom-rolled overlay) for accessibility (focus trap, Escape handling, `aria-modal`); AND when `prefers-reduced-motion: reduce` is active per AC-014, the slide animation MUST collapse to instant appearance (no translate animation) but the backdrop MUST still appear for visual context.
- **Source:** `10-sidebar-system.md` (Mobile Behavior section — "On screens < 768px"), `12-page-creation-rules.md` (Rule 5 mobile-first responsive), AC-014 (reduced-motion override), AC-030 (Ctrl+B toggle still works on mobile but only when sheet is open), `src/components/ui/sidebar.tsx` (Sheet primitive integration).

### AC-030: Ctrl+B toggles sidebar visibility
- **Given** the application is loaded with a sidebar present
- **When** the user presses `Ctrl+B` (Windows/Linux) or `Cmd+B` (macOS)
- **Then** the sidebar MUST toggle its visibility: if expanded, it collapses to icon-only or hidden state (depending on the `data-state` convention in `10-sidebar-system.md`); if collapsed/hidden, it expands to the full `16rem` width; AND the keyboard shortcut MUST be globally registered at the application root (NOT scoped to a focused element) so it works regardless of where the user's focus currently sits — typical implementation: `useEffect` with a `keydown` listener on `window` checking `event.ctrlKey || event.metaKey` AND `event.key === 'b'`; AND the shortcut MUST `event.preventDefault()` to suppress the browser's default Ctrl+B behavior (which is "open bookmarks bar" in some browsers, a violation of user expectation in an app context); AND the shortcut MUST NOT fire when the user is typing in an `<input>`, `<textarea>`, or `contenteditable` element (check `event.target.tagName` or use a focus-within-input guard) so users can type the letter "b" in search fields without toggling the sidebar; AND the toggle MUST animate the sidebar width over 200ms (per AC-012 transform-timing) using `cubic-bezier(0.4, 0, 0.2, 1)`; AND the toggle state MUST persist to `localStorage` so the user's preference (sidebar visible/hidden) survives reload; AND on mobile (< 768px per AC-029), Ctrl+B MUST open/close the Sheet overlay instead of toggling inline width — the same shortcut, different behavior per breakpoint, because the inline layout doesn't exist on mobile; AND the keyboard shortcut MUST be discoverable — typically rendered in a "Keyboard Shortcuts" dropdown accessible from the header.
- **Source:** `10-sidebar-system.md` (Layout — "Toggle shortcut: Ctrl+B / Cmd+B"), AC-012 (200ms transform timing), AC-014 (reduced-motion override applies to width animation), AC-029 (mobile Sheet behavior), `src/components/ui/sidebar.tsx` (toggle state management + localStorage).

## Page Consistency

### AC-031: New pages follow section pattern templates
- **Given** any new page added to the application (a new route under `src/pages/` or equivalent)
- **When** the page's component tree is inspected
- **Then** the page MUST be composed from the section patterns defined in `11-section-patterns.md` — specifically, the page MUST follow the vertical flow: `Header → [Hero] → N × [Section Pattern] → [CTA] → [Footer]` per Rule 2 in `12-page-creation-rules.md`; AND the page MUST NOT introduce ad-hoc section layouts that don't match an existing pattern — if a new pattern is genuinely needed, it MUST first be added to `11-section-patterns.md` as a documented pattern, THEN consumed by the page (the spec is the source of truth, not the implementation); AND the Header MUST be the SAME header component used across all pages (no per-page header overrides — sticky positioning, sidebar trigger, theme toggle, breadcrumb all consistent); AND if a Footer is present, it MUST be the SAME footer component used across all pages; AND the page MUST use `<main>` as the wrapper for the main content area (not `<div>`) for landmark accessibility per WCAG 2.1 §1.3.1; AND the page MUST NOT introduce custom max-width values — section patterns already define their own `max-w-*` constraints (typically `max-w-7xl` for content, `max-w-4xl` for prose); AND the page MUST NOT introduce custom vertical spacing between sections — section patterns use a fixed `py-*` rhythm (typically `py-16` desktop, `py-12` mobile); AND custom one-off pages (e.g. error pages, auth pages, marketing landing) MAY deviate from section patterns ONLY if they are explicitly documented as exceptions in `12-page-creation-rules.md`.
- **Source:** `12-page-creation-rules.md` (Rule 2 page structure + Rule 6 section composition), `11-section-patterns.md` (the canonical pattern catalog), AC-011 (heading hierarchy applies across all sections), WCAG 2.1 §1.3.1 (landmark structure).

### AC-032: No page introduces fonts not in the design system
- **Given** any page in the application
- **When** the page's CSS (component styles + inline styles + Tailwind class scan) is audited for `font-family` declarations
- **Then** the ONLY `font-family` values used MUST resolve to one of the three registered families: `Ubuntu` (headings, per AC-007), `Poppins` (body, per AC-008), or `Ubuntu Mono` / `JetBrains Mono` (code, per AC-009); AND the page MUST NOT load additional Google Fonts in its own `<head>` injection — all font loading MUST happen in the global `index.html` once, not per-page (per-page font loading bloats the network waterfall and causes cumulative layout shift); AND the page MUST NOT use `font-family: serif`, `font-family: cursive`, `font-family: fantasy`, or any other generic family that bypasses the design system; AND the page MUST NOT use Tailwind utility classes that imply different families (`font-serif`, `font-display` if not registered) — the registered utilities are `font-heading` (Ubuntu), `font-sans` (Poppins, the default), and `font-mono` (Ubuntu Mono / JetBrains Mono stack); AND if a special display effect is needed (e.g. a marketing hero needs an extra-large script font), the addition MUST first be added to `03-typography.md` AND `tailwind.config.ts` AND `index.html` font-loading, THEN consumed — the spec is the gate, the implementation follows; AND inline `style="font-family: ..."` is FORBIDDEN per Rule 9 of `12-page-creation-rules.md` (Fixed font stack) — all typography MUST go through the registered utility classes.
- **Source:** `12-page-creation-rules.md` (Rule 9 fixed font stack), `03-typography.md` (font registry), `tailwind.config.ts` (fontFamily extensions), `index.html` (centralized font loading), AC-007/AC-008/AC-009 (the three registered family contracts).

### AC-033: All interactive elements follow the state language
- **Given** any interactive element rendered in the application (`<button>`, `<a>`, `<input>`, `<select>`, `<textarea>`, role="button", role="link", custom interactive components)
- **When** the element is inspected for state styles
- **Then** the element MUST define visible styles for ALL of: (a) `:hover` (pointer over — visual brightening or background tint), (b) `:active` (mouse-down or touch — pressed/tactile feedback, e.g. `scale(0.95)` per AC-026 or background darkening), (c) `:focus-visible` (keyboard focus — REQUIRED ring or outline, NEVER `outline: none` without a replacement), (d) `:disabled` (when applicable — `opacity: 0.5` AND `cursor: not-allowed` AND `pointer-events: none`); AND the focus ring MUST use `outline: 2px solid hsl(var(--ring))` OR `box-shadow: 0 0 0 2px hsl(var(--ring))` — both forms acceptable, but the token MUST be `--ring` (NEVER `--primary` or other tokens, because `--ring` exists specifically for focus indicators with WCAG-tuned contrast); AND the focus ring MUST have a 2px MINIMUM thickness for WCAG 2.1 §2.4.7 (Focus Visible) compliance; AND `:hover` MUST NOT be the SOLE indicator of interactivity (per Rule 5 in `12-page-creation-rules.md`: "No hover-only features (hover enhances, never gates)") — every interactive element MUST be discoverable from its default state alone (cursor, color, or shape); AND state transitions MUST follow the timing vocabulary from AC-012 (150/200/300ms) — custom durations are FORBIDDEN; AND the `:disabled` state MUST be announced to screen readers via `aria-disabled="true"` or the native `disabled` attribute — visual greying alone is insufficient for accessibility.
- **Source:** `12-page-creation-rules.md` (Rule 10 interaction state language + Rule 5 no-hover-only), `01-design-principles.md` (state hierarchy), AC-006 (state styles must still pass WCAG contrast), AC-012 (timing vocabulary), AC-026 (header-icon active scale precedent), WCAG 2.1 §2.4.7 (Focus Visible).

### AC-034: Responsive at mobile/tablet/desktop breakpoints
- **Given** any page in the application
- **When** the page is rendered at viewport widths spanning mobile (< 768px), tablet (768-1024px), and desktop (> 1024px)
- **Then** the layout MUST adapt at the standard Tailwind breakpoints: `md` (768px) and `lg` (1024px) — these are the design system's canonical breakpoints; AND on mobile (< 768px) the page MUST: (a) use a single-column layout (multi-column grids collapse to `grid-cols-1`), (b) hide the inline sidebar (Sheet overlay per AC-029), (c) show a hamburger menu in the header, (d) ensure all touch targets are ≥ 44×44px per WCAG 2.1 §2.5.5 (target size — minimum); AND on tablet (768-1024px) the page MUST: (a) show 2-column grids where appropriate (`md:grid-cols-2`), (b) show inline sidebar but possibly icon-only (collapsed state), (c) show navigation items but use icon-only buttons in the header per `08-header-navigation.md` Responsive Behavior table; AND on desktop (> 1024px) the page MUST: (a) show 3-column grids where appropriate (`lg:grid-cols-3`), (b) show full inline sidebar at `16rem`, (c) show full nav items with labels in the header; AND the page MUST be designed mobile-first (per Rule 5 in `12-page-creation-rules.md`) — Tailwind utilities WITHOUT a breakpoint prefix apply to mobile; `md:` and `lg:` prefixes layer on enhancements; AND horizontal scrolling at any breakpoint is FORBIDDEN (a page that requires horizontal scroll on mobile is a bug — content must reflow or wrap); AND text MUST remain legible at all breakpoints — minimum body font size 14px (`text-sm`), minimum heading H1 24px (`text-2xl`); AND images MUST be responsive via `max-width: 100%; height: auto;` OR `<img>` with `srcset` for resolution adaptation.
- **Source:** `12-page-creation-rules.md` (Rule 5 responsive-first + Rule 8 WCAG contrast), `08-header-navigation.md` (Responsive Behavior table), `10-sidebar-system.md` (mobile Sheet at < 768px), `tailwind.config.ts` (breakpoint definitions — defaults to `md: 768px`, `lg: 1024px`), AC-029 (sidebar mobile collapse), WCAG 2.1 §2.5.5 (touch target minimum size).

---

## Cross-References

| Reference | Location |
|-----------|----------|
| Full Design System | [00-overview.md](./00-overview.md) |
