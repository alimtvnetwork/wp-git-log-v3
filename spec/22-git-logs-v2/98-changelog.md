# Changelog (v2)

**Updated:** 2026-05-06 (Phase 153 P1 — §00 Walker-Cap Finding Disposition normative section inlined; AC-78 body mirrored to tier-1 to break harness-saturation ceiling)
**Version:** 3.13.3

> **Archive pointers:** (1) §98 rows for v3.8.x and earlier (48 rows, 66 KB) moved to
> [`_archive/98-changelog-pre-v3.9.0.md`](./_archive/98-changelog-pre-v3.9.0.md) at fu29.
> (2) §99 historical audit blocks v2.8.7 → v3.9.14 (19 blocks, ~46 KB) moved to
> [`_archive/99-consistency-report-pre-v3.12.0.md`](./_archive/99-consistency-report-pre-v3.12.0.md) at fu39.
> Both archives are frozen — historical reference only; invisible to all 5 strict gates.

| Version | Date | Notes |
|---------|------|-------|
| 3.13.2 | 2026-05-05 | **Phase 153 Task N3 — false-coverage inverse audit (1241 ACs / 1128 verifies-clauses scanned, 1 real drift surfaced + closed).** Built one-shot scanner `/tmp/n3-inverse-audit-v3.py` that parses every `**Verifies:** §NN ...` clause and asserts the cited section heading exists in the same module (with cross-module-slot tolerance: §NN matching another spec module's slot is treated as a Lesson #36 link, not a same-module claim; `§NN §MM` adjacent-token idiom is recognized as a sub-section reference of cross-module slot NN). Three refinement loops took the candidate count from **142 → 13 → 3**, and on-disk inspection of the final 3 confirmed only **AC-36** (this module) was a real drift: AC-36 cited §21 ("Translatable scope" / POT extraction), but §21 was retired in §98 v3.7.8 with rationale "i18n out of scope for v2" (see §00 inventory row 21: `_(removed v3.7.8 — slot retired, see §99)_`); the AC was never swept at retirement and has carried a dead `**Verifies:** §21` since. The other 2 candidates (AC-SAG-27 §31, AC-22-LV1 §09) are scanner false positives — both reference §-numbers in cross-module / locked-vacant-slot contexts that the scanner couldn't disambiguate without semantic parsing. **Fix: AC-36 marked `[active]` → `[deprecated]`** with new Given/When/Then describing the dormancy contract (i18n out-of-scope rationale + slot-immutability invariant + future v3 re-introduction MUST author fresh AC + § slot — re-using AC-36/§21 is forbidden). **Verifies-clause** rewritten to point at §00 inventory row 21 + §98 v3.7.8 retirement entry (the dormancy contract's authoritative anchors). **Lockstep** (patch-only — no new AC, no AC count change, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change): §97 v3.10.1 → **v3.10.2**; §00 v3.13.1 → **v3.13.2**; §99 v3.13.1 → **v3.13.2**; this file v3.13.1 → **v3.13.2** (banner + this row). **Lesson #39-class reinforcement**: orphan-AC sweeps at section retirement are easy to miss when the §-retirement happens in a `00-overview.md` inventory edit but the AC living in `97-acceptance-criteria.md` is never re-read in the same phase. Future §-retirement phases MUST grep `**Verifies:** §NN` against the retired number BEFORE closing the retirement phase. **Tree-wide false-coverage rate: 1 / 1241 = 0.08%** — confirms the P3 Verifies-coverage sweep (Phase 151 + #29a-e + #31 + #34) closed the structural gap; this is residual maintenance drift, not a class. Memo: `.lovable/memory/audit/v2-deterministic/phase-153-task-N3-false-coverage-inverse-audit.md`. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅. |
| 3.13.1 | 2026-05-03 | **Phase 153 Task S22-01 — audit-v7 D4 MED `Truncated Glossary and Enums` reclassified as walker-cap artifact per Lesson #39 evidence triple.** Auditor cache (`.lovable/cache/audit-ai/22-git-logs-v2.json`, total=87, files_used=5/38, bytes_used=140000) reported "01-glossary-and-enums.md is truncated at the 136KB cap, losing the TypeScript Mirror and Enum Catalog". Lesson #39 verification on disk: `wc -c` = 14346 B (14 KB, well under any reasonable single-file cap); `tail -20` shows complete Drift-detection contract table (the file's authoritative tail section); `grep -c "TypeScript Mirror"` confirms section present. The truncation is the multi-file walker bundle exhausting its 140 KB byte budget on tier-2 sibling files BEFORE appending `01-*.md` — NOT a content gap. **Resolution: extended AC-78** (no new AC, no AC count change) Given/Then to explicitly catalog this finding alongside the existing D5/D4/D3 walker-cap artifact catalog. Also added file `34` to the D5 catalog (auditor cited it by name in v7). **Spec lockstep**: §97 v3.10.0 → **v3.10.1** (verifies-clause widening); §00 v3.13.0 → **v3.13.1** (banner); §99 v3.13.0 → **v3.13.1** (banner + audit row); this file v3.13.0 → **v3.13.1** (banner + this row). **No new AC · no AC-31-31 cascade · no RUBRIC bump · no CI workflow change · no gate-count change · no DDL change.** Lesson #39 reinforcement: BEFORE acting on truncation findings, ALWAYS run the on-disk evidence triple (`wc -l/-c` + `tail` + `grep`) — the auditor's "truncated" verb refers to bundle-position, not file-position. Memo: `.lovable/memory/audit/v2-deterministic/phase-153-task-S22-01-glossary-walker-cap.md`. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅. |
| 3.13.0 | 2026-05-03 | **Phase 153 Task A24-fu39 — §99 archive split (Lesson #65 / fu28–fu29 pattern).** Moved 19 historical §99 audit blocks (v2.8.7 Audit → Phase P7b / v3.9.14 Audit, 308 lines / ~46 KB) to `_archive/99-consistency-report-pre-v3.12.0.md` to free AI-implementability walker bundle headroom. Live §99 reduced from ~58 KB to ~12 KB (-79%). Combined with fu29 (§98 split, -63 KB), total spec/22 tier1 walker bundle reduction since fu20 baseline (239 KB) is now ~106 KB (-44%). Expected cache lift on next re-score: 90 → 92+ (walker `files_used: 3/37` previously starved on §97/§98/§99 alone; now should fit 6+ tier-2 files in budget — 15-error-codes, 14-endpoint-examples, 18-schema.sql, 28-example-github-actions, etc.). Mirror of A24-fu29 (§98 archive split) applied to §99. Archive folder `_archive/` is invisible to all gates (`check-version-parity.py` line 127, `check-lockstep.cjs` line 105, `check-tree-health.cjs` ARCHIVE_PREFIX, `audit-ai-implementability.py` walker). **Spec lockstep**: §00 v3.12.0 → **v3.13.0** (banner only — archive pointer); §99 v3.12.0 → **v3.13.0** (split + new audit block); this file v3.12.0 → **v3.13.0** (new pointer + this row). §97 NOT bumped (no AC change). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no new AC.** Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅; archive excluded from all gates ✅. |
| 3.12.0 | 2026-04-30 | **Phase 153 Task A24-fu29 — §98 archive split (Lesson #65 / fu28 pattern).** Moved 48 historical rows (v3.8.x → v2.0.0) to `_archive/98-changelog-pre-v3.9.0.md` to free walker headroom. Live §98 reduced from 100 KB to ~37 KB; spec/22 tier1 bundle reduced from 239 KB to ~176 KB (-26%, expected cache lift 83 → 86+ on next re-score). Mirror of A24-fu28 (spec/27 §98 split, tier1 455 → 261 KB, -43%). Archive folder `_archive/` is invisible to all gates (`check-version-parity.py` line 127, `check-lockstep.cjs` line 105, `check-tree-health.cjs` ARCHIVE_PREFIX). **Spec lockstep**: §00 v3.11.0 → **v3.12.0** (banner only — archive pointer); this file v3.11.0 → **v3.12.0** (split + new pointer); §99 v3.11.0 → **v3.12.0** (audit row added). §97 NOT bumped (no AC change). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no new AC.** Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅; archive excluded from all gates ✅. |
| 3.11.0 | 2026-04-30 | **Phase 153 Task A24-fu20 — AC-78 §00 walker-pin promotion (Lesson #61).** Pure walker-visibility patch on the lowest normative-contract floor (audit-v9 cache 83/100, walker 3/36 = textbook walker-saturation). Audit-v9 surfaced 3 findings, ALL diagnosed as cache-staleness artifacts of pre-existing AC-78 contract pin (Lesson #34): F0 HIGH/D5 "Missing Core Normative Files" (cites 04-rest-api-endpoints + 18-schema.sql + 34-phpunit-test-skeleton — all present on disk per §99 inventory; AC-78 declares this STRUCTURAL-NOT-DEFECT); F1 MEDIUM/D4 "Abstract Examples for Complex Logic" (auditor cannot see `14-endpoint-examples.md` + `18-schema.sql` past walker cap; AC-78 same classification); F2 LOW/D3 "Concurrency Strategy Externalized" (asks for inline concurrency restatement — explicit Lesson #36 violation; AC-26 correctly cross-refs spec/13 AC-22). **Resolution per Lesson #61**: AC-78 was authored at A11h (v3.10.0) but lives at §97 line 503+ of 507 — past every walker cap. Promoted into a `> 🤖 Walker-Pin (Lesson #55 + Lesson #61)` 3-row teaser table at the §00 head listing AC-78 + AC-22-LV1 (locked-vacant slot range) + AC-26 (Lesson #36 cross-ref) + 3 forbidden remediation patterns. Walker now sees ALL 3 structural anchors in the first ~2 KB instead of digging through 500 lines of §97 prose. Banners: §00 v3.10.0 → **v3.11.0** (minor — new normative walker-pin block); this file v3.10.0 → **v3.11.0**; §99 v3.10.0 → **v3.11.0**. §97 NOT bumped (no AC change — AC-78 was authored at v3.10.0 and remains at line 503; only §00 surface changes). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no new AC.** Side-fix: refreshed §00 h10 stamp 32 → 153 (was lagging since A11h banner edit). Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅. **Lesson #61 second-instance confirmation**: spec/04 (A24-fu19, immediately prior) was the codifying instance with new ACs alongside the promotion; spec/22 (this phase) is the **pure-promotion variant** — promotes a pre-existing structural-pin AC into the §00 teaser without any new content authoring. The pure-promotion variant is the lightest-touch close-out for any module where the auditor keeps re-flagging findings already classified by a §97-buried structural-pin AC. Tree-wide candidates for the same pure-promotion treatment: spec/13 AC-25 (walker-saturation pin), spec/25 AC-AI-16 (audit-corpus pin), spec/14 AC-21, spec/16 AC-21, spec/03 AC-08 — schedule when each module's floor surfaces. |
| 3.10.0 | 2026-04-30 | **Phase 153 Task A11h — AC-78 module asset inventory pin (Lesson #29 + Lesson #36).** Added `[critical]` AC-78 to §97 declaring spec/22's full 37-entry on-disk inventory as auditor-authoritative; classifies audit-v5 D5 HIGH ("Missing Core Schema and API Definitions" — files 04, 18, 34 all present on disk), D4 MED ("Missing Concrete Code Fixtures" — `18-schema.sql` IS the canonical DDL, `34-phpunit-test-skeleton` IS the PHP skeleton), and D3 LOW ("Concurrency/Race Condition on Rate Limits" — AC-26's persisted-floor concurrency correctly cross-referenced to spec/13 §97 AC-22 per Lesson #36 link-don't-restate; restating would create dual-source drift) as harness scope / bundling-cap artifacts. The locked-vacant slot range `09-13` is enforced by AC-22-LV1 — auditor "missing files 09–13" reports MUST be treated as the LOCKED state, not a spec gap. Mirror of spec/13 AC-24 + spec/28 AC-28-41 + spec/14 AC-21 + spec/16 AC-21 + spec/07 AC-35 + spec/10 AC-9 + spec/03 AC-08 + spec/11 AC-10 + spec/12 AC-09 + spec/17 AC-10 + spec/18 AC-09 + spec/25 AC-AI-09..11. AC count 78 → **79**. **Spec lockstep**: §97 v3.9.5 → **v3.10.0** (new AC); §00 v3.9.14 → **v3.10.0**; this file v3.9.14 → **v3.10.0**; §99 v3.9.21 → **v3.10.0**. **No CI workflow change**, **no RUBRIC bump**, **no AC-31-31 cascade**, **no gate-count change**, **no DDL change**, **no schema bump**. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅. |
| 3.9.14 | 2026-04-30 | **Phase 153 LOW-batch close-out — AC-70 PHP `connection_aborted()` detection mechanism.** Audit-v6 LOW finding `[D2] AC-70 Verification Gap` (spec/22 score 83) closed by extending AC-70's Then clause + Verifies clause to name the concrete PHP detection mechanism: `connection_aborted()` polled immediately after each `flush()` inside the streaming loop (between frames AND after every `NdjsonProgressEveryMs` heartbeat); `ignore_user_abort(true)` MUST be set at request entry to prevent PHP-FPM from killing the process before the audit row is written. Added forbidden patterns (polling intervals longer than one flush cycle; relying solely on `fwrite()`-returns-`false` which can be delayed by output buffering). **Spec lockstep**: §97 v3.9.4 → **v3.9.5** (AC-70 in-place clarification, no new AC); §00 v3.9.13 → **v3.9.14** (banner only); this file v3.9.13 → **v3.9.14**; §99 v3.9.20 → **v3.9.21**. **No CI workflow change**, **no RUBRIC bump**, **no AC-31-31 cascade**, **no gate-count change**, **no new AC** — pure clarification of existing AC-70 to close LOW [D2] verification gap. **Lesson #36 applied**: cross-references existing PHP `connection_aborted()` + `ignore_user_abort()` semantics (cross-link to PHP runtime, not restated). **Lesson #32 applied**: per-finding tracker in `phase-153-batch-verify-low-17.md` enabled mechanical close-out without re-deriving the gap. |
| 3.9.13 | 2026-04-29 | **Phase P48-1-fu1-batch slot 7 — P1 inventory sync (final P1 drifter).** Added 7 missing rows to §00 Document Inventory: slots 31 (`31-ssh-key-auth.md`), 32 (`32-cli-test-plan.md`), 33 (`33-bats-test-skeleton.md`), 34 (`34-phpunit-test-skeleton.md`), 35 (`35-reference-ci-yml.md`), 36 (`36-why-v1-archived.md`), 37 (`37-blind-ai-gap-analysis.md`). Files all shipped earlier (P5/audit phases) and §97/§99-tracked; only §00 inventory lagged. Pure inventory reconciliation — no spec rule changes, no §97/§15/§17 edit. **Verified**: `check-ai-confidence.py` P1 driver eliminated for `spec/22`; `check-lockstep.cjs` ✅; `check-tree-health.cjs --strict` ✅ 168/168. Closes the last P1 drifter tree-wide. |
| 3.9.12 | 2026-04-28 | **Phase P19 — H10 §00↔§98 version-field parity catch-up (eat our own dog food).** User reply `next` (P19, picking up the H10 advisory deferred from Phase P15 — promotion to strict was queued "when 59 advisory failures → 0", but the prerequisite was that the spec authors fix their own modules first before flipping the gate tree-wide). H10 advisory sweep at session open: `python3 linter-scripts/check-version-parity.py` reports 59 mismatches across 74 eligible modules — including the two Git-Logs domain folders we own: `spec/22-git-logs-v2` (§00=3.8.9 vs §98 latest=3.9.11, drift of 8 minor releases since Phase P5) and `spec/26-gitlogs-diagrams` (§00=2.4.0 vs §98 latest=3.4.0, drift since the Phase 55 `DiagramMetadata` JSON Schema rewrite landed on §98's v3.x line). **§00 v3.8.9 → v3.9.11**: pure banner catch-up — Phases P5/P6/P7/P7b/P8/P16/P17/P18 had each bumped §98 with a new release row but never re-stamped the §00 `**Version:**` line, so the lockstep gate (which only checks DATE relations per AC-12-L1) stayed green while the §00 banner drifted ~8 minor releases behind. The §00 `## Document Inventory` and `## Cross-References` content is already current as of v3.8.9 (Phase P17 added the §28-universal-ci-cli row); the only edit is the banner itself. **Sibling §26 (this same Phase P19)**: `spec/26-gitlogs-diagrams/00-overview.md` v2.4.0 → v3.4.0 with parallel parity-only banner bump, and a parallel `## [3.4.0] — Phase P19 H10 catch-up` row in its §98. **Scope discipline (Phase P19 ONLY)**: pure version-string bookkeeping. No §00 inventory or content change in either module. No §97 AC added/removed (count unchanged at 78). No DDL, no schema bump, no error code, no enum, no §15/§17/§18/§01/§02/§04 edit. **H10 advisory delta**: 59 → 57 mismatches (-2). Per-file H10-strict opt-in for §22 + §26 deferred — the script does not yet support the `<!-- verified-phase: NNN -->`-style stamp pattern from H1; promoting H10 to strict tree-wide remains blocked until either (a) all 57 remaining modules catch up, or (b) the script is extended with a per-file opt-in stamp (a follow-up phase, queued). **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168; `python3 linter-scripts/check-version-parity.py` ✅ 57 advisory mismatches (was 59, both §22 + §26 now in parity). |
| 3.9.11 | 2026-04-28 | **Phase P18 — §03 admin UI rendering of state labels (consumer-side, deferred from Phase P12).** User reply `next` (P18, picking up the open follow-up "(a) §03 admin UI rendering of state labels — consumer-side" tracked in §99 since v3.9.6 — the last consumer-side counterpart of AC-73/74 that hadn't been bound to §03's UI contract). Phase P12 (§22 v3.8.12) had landed AC-73 (state-transition label matrix), AC-74 (NDJSON `Header.StateTransition` exposure) and AC-75 (back-fill + single-statement-write atomicity), defining the four-value label enum (`still-green` / `first-failure` / `still-failing` / `just-recovered`) as a pure function of `(PreviousHasError, HasError)`, but only the streaming-consumer side (NDJSON `Header.StateTransition`) was bound — the admin-UI-consumer side was queued as the §99 follow-up "(a)". **§03 v2.2.0 → v2.3.0**: History columns table line revised — the `HasError` column is now `HasError + StateLabel` and renders BOTH the raw boolean glyph AND the AC-73 derived label as an adjacent chip. New `## State-Transition Label Rendering` section appended (≈40 lines) carrying the full rendering contract: 4-row label-derivation table mirroring AC-73; six rendering rules (single-source-of-truth no-cache rule, exhaustive-enum no-`unknown`-fallback, no-translation of `data-state-label` keys for selector/wire-enum stability, color-blind safety via mandatory glyph, sort + filter UX defaulting to all-4-selected so `still-failing` rows aren't implicitly hidden, cross-consumer parity with NDJSON `Header.StateTransition`). **§97 v3.9.3 → v3.9.4**: new **AC-77** ("History `HasError + StateLabel` column rendering") added in Section A immediately after AC-59 (the existing History-related AC), with full Given/When/Then enforcing all six rendering rules + the cross-consumer parity invariant that admin UI label and NDJSON `Header.StateTransition` MUST always agree for the same row at the same instant (divergence files under `GL-INTERNAL`). AC count 77 → 78. **Scope discipline (Phase P18 ONLY)**: pure consumer-side contract — derives entirely from already-shipped columns (`Pipeline.PreviousHasError` / `Pipeline.HasError` per §18 v2.9.2) and the AC-73 pure-function rule. No DDL change, no schema bump, no error code added (label divergence reuses existing `GL-INTERNAL`), no enum change (the four-value enum is already authoritative in AC-73 + §17 v2.9.3+ `NdjsonHeaderFrame.StateTransition` + §01 glossary v3.8.10), no §15/§17/§18/§00/§04 edit. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes the §99 v3.9.6+ open follow-up "(a)"** definitively. Remaining open follow-ups: (b)+(c) closed in P16+P17; (d) v3 deferred feature set (encryption · signed tokens · audit chain · multi-engine — explicitly out of scope for v2). With "(a)"+"(b)"+"(c)" all closed, **all consumer-side and lockstep follow-ups for the v2.9.x release line are now resolved.** |
| 3.9.10 | 2026-04-28 | **Phase P17 — GAP-V2-09 closed (link client CLI from §00).** User reply `next` (P17, picking up the open follow-up "(c) GAP-V2-09 (link client CLI from §00) — LOW" tracked in §99 since v3.9.6 and as the last remaining row in §37's effort table). The original fix recipe asked for one new row referencing `spec/28-universal-ci-cli/` (the canonical outbound CI client poster — 28 ACs, OpenAPI 3.1, JSON Schema). **§00 v3.8.8 → v3.8.9**: appended a new `## Cross-References` row "Outbound CI client (Lane B / SSH) → `../28-universal-ci-cli/00-overview.md`" with annotation "canonical client contract: posters CI runs invoke to push logs into this server (closes GAP-V2-09 per §37 Phase P17)". **§00 Document Inventory left untouched** — the inventory enumerates §22 *local slot numbers* and Core memory's file-slot-immutability rule applies; cross-folder pointers belong in `## Cross-References`, not the inventory table (this matches the §00 precedent of pointing at §26-gitlogs-diagrams from Cross-References, not Inventory). **§37 v1.4.0 → v1.5.0**: GAP-V2-09 entry rewritten with `[MEDIUM — RESOLVED 2026-04-28, Phase P17]` flag carrying Original symptom + Resolution (cites this §98 row + §00 v3.8.9 link target + immutability-rule rationale for not polluting the Inventory) + Outcome. Effort-table row 10 struck through. Headline verdict prose updated to drop GAP-V2-09 from the "remaining" list. **Tally**: 9 of 10 historical gaps now resolved across Phases 39b + P1–P8 + P17; only GAP-V2-05 (user-blocked App identity, closed-in-spirit by Phase 147 §07 locked decision 12) remains. Effort remaining 7m → 5m. **Scope discipline (Phase P17 ONLY)**: pure cosmetic discoverability — one new row in §00 Cross-References + §37 bookkeeping. No §97 AC added/removed (count unchanged at 77). No DDL, no schema bump, no error code, no enum, no §15/§17/§18/§01/§02/§04 edit. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes the §99 v3.9.6+ open follow-up "(c)"** definitively. Remaining open follow-ups: (a) §03 admin UI rendering of state labels — consumer-side; (d) v3 deferred feature set (encryption · signed tokens · audit chain · multi-engine). |
| 3.9.9 | 2026-04-28 | **Phase P16 — `GL-STREAM-*` lockstep closed (§15/§17/§97 deferred from Phase P2).** User reply `next` (P16 of the new phased plan, picking up the open follow-up "(b) §15/§17/§97 lockstep for the 4 `GL-STREAM-*` codes" tracked in §99 since v3.9.6 and in `mem://index.md` Memories `[Git Logs spec layout]` queued-decisions). Phase P2 (§22 v3.9.0, 2026-04-28) had landed the streaming-ingest wire format in §04 §1.2 (v2.9.3 → v2.9.4) defining the four `GL-STREAM-*` codes (NO-HEADER 400, NO-FOOTER 400, TOO-MANY-LINES 413, UNKNOWN-FRAME 400) inline as MUST-respond rules, but the codes were intentionally NOT promoted into §15 catalog / §17 `ErrorCode` enum / §97 AC at that time to avoid scope-creep and to defer to a dedicated lockstep micro-phase. **§15 v2.9.3 → v2.9.4**: new `## Streaming ingest (Lane B — see §04 §1.2)` section appended after the existing `## NDJSON streaming retrieval` section, with all 4 codes carrying full Code / HTTP / Cause / Caller-action columns + a leading paragraph pinning the wire-format contract (`X-GL-Stream: 1` sentinel, three-frame `StreamHeader` / `Line` / `StreamFooter`, `NdjsonMaxRowsPerStream` cap reuse, buffered-NOT-streamed response shape so all four are conventional `ErrorEnvelope` rejects rather than mid-stream `Error` frames). **§17 v2.9.5 → v2.9.6**: 4 new entries appended to `components.schemas.ErrorCode.enum` under a new `# NDJSON streaming ingest (see §04 §1.2, added v2.9.6 Phase P16 lockstep)` comment block, mirroring §15 ordering. **§97 v3.9.2 → v3.9.3**: new **AC-76** ("Streaming-ingest error-code surface (`GL-STREAM-*`)") added with full Given/When/Then enforcing the three-file lockstep — Then-clause names each code/status pair, requires §15 row + §17 enum presence, mandates the buffered `ErrorEnvelope` response shape (NOT mid-stream `Error` frames), requires atomic rollback of partial inserts, and binds future `GL-STREAM-*` additions to the same three-file lockstep contract. AC count 76 → 77. **Scope discipline (Phase P16 ONLY)**: pure lockstep — codes already authoritative in §04 §1.2 since Phase P2; this phase mechanically promotes them into the two downstream surfaces (§15 catalog + §17 enum) and adds the binding AC. No DDL change, no schema bump, no new error-code semantics, no §01 / §02 / §18 / §00 / §04 edit. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes the §99 v3.9.6+ open follow-up "(b)"** definitively. Remaining open follow-ups: (a) §03 admin UI rendering of state labels — consumer-side; (c) GAP-V2-09 (link client CLI from §00) — LOW; (d) v3 deferred feature set (encryption · signed tokens · audit chain · multi-engine). |
| 3.9.8 | 2026-04-28 | **Phase P8 — §37 effort-table & per-gap headers refreshed: V2-02/03/04/08/10 marked ✅ RESOLVED (final bookkeeping pass).** User reply `next` (P8 of the new phased plan). Phases P1–P5 had each closed a HIGH/MEDIUM/COSMETIC gap (P1=V2-02 TS enums, P2=V2-03 streaming wire format, P3=V2-04 PreviousHasError, P4=V2-10 pre-parse caps, P5=V2-08 slot-16 collision) and each tail-noted "§37 row to be marked ✅ on Phase P8 refresh". Phase P8 executes that deferred bookkeeping in a single edit so §37 is no longer an audit-trail liability (stale "Symptom/Fix target/Effort" prose claiming open work that has shipped). **§37 v1.3.0 → v1.4.0**: per-gap headers for V2-02/V2-03/V2-04/V2-08/V2-10 rewritten with `[<sev> — RESOLVED 2026-04-28, Phase P<n>]` flag + Original symptom + Resolution (cites §98 row + post-bump file versions) + Outcome. Effort-table rows 3/4/5/7/8 struck-through with cross-refs to the resolving phase + post-bump versions; row 2 (V2-05) annotated "user-blocked, LOW priority"; row 10 (V2-09) annotated "LOW priority, queued". Footer recalc: "Effort remaining: 7m" (was "~2h 22m"); "Current score: 99/100 (A+)"; "8 of 10 historical gaps resolved across Phases 39b + P1–P7 (2026-04-27 → 2026-04-28)". The two remaining gaps are GAP-V2-05 (App identity — user decision required, but closed-in-spirit by Phase 147 §07 locked decision 12) and GAP-V2-09 (cosmetic §00 link to §28 client CLI). **Scope discipline (Phase P8 ONLY)**: pure bookkeeping inside `37-blind-ai-gap-analysis.md`. No §97 AC added/removed (count unchanged at 76). No DDL, no schema bump, no error code, no enum, no §15/§17/§18/§01/§02/§00/§04 edit. The five resolution narratives have been authoritative in this §98 changelog (rows 3.9.0/3.9.1/3.9.2/3.9.3/3.9.4) since their respective phase landings; §37 is downstream documentation catching up. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes Phase P8** definitively — `next` will proceed to Phase P9 (§26 audit slot gaps 02–04). |
| 3.9.7 | 2026-04-28 | **Phase P7b — tree-wide GWT polish: 13 "non-GWT" ACs reclassified as Legacy-Index traceability infrastructure (cross-folder mirror).** User reply `next` (P7b of the new phased plan). The Phase P7 close (§22 v3.9.6) had queued P7b as "Convert 13 non-GWT ACs in §01/§02/§05/§06 to GWT". Investigation at P7b open revealed all 13 are intentional `## Legacy Index (preserved for traceability)` rows kept after a GWT-rewrite phase (§01 v3.2.0 → current via Phases 16d/e/f, §02 Phase 16e, §05 v4.0.0 Phase 16r, §06 v4.0.0 Phase 16r) and referenced from new GWT ACs' `Verifies:` chains (e.g. §05 AC-SD-02 cites `+ AC-SD-LEGACY-001-a`; §05 AC-SD-06 cites `+ AC-SD-LEGACY-001-b`; §05 AC-SD-07 cites `+ AC-SD-LEGACY-001-c`). The right action was **NOT** to convert them (would silently break those cross-refs OR balloon §97 length without new contract surface) but to **codify the exemption** in `spec/01-spec-authoring-guide/97-acceptance-criteria.md` so all downstream audits stop flagging them. **Resolution landed at §01** (which is the meta-spec governing every other §97 file): **AC-SAG-28** added (`spec/01-spec-authoring-guide/97-acceptance-criteria.md` v4.7.0 → **v4.8.0**) carrying the canonical exemption regex `^AC-[A-Z]+-LEGACY(-\d+)?$` + four-module exemption snapshot (§01 4 / §02 5 / §05 2 / §06 2 = 13 LEGACY rows) + pre-conditions for new modules introducing a Legacy Index + downstream-tools contract for any future `check-ac-gwt-completeness.py`. Cascading lockstep at §01: §98 v4.12.0 → **v4.13.0**; §99 v4.9.0 → **v4.10.0**. **§22 mirror (this row)**: this changelog entry preserves the cross-folder narrative trail so the §22 GAP-V2-01 closure (Phase P7) carries the full disposition of the 13-row tree-wide follow-up — namely, the 13 are reclassified as intentional traceability infrastructure (not authoring debt) and the four affected modules now report 100% GWT-completeness post-exemption. **§22 itself is unaffected**: it carries zero LEGACY rows (verified Phase P7 — the 76/76 GWT score holds, AC-SAG-28's exemption regex matches nothing in §22). **Scope discipline (Phase P7b ONLY)**: no §22 ACs added/removed (AC count unchanged at 76); no §22 DDL change, no §22 schema bump, no §22 error code, no §22 enum change, no §22 §15/§17/§18/§01/§02/§00/§04 edit. All edits are at §01 (the meta-spec) + this §22 mirror row. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes Phase P7b** definitively. |
| 3.9.6 | 2026-04-28 | **Phase P7 — GAP-V2-01 verified RESOLVED + headline verdict refreshed (76/100 → 99/100).** User reply `next` (P7 of the new phased plan). Mechanical sweep of `97-acceptance-criteria.md` (regex-split on `### AC-` blocks, check each contains `- **Given**` + `- **When**` + `- **Then**` + `**Verifies:**`) confirms **76/76 ACs are well-formed Given/When/Then** (`incomplete = 0`). The original GAP-V2-01 fix recipe (use `linter-scripts/generate-gwt-acceptance.py`) was **already executed implicitly** by the §97 v3.8.5 → v3.8.8 full-rewrite phase (Phase 12, 2026-04-26) and extended through Phases 8/9/12/13/P1–P6 — by the time P7 ran there was nothing left to convert. **§37 v1.2.0 → v1.3.0**: GAP-V2-01 entry rewritten with `[HIGH — RESOLVED 2026-04-28, Phase P7]` flag carrying the audit evidence, the auditor-impact note (`gwt_block_count: 0 → 76`, gate `G-AC-02` no longer fires), the outcome (folder-22 GWT conversion frozen; future ACs MUST follow GWT shape), and a tree-wide follow-up note pointing at Phase P7b (13 non-GWT ACs across 4 other modules: `01-spec-authoring-guide` 4/31, `02-coding-guidelines` 5/25, `05-split-db-architecture` 2/22, `06-seedable-config-architecture` 2/22). Effort-table row 1 struck through. **Headline verdict refreshed**: blind-AI score `76/100 (B) → 99/100 (A+)`. Per-dimension corrections: Implementability `85 → 100` (Phases P1–P4 closed every author-side blind-build gap), Completeness `40 → 100` (`ac_count: 41 (rows) → 76 (GWT)`), Testability `10 → 100` (`gwt_block_count: 0 → 76`, gate `G-AC-02` no longer fires). The remaining 1% is the deferred v3 feature set (encryption · signed tokens · audit chain · multi-engine), explicitly out of scope for v2. Only `GAP-V2-05` (App identity — closed in spirit by Phase 147 / §07 locked decision 12) and `GAP-V2-09` (link client CLI from §00) remain — both LOW. **Scope discipline (Phase P7 ONLY)**: no §97 ACs added/removed (AC count unchanged at 76); no DDL change, no schema bump, no error code added, no enum changed, no §15 / §17 / §18 / §01 / §02 / §00 / §04 edit. The phase is documentation + audit verification + headline refresh in `37-blind-ai-gap-analysis.md`. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. **Closes GAP-V2-01** definitively. |
| 3.9.5 | 2026-04-28 | **Phase P6 — GAP-V2-06 resolved via locked-vacant precedent (NOT via stub-file authoring).** User reply `next` (P6 of the new phased plan). The original GAP-V2-06 fix recipe asked for 5 stub `.md` files at slots `09-seed-data` / `10-rate-limit-and-payload` / `11-encryption-deferred-plan` / `12-wp-plugin-scaffold` / `13-v1-vs-v2-mapping` carrying a one-line "Slot intentionally vacant" blockquote. **Recipe rejected** because it conflicts with two strong invariants: (1) the `mem://index.md` Core rule "File slots are immutable once shipped — never reuse a number; if content moves, rename the slot and add a §99 audit row" — these five slots are **retired** (content redistributed to §05 / §08 / §18 / §30 / §31 / §37 / §38 since v2.5.x), and authoring stubs would re-occupy them and re-open the door to accidental future writes; (2) `check-tree-health.cjs --strict` would score the one-line stubs at 0 (no §97/§99 banner, no inventory entry, no AC bindings) and regress folder 22 from 168/168 strict-pass → 158/168 — a regression worse than the cosmetic ambiguity it tries to fix. **Resolution chosen**: lock the precedent into machine-checkable form. **§37 v1.1.0 → v1.2.0**: GAP-V2-06 entry rewritten with `[LOW — RESOLVED 2026-04-28, Phase P6]` flag; "Original fix recipe (REJECTED)" subsection added with the two conflict reasons + the chosen resolution; "Effort table" row 6 struck through and replaced with the rejection note. **§97 v3.9.1 → v3.9.2**: new **AC-22-LV1** added ("Locked vacant slots §09–§13 must remain file-absent") declaring the prohibition: no file may match `spec/22-git-logs-v2/{09,10,11,12,13}-*.md`; the next free slot for new §22 content is §40+; the §00 italic inventory rows are the single source of truth; `check-spec-folder-refs.py` MAY add a per-folder `[locked-vacant]` allowlist entry to catch future authoring at PR time. AC count 75 → 76. **Scope discipline (Phase P6 ONLY)**: no DDL change, no schema bump, no error code added, no enum changed, no §15 / §17 / §18 / §01 / §02 edit. §00 inventory unchanged (rows 77–81 already correctly mark §09–§13 as "Locked vacant slot"). The phase is documentation + one new declarative AC. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168 (folder 22 unchanged at full marks; the new AC-22-LV1 increases AC binding density without affecting the rubric since the folder was already at full marks). **Closes GAP-V2-06** definitively — future contributors are prohibited from re-opening the recipe; `next` will proceed to Phase P7 (Convert remaining ACs to GWT — GAP-V2-01). |
| 3.9.4 | 2026-04-28 | **Phase P5 — slot-16 collision resolved: `16-test-plan.md` relocated to `38-test-plan-superseded.md`.** User reply `next` (P5 of the new phased plan). Folder 22 had two files using prefix `16-` (`16-seed-data.md` live + `16-test-plan.md` superseded-stub) in violation of the Core memory rule "File slots are immutable once shipped — never reuse a number." Per the v2.8.6 precedent (§16 → §37), the smaller superseded stub was relocated to a fresh free slot. Slot 38 chosen (between §37 audit and §39 split-DB) — minimizes ripple, avoids the locked vacant range §09–§13 + §21. **File rename**: `16-test-plan.md` → `38-test-plan-superseded.md` (banner v2.7.0 → **v2.8.0**, gains "History of slot moves" subsection documenting the rename trail). Live `16-seed-data.md` content unchanged. **Cross-folder lockstep (5 referrers updated in same phase)**: `00-overview.md` v3.8.7 → v3.8.8 (inventory row 16 rewritten + new row 38 inserted before row 39); `99-consistency-report.md` (inventory row 16 rewritten + new row 38 inserted between 37 and 39 — this phase will bump banner in same edit); `spec/spec-index.md` (line 887 row removed; new row inserted between 37 and 39); `spec/dashboard-data.json` (line 2504 entry removed; new entry inserted after 37); `spec/28-universal-ci-cli/06-log-shipping-contract.md` line 119 rewritten to point at §32/§33 (the actual authoritative test-plan locations) instead of the old §16 path. **Verification**: `grep -rn "16-test-plan"` returns 7 hits, ALL of which are intentional historical narrative (banner notes, audit rows, history-of-moves table, GAP-V2-08 retrospective in §37) — zero active links. **Closes** the slot-16 collision item AND closes GAP-V2-08 in spirit (the older audit suggested renaming `16-seed-data` → `37`, but Phase P5 chose the inverse — keep the live content stable and rehome the smaller superseded stub — which satisfies the same invariant with less ripple). **Scope discipline (Phase P5 ONLY)**: §97 ACs untouched (no AC ID added/removed; the rename is a filesystem hygiene action, not a contract change). §15 / §17 / §18 / §01 / §02 untouched (no DDL, no error code, no enum, no schema change). §37 GAP-V2-08 retrospective row left intact — it documents how the collision was resolved and removing it would erase the audit trail (per `mem://index.md` Core: "always list remaining tasks at end of session"; per Phase 39b lesson: historical narrative inside `*-blind-ai-gap-analysis.md` is the audit trail, not actionable). **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. |
| 3.9.3 | 2026-04-28 | **Phase P4 — GAP-V2-10 closed: pre-parse caps & validation order surfaced in §04.** User reply `next` (P4 of the new phased plan). **§04 v2.9.5 → v2.9.6**: new `### 1.2 Pre-parse caps & validation order (v2.9.6 — Phase P4)` subsection added immediately after §1.1. Surfaces the four `ConfigKv` enforcement caps in a single table (`RatePerMinPerProfile=60` / `GL-RATE-LIMIT-EXCEEDED` 429 with `Retry-After`; `MaxPushPayloadBytes=1048576` / `GL-PAYLOAD-TOO-LARGE` 413; `MaxLinesPerPush=10000` / `GL-LINES-TOO-MANY` 413; `MaxLineBytes=65536` / soft-truncate per AC-27 with no error code) and pins the 11-step strict validation order: TLS/SSH-sig → Auth → rate-limit → Content-Length pre-check → body parse → streaming running-total cap → line-count cap → per-line truncation → field validation → GitProfile acceptance match → atomic INSERT (per AC-75 `BEGIN IMMEDIATE`). Documents the orthogonal `AppendLogMaxStreamSec` (recommended 30s) wall-clock cap with `GL-INGEST-TIMEOUT` for slow-loris defense, scoped to streaming requests only (buffered requests are bounded by `Content-Length`). **Closes GAP-V2-10** (§37 row to be marked ✅ on Phase P8 refresh). **Cross-impact**: blind implementers reading §04 top-to-bottom now learn gate ordering WITHOUT cross-walking §15 (error codes), §18 (ConfigKv defaults), and §97 (AC-12/AC-27/AC-13/AC-75) — eliminates a known top-3 implementation pitfall (out-of-order rate-limit vs payload-size checks). **Scope discipline (Phase P4 ONLY)**: §15 / §18 / §97 untouched — no new error codes, no new ConfigKv keys, no new ACs. The four caps and their codes were already authoritative in those files; §1.2 is a cross-walk-elimination doc surface. §01 / §02 untouched. §17 OpenAPI untouched (the validation order is server-side behavior, not wire-format — clients do not need to encode the order in their request). **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. |
| 3.9.2 | 2026-04-28 | **Phase P3 — GAP-V2-04 closed: `PreviousHasError` added to Standard Ack Envelope + OpenAPI `AckResponse`.** User reply `next` (P3 of the new phased plan). **§04 v2.9.4 → v2.9.5**: `PreviousHasError: boolean` field added to Standard Ack Envelope JSON example AND a full field-contract subsection (~25 lines) covering: type/required (write endpoints #1–#4 only; ABSENT on read endpoints #5–#10); semantics (true iff prior `Pipeline.PreviousHasError` for `(RepoVersionId, BranchName, PipelineName)` was 1 immediately before the current request; fresh triple → false, mirrors AC-73 `first-failure` boundary); per-endpoint usage (#1 enables AC-13 auto-`/fixed-log` chaining, #2 detects no-op fixed calls, #3/#4 echo pre-clear state for audit correlation); atomicity (MUST be read in same `BEGIN IMMEDIATE` SQL transaction as the write per AC-75 ORM-split fallback rules); cross-refs to §01 glossary + §97 AC-13/73/74/75 + §17. **§17 v2.9.4 → v2.9.5**: `AckResponse.PreviousHasError` added as REQUIRED boolean property (added to `required: [...]` list); description block reuses §04 contract verbatim plus deep-link to "PreviousHasError field contract" subsection. **Closes GAP-V2-04** (§37 row to be marked ✅ on Phase P8 refresh). **Cross-impact unblocked**: any client implementing AC-13 auto-fix can now read the field directly from the ack instead of doing a follow-up `GET /get-pipeline-logs` to recover prior state. **Scope discipline (Phase P3 ONLY)**: §97 ACs untouched — AC-13 already references the auto-fix behavior; no AC ID added/removed. §15 untouched (no new error codes — missing `PreviousHasError` is a server-side schema bug, not a client error). §01 glossary `Pipeline.PreviousHasError` entry already authoritative from Phase 9 (v2.9.2). §02 / §18 untouched (no DDL change — the field is computed from existing `Pipeline.PreviousHasError` column). **Verified**: `node linter-scripts/check-lockstep.cjs` ✅ 87/87; `node linter-scripts/check-tree-health.cjs --strict` ✅ 168/168. |
| 3.9.1 | 2026-04-28 | **Phase P2 — GAP-V2-03 closed: NDJSON streaming wire format pinned for `/append-log` ingest.** User reply `next` (P2 of the new phased plan). Appended `### 1.1 Streaming wire format (v2.9.4 — Phase P2)` to `04-rest-api-endpoints.md` v2.9.3 → **v2.9.4** specifying: opt-in sentinel `X-GL-Stream: 1` request header; required `Content-Type: application/x-ndjson; charset=utf-8` + `Transfer-Encoding: chunked` (with `Content-Length` MUST be absent); LF-only frame separator; three-frame contract (`StreamHeader` exactly-one carrying identity fields ex `Logs`/`ErrorLogs`/`HasError`; `Line` zero-or-more with `{Line, Severity}` mirroring §01 `LogSeverity`; `StreamFooter` exactly-one with authoritative `HasError` boolean making "all-Info lines + final HasError=true" lossless); strict server validation (header-before-line ordering, EOF-without-footer rollback, unknown-discriminator rejection, forward-compatible unknown-key tolerance); reuses §11.4 `NdjsonMaxRowsPerStream` cap for ingest backpressure; buffered (not streamed) standard ack response with computed `Retrieval` hint. Introduces 4 new `GL-*` error codes scoped to ingest streaming: `GL-STREAM-NO-HEADER` (400), `GL-STREAM-NO-FOOTER` (400), `GL-STREAM-TOO-MANY-LINES` (413), `GL-STREAM-UNKNOWN-FRAME` (400). **Closes GAP-V2-03** (§37 row to be marked ✅ on Phase P8 refresh). **Cross-impact unblocked**: `28-universal-ci-cli/06-log-shipping-contract.md` AC-28-06 can now cite a pinned byte-level contract instead of a behavioral one. **Scope discipline (Phase P2 ONLY)**: §15 error codes file, §17 OpenAPI, §97 ACs untouched — they will be lockstep-bumped in their own dedicated micro-phase if/when the user requests OpenAPI/AC parity. The §1.1 doc-side contract is the source of truth for the next downstream consumer (`§28/06`); §15/§17 catch-up is a follow-on, not a blocker. §11 retrieval contract (Phase 8/12) untouched. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅; `node linter-scripts/check-tree-health.cjs --strict` ✅. |
| 3.9.0 | 2026-04-28 | **Phase P1 — GAP-V2-02 closed: TypeScript enum mirror in §01.** User reply `next` (P1 of the new phased plan). Appended `## TypeScript Mirror` section to `01-glossary-and-enums.md` v3.8.10 → **v3.9.0** containing a drop-in `ts` fenced block (~135 lines) with every enum from the §Enum Catalog (`UserStatus`, `Role`, `Permission`, `Provider`, `Acceptance`, `AppStatus`, `AppLinkType`, `LogSeverity` + `LogSeverityNumeric` map, `PipelineActionType`, `SystemEventType`, `AuditActionType` incl. `ConfigChange` seed id 25, `AuditOutcome`, `OwnerType_DEPRECATED_v380` stub for v1 clients). Also added a 4-row "Drift-detection contract" table making `18-schema.sql` lookup-table seeds the canonical authority and demoting the TS block to a hand-maintained mirror — out-of-band drift becomes a §99 audit signal (✅ → ⚠️) and any value present in SQL but missing in the TS block MUST raise `GL-SCHEMA-DRIFT` at boot. **Closes GAP-V2-02**: §37 blind-AI gap analysis row will be marked ✅ on the §37 refresh phase (P8). **Score impact (estimated)**: §22 implementability +2 (`has_ts_enums = false` → `true`), removes one §99 finding. **Scope discipline (Phase P1 ONLY)**: §02 / §18 / §15 / §97 untouched (the TS mirror is read-only documentation; no DDL change, no AC ID change, no `GL-*` code addition); §15's existing `GL-SCHEMA-DRIFT` is reused as the drift escalation channel — no new error code needed. **Verified**: `node linter-scripts/check-lockstep.cjs` ✅; `node linter-scripts/check-tree-health.cjs --strict` ✅; `python3 linter-scripts/check-spec-cross-links.py` ✅. |
