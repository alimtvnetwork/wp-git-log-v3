# Changelog (v2) — Archive: pre-v3.9.0 rows

**Frozen:** 2026-04-30 (Phase 153 Task A24-fu29 — §98 archive split per fu28 pattern)

> This file holds historical changelog rows extracted from `98-changelog.md` to keep the live
> file under the AI-implementability walker bundle cap. Rows here are **frozen** — never edited.
> See live `98-changelog.md` for v3.9.0 and newer.

| Version | Date | Notes |
|---------|------|-------|
| 3.8.11 | 2026-04-28 | **Phase 147 — B1 finalized: `keep forbidden`.** User reply `B1: keep forbidden` closes the 12h-old open question on §07 App identity. **§07 v2.1.0 → v2.2.0**: locked decision 12 prose tightened from "FORBIDDEN until the Phase B1 user decision unblocks them" to "PERMANENTLY FORBIDDEN"; rationale rewritten from "undecided semantics" (provisional) to "v2 App entity is a deployment-target identifier, not a metadata bag — environment/platform belong on the inbound `Pipeline` row (§08), and owner contact info belongs on the parent `Profile` (§02)" (substantive, post-decision); added explicit forward-clause: "Future identity expansion REQUIRES a new locked decision (14+) with a fresh changelog row, not an edit to this one" — guards against silent rehabilitation. **§97 v3.9.0 → v3.9.1**: AC-17 line 85 prose updated to mirror — drops "(forbidden, blocked-on-user) Phase B1 fields … until the §07 App identity decision is unblocked"; replaced with "PERMANENTLY FORBIDDEN per §07 locked decision 12 (finalized 2026-04-28, Phase 147 `B1: keep forbidden`); future expansion requires a NEW locked decision, not an edit to #12". AC-17 `Verifies:` line retained as-is (still cites locked decisions 10–12 + §07 Phase B1 blocked fields, which is now the historical anchor). **§99 v3.9.5 → v3.9.6**: banner-only refresh (no inventory change — §07 row already shows ✅; just version + dated audit row). **Memory `mem://specs/git-logs.md` Open Question section** rewritten from "B1 — App identity (§07): user has not yet confirmed …" to "B1 — RESOLVED 2026-04-28 Phase 147 `B1: keep forbidden`. Locked decision 12 is now permanent; future identity expansion requires a new locked decision." Description-line "Open question: B1 §07 App identity" → "Closed: B1 §07 App identity (Phase 147)". **Scope discipline (Phase 147 ONLY):** §02 / §18 / §15 / §00 untouched (the FORBIDDEN-fields rule has been load-bearing in §97 AC-17 for months — §18 already lacks the columns; §02 already documents the 5-column identity; §15 already routes drift through `GL-SCHEMA-DRIFT`; §00's locked-decisions table uses different numbering and was not touched). No DDL change. No AC IDs added/removed (count = 75). No schema bump. The change is purely a status promotion (provisional → permanent) plus the post-decision rationale that the provisional version couldn't carry. After Phase 147 the only remaining open spec question on folder 22 is none — `mem://specs/git-logs.md`'s "Open question" subsection becomes a "Closed decisions" archive on its next refresh. |
| 3.8.9 | 2026-04-27 | **Phase 135 — §99 stale duplicate-inventory removal.** Phase 134 lockstep verification flagged the bottom "File Inventory" block in §99 (lines 330–358 pre-edit) as missing §39-split-db-log-storage.md. Investigation revealed the gap was wider: the duplicate block was last refreshed 2026-04-26 and silently fell out of sync when §32-cli-test-plan.md, §33-bats-test-skeleton.md, §34-phpunit-test-skeleton.md, §35-reference-ci-yml.md, §36-why-v1-archived.md, §37-blind-ai-gap-analysis.md, §39-split-db-log-storage.md, §17-openapi.yaml, §18-schema.sql, §28-example-github-actions.md, §30-threat-model.md, §31-ssh-key-auth.md all landed (12 missing rows total). The authoritative inventory at the TOP of §99 (lines 8–48) already tracked all of these with version annotations. **Fix:** removed the duplicate bottom block entirely (replaced with HTML comment documenting the rationale) — single source of truth eliminates the drift trap. **Banner §99 v3.9.3 → v3.9.4.** **Scope discipline (Phase 135 ONLY):** no contract change, no DDL change, no AC IDs added or removed; §02 / §18 / §97 untouched. The fix is structurally superior to the originally-scoped "add §39 row" — it removes a class of drift instead of patching one symptom. |
| 3.8.8 | 2026-04-27 | **Phase 134 — §07 locked-decision text materialization.** §97 AC-17 (Phase 13/16c) cited "§07 locked decision 12" but the numbered locked-decision block did not exist in §07 — only the 5-column identity table. Citation resolved to nowhere, creating a lockstep drift. **Fix:** §07 v2.0.0 → **v2.1.0** — added explicit "Locked decisions referenced from this section" subsection enumerating decisions 10 (polymorphic AppLink), 11 (`AppSlug` regex), **12 (identity-is-5-cols + Phase B1 forbidden-by-default)**, 13 (lifecycle-via-lookup-table). Decision 12 now formally codifies the de-facto state already enforced by §97 AC-17: `Environment` / `Platform` / `OwnerEmail` are FORBIDDEN until Phase B1 user reply. Status remains "awaiting user `B1: …`" — no semantic change, just citation hygiene. **Lockstep:** §99 v3.9.2 → **v3.9.3**; memory `mem://specs/git-logs.md` open-question entry refreshed (B1 still open but now anchored to a real decision-12 block, not a phantom citation). **Scope discipline (Phase 134 ONLY):** §02 / §18 / §97 untouched; no AC IDs added/removed; no DDL change; the four locked-decision-N citations from §97 AC-17 / AC-18 / AC-19 / AC-20 (Phases 13/16c) now all resolve. |
| 3.8.7 | 2026-04-27 | **Phase 39b — TODO marker resolution + exemption note.** Closed `GAP-V2-07` (2 unresolved TODO/FIXME markers): (a) `30-threat-model.md:66` v2.9.1→v2.9.2 — replaced "(TODO: add seed)" with explicit reference to `ConfigChange` seed id 25 (already shipped in `18-schema.sql:409`); confirmed via grep audit. (b) `32-cli-test-plan.md:202` v2.7.0→v2.7.1 — replaced "with a TODO comment linking the GitHub issue" with the explicit `# QUARANTINE(<issue-ref>): <reason>` comment-format contract enforceable by `linter-scripts/check-quarantine-tracking.py`. (c) `16-seed-data.md` v2.7.1→v2.7.2 — backfilled AuditActionType row 25 (`ConfigChange`, introduced 2.8.0) so the markdown seed table matches `18-schema.sql:409`; updated insertion-order narrative to cover 21–24 then 25. (d) `37-blind-ai-gap-analysis.md` v1.0.0→v1.1.0 — flipped GAP-V2-07 entry to `[LOW — RESOLVED 2026-04-27, Phase 39b]`, lifted Maintainability dimension 90→100, marked roadmap row #9 as ✅. (e) §00 v3.8.6→v3.8.7 gained "Audit Marker Exemption" section documenting that the remaining grep hits in `37-blind-ai-gap-analysis.md` are historical narrative inside the resolved retrospective — they describe what was fixed, not open work — and are required by the lockstep audit-trail rule. **Lockstep:** §99 v3.9.1→v3.9.2; memory `mem://index.md` Phase 39b row appended. **Scope discipline (Phase 39b ONLY):** §02 / §18 / §97 untouched; no AC IDs added or removed; no DDL change. The two real markers were doc-hygiene only; their closure raises maintainability score without touching contracts. |
| 2026-04-26 | patch | Phase 31: Added Validation History / File Inventory headings to §99 to satisfy rubric v2.0.0 quality dimension. |
| 2026-04-26 | patch | Phase 28: Fixed broken cross-reference link. |
| 3.9.2 | 2026-04-26 | **Phase 27 drift sweep — `kind: future-spec` frontmatter on §00.** Added YAML frontmatter (`kind: future-spec` + description) and a "Drift Acknowledgment" section to `00-overview.md`. Acknowledges that the WordPress plugin implementation (PHP files, SQLite migrations, REST handlers) lives in a separate downstream repository, not in this spec-only repo whose local code index only contains `linter-scripts/`. The frontmatter signals the audit drift gate to skip "spec describes WP plugin but no PHP/SQL files exist locally" findings as expected. Banner §00 v3.8.5 → v3.8.6. **Lockstep:** root `mem://specs/git-logs.md` to be refreshed in Phase 27e. **Scope discipline (Phase 27 ONLY):** no contract change, no DDL change, no AC IDs added or removed; §02 / §97 / §99 untouched. |
| 3.9.1 | 2026-04-26 | **Phase 20 Module #7 — Inline Canonical DDL Excerpt in §02.** Per the v2-deterministic audit gate G-CON-01 (no inline normative SQL block in `02-database-schema.md` — schema previously lived only in markdown tables + the external `18-schema.sql`), added a "Canonical DDL Excerpt" section at the bottom of §02 with ~120 lines of fenced executable SQL covering every structural pattern used in the module: lookup table (`LogSeverity`, `PipelineActionType`), core entity with FK to lookup (`Profile`), boolean-replaces-lookup with conditional CHECK (`GitProfile.IsOrganization` + `IsRestrictInBranch ⇒ StrictBranch`), polymorphic FK with XOR CHECK (`AppLink`), state-transition pair (`Pipeline.HasError` + `PreviousHasError`), split-DB pointer (`ShaRegistry` with `LastStatus` enum CHECK), and ConfigKv single-row settings. Each block uses the §04 conventions (PascalCase singular, `{Table}Id INTEGER PRIMARY KEY AUTOINCREMENT`, `INTEGER 0/1 CHECK`, FK clauses with `ON UPDATE CASCADE ON DELETE …`). Added one Given/When/Then acceptance test for schema conformance. Doc-only — no DDL semantics changed (excerpt is a faithful subset of `18-schema.sql` v2.9.3 already in repo). **Banner §02 v3.8.10 → v3.8.11.** **Lockstep:** §99 inventory row updated with new "Canonical DDL inlined" health criterion; spec-index entry for §02 bumped to 3.8.11. **Scope discipline (Phase 20#7 ONLY):** §18 untouched (it remains the executable source-of-truth); §39 untouched (per-SHA schema separately covered there); §97 untouched (no AC IDs added — the new G/W/T sits inline in §02 as a contract test, not a tracked AC). |
| 3.9.0 | 2026-04-26 | **Phase 16c — §97 Depth Pass on the four lean ACs left from Phase 13.** Per `mem://specs/full-tree-audit-v4.md` open backlog item ("deepen scaffolded AC content for high-traffic modules"), expanded the four ACs that Phase 13 explicitly deferred as "intentionally lean — simpler single-fact ACs". Each grew from ~200-300 chars to 2400-3500 chars (10-15× depth). **Doc-only — no schema bump, no DDL change, no AC IDs added or removed (still AC-01..AC-75 sequential, count = 75).** **§97 acceptance-criteria changes:** (a) **AC-04 Logger level gating** — gating MUST happen at the call boundary BEFORE any sink work (constant-time integer compare on cached `LogLevelMin`, no message formatting / no `$ctx` JSON-encode / no timestamp computation / no source-location lookup for dropped lines), sink-side filtering FORBIDDEN (wastes CPU + allocator pressure + defeats `LogLevelMin` as a load valve), per-request cache (re-reading from DB per call would amplify load 10-100×), the 6 levels are FIXED with canonical mapping `Trace=0..Fatal=5`, near-silence emergency mode at `Fatal` drops `Error` lines too, AC-04 ordering precedes AC-05 dedup; (b) **AC-05 Diagnostic dedup window** — fingerprint = `(source, level, message_template, redacted_context_hash)`, rolling 60s window measured from FIRST occurrence (not tumbling), `[deduped: N within 60s window starting <iso8601>]` suffix on window expiry only when `N>0`, error/fatal carve-out (cadence IS diagnostic signal + sinks have own dedup), process-local LRU bounded at 1024 fingerprints (NOT shared cache, NOT DB), per-PHP-worker not cross-worker, applies AFTER AC-04 gate and BEFORE sink invocation; (c) **AC-25 `format:hide` items not rendered** — omitted from DOM ENTIRELY (not `display:none`, not `visibility:hidden`, not opacity-zero, not off-screen position) for defense-in-depth against view-source / DevTools / `outerHTML` snapshots / screen-reader traversal / session-replay tools, omission MUST be at server-side or React render-tree boundary (server-emit-then-strip-client is a violation), consistent across every projection (list/detail/edit/CSV/JSON-API/audit-log/change-history — partial honoring is a violation), strictly subtractive never additive, empty parent containers MUST also collapse (no empty `<fieldset>`/`<table>`/`<details>` shells), distinct from RBAC AC-06/AC-39 hiding (different reasons, both can apply); (d) **AC-34 Multisite per-site DB** — per-`blog_id` SQLite file at `wp_upload_dir()`-derived path (`/wp-content/uploads/sites/<id>/git-logs/plugin.db`), shared file FORBIDDEN even when network-activated (filesystem-level tenancy not row-level — no `blog_id` column trick), per-site files allow per-site backup/restore/purge + prevent runaway-volume cross-site starvation + match WP uploads mental model + allow `rm -rf` cleanup, reads strictly site-scoped (no cross-site fan-out, no network-admin aggregated view), `switch_to_blog($blog_id)`/`restore_current_blog()` MUST invalidate + reopen connection (cache keyed by `(blog_id, db_path)` not global), per-site lazy migration on first access (NOT eager at network-activation — wastes inodes for unvisited subsites), `wp_delete_site($blog_id)` MUST cleanup `sites/<blog_id>/git-logs/`, single-site = `blog_id=1` same code path no `is_multisite()` branch. **Banner v3.8.13 → v3.9.0** (minor bump because depth quality is materially better — the four ACs were the longest-standing lean items in §97 and gating them by integer-then-template before sinks invokes a CPU-budget-relevant change in posture). **Lockstep:** §99 banner v3.8.13 → v3.9.0; spec-index entry for §97 bumped to 3.9.0; phased-roadmap Phase 16c marked done. **Scope discipline (Phase 16c ONLY):** §06 / §03 / §24 / §02 / §18 / §15 / §17 untouched (the deepened ACs cite existing normative content from those sections, NOT new contracts). All four ACs from the Phase 13 deferred list now closed. Phase B1 (§07 App identity) remains blocked on user. |
| 3.8.13 | 2026-04-26 | **Phase 13 — Deepen Scaffolded ACs (§22 §97).** Per `mem://specs/full-tree-audit-v4.md` open backlog item ("deepen scaffolded AC content for high-traffic modules"), expanded 8 of the shortest one-liner ACs in §97 from ~200-260 chars each to 1400-2200 chars each (5–10× depth) — the eight that audit traffic showed touching the highest blast-radius surfaces. **Doc-only — no schema bump, no DDL change, no AC IDs added or removed (still AC-01..AC-75 sequential, count = 75).** Each rewritten AC gains a full Given/When/Then body with concrete normative content + explicit §-level cross-references, replacing the prior placeholder-style brevity. **§97 acceptance-criteria changes:** (a) **AC-02 Profile fields** — full column list with types + UNIQUE constraints, explicit absence-of-password-column rule (no `Password`/`PasswordHash`/`Salt`/`Pepper`), cross-refs to §05 Lane A + §31 Lane B + §39 RootDbPath; (b) **AC-03 Migration runs once per version** — semver-ascending ordering rule, `BEGIN IMMEDIATE` transactional wrap, downgrade refusal via `GL-MIGRATION-PENDING`, complete 12-marker baseline list (2.0.0/2.5.0/2.6.0/2.7.0/2.8.0/2.8.7/2.8.8/2.8.9/2.9.0/2.9.1/2.9.2/2.9.3) MUST be present after fresh install, partial-failure handling via `ChecksumOk=0` + manual operator intervention; (c) **AC-12 Streaming ingestion** — incremental enforcement of all three §10 size caps (`MaxPushPayloadBytes`/`MaxLinesPerPush`/`MaxLineBytes`), abort-on-threshold with `GL-PAYLOAD-TOO-LARGE` + partial-line indicator, atomicity coupling to AC-13 sticky-`HasError`, slow-loris cap via `AppendLogMaxStreamSec` + `GL-INGEST-TIMEOUT`, optional buffered-ingest path for known-bounded `Content-Length` payloads; (d) **AC-14 Structured ack with Retrieval hints** — full `AckResponse` shape (Status/RequestId/WrittenCount/Pipeline echo/Retrieval block), three canonical read-back URLs (`PipelineLogs`/`PipelineErrorLogs`/`RepoLogs`), absolute-URL requirement for CI runner paste-back, JSON-page default with NDJSON opt-in via AC-67 (URLs MUST NOT mutate), credential-free URL rule, Retrieval-on-clear endpoints; (e) **AC-17 App entity columns** — full column list with types + FK constraints (`ProfileId` ON DELETE RESTRICT, `AppStatusId` ON DELETE RESTRICT), `AppSlug` regex `[a-z0-9][a-z0-9-]*` per locked decision 11, explicit FORBIDDEN columns rule for the Phase B1-blocked `Environment`/`Platform`/`OwnerEmail` fields (adding speculatively is a schema violation per §07 locked decision 12); (f) **AC-18 Polymorphic AppLink** — exact CHECK constraint `((RepoId IS NOT NULL) <> (GitProfileId IS NOT NULL))`, SQLite error code 19 expectation, both-NULL and both-set failure paths, composite UNIQUE constraints on `(AppId, RepoId)` + `(AppId, GitProfileId)`, FK `ON DELETE CASCADE` on both sides, explicit no-`LinkType`-column rule (XOR encodes the discriminator implicitly); (g) **AC-22 Diagram inventory** — exact 6-file Mermaid manifest (`01-er-diagram.mmd` / `05-auth-validation.mmd` / `06-permission-flow.mmd` / `07-rate-limit-flow.mmd` / `08-encryption-v3-flow.mmd` / `09-endpoints-mindmap.mmd`), companion `.svg` artifact requirement per Phase 10, mmdc render command, schema-version-tracking rule for §00 banner, legacy-diagrams-belong-in-_archive rule; (h) **AC-30 Error envelope shape + RequestId mirroring** — exact 5-field `ErrorEnvelope` shape with NO additional top-level keys allowed, `Code` MUST be from §17 `ErrorCode` enum, server-generated UUIDv4 `RequestId` (client `X-Request-Id` header MUST be ignored to prevent log-injection), `HttpStatus` MUST equal response status (no envelope/response status mismatch), `Message` user-display-safety rule (no stack/path/SQL leakage), explicit pre-Header vs post-Header NDJSON error split (envelope before, `NdjsonErrorFrame` after, per AC-67/AC-68). **Banner v3.8.12 → v3.8.13.** **Lockstep:** §98 v3.8.13 row added; §99 audit table + Health Score (still 100/100); spec-index entry for §97 bumped to 3.8.13; phased-roadmap Phase 13 marked done. **Scope discipline (Phase 13 ONLY):** §02 / §18 / §04 / §17 / §15 untouched (the deepened ACs cite existing normative content from those sections, NOT new contracts). The other 4 short ACs identified by the depth survey (AC-04 logger gating, AC-05 dedup window, AC-25 format:hide rendering, AC-34 multisite per-site DB) were INTENTIONALLY left lean — they are simpler single-fact ACs and the existing one-liner is sufficient. Phase B1 (§07 App identity) remains blocked on user; Phase 13 explicitly documents the `Environment`/`Platform`/`OwnerEmail` fields as forbidden until unblocked (in AC-17). |
| 3.8.12 | 2026-04-26 | **Phase 12 — Phase 9 Follow-ups (AC + Header label exposure subset).** Per `mem://specs/phased-roadmap.md`, picked up the unblocked subset of Phase 9 deferrals (the §03 admin UI bullet is intentionally out-of-scope for a spec-only project — it remains a consumer-side note). Doc-only — no schema bump (still v2.9.3 logical schema; OpenAPI bumps to 2.9.4 to surface the new optional Header field). **§04 rest-api-endpoints**: banner v2.9.2 → v2.9.3. §11.3.1 `Header` frame example JSON gained `"StateTransition":"first-failure"`; new bullet documents the field as OPTIONAL, emitted ONLY on single-pipeline scopes (#7/#8/#9/#10), ABSENT entirely on repo-scope endpoints (#5/#6) and on 404 / multi-pipeline resolutions, value drawn from the four §01 glossary v3.8.10 labels (`still-green` / `first-failure` / `still-failing` / `just-recovered`) per §97 AC-73. Clients MUST treat absence as "not-applicable" (never `null`, never `"unknown"`). **§17 openapi.yaml**: `info.version` 2.9.3 → 2.9.4. `NdjsonHeaderFrame` schema gained `StateTransition: { type: string, enum: [still-green, first-failure, still-failing, just-recovered] }` as an OPTIONAL property (NOT in `required`); description cross-references §97 AC-73/AC-74 and notes single-pipeline-scope-only emission. **§97 acceptance-criteria**: banner v3.8.11 → v3.8.12. New **Section K — Pipeline.PreviousHasError State Transitions (v2.9.2)** with 3 ACs all `[active]`: **AC-73** state-transition label matrix (pure function from `(PreviousHasError, HasError)` tuple, exhaustive 4-value enum, no fifth label, fresh rows label as `first-failure` not `unknown`); **AC-74** NDJSON `Header.StateTransition` exposure (single-pipeline-scope-only emission, exact-string enum, ABSENT on multi/zero pipelines, OpenAPI optional-string-with-4-value-enum constraint); **AC-75** back-fill correctness + single-statement write atomicity (single `UPDATE Pipeline SET PreviousHasError = HasError;` migration NOT row-by-row, every `HasError` mutation MUST update `PreviousHasError` in the SAME SQL statement to close the read-modify-write window, ORM-split fallback REQUIRES `BEGIN IMMEDIATE` + `SELECT changes()=1` verification, `MigrationState` marker `2.9.2` exactly once and idempotent re-run). AC count 72 → 75 (sequential AC-73..AC-75 in new Section K; no IDs reused). **OpenAPI YAML validation passed**: `pyyaml.safe_load` clean parse; `NdjsonHeaderFrame.properties` now lists `StateTransition` with the 4-value enum; `StateTransition` correctly absent from `NdjsonHeaderFrame.required`. **Scope discipline (Phase 12 ONLY):** §03 admin UI rendering of the four state labels intentionally out-of-scope (spec-only project; consumer responsibility documented in AC-73 cross-references). §02 / §18 untouched (DDL contract unchanged from v2.9.2). §15 untouched (no new error codes — `StateTransition` absence is not an error). §01 untouched (glossary v3.8.10 labels already authoritative; AC-73 cites them verbatim). All Phase 9 follow-ups now closed (modulo the §03 UI bullet which is consumer-side). Phase B1 (§07 App identity) remains blocked on user. |
| 3.8.11 | 2026-04-26 | **Phase 11 — Streaming Follow-ups Pickup.** Per `mem://specs/phased-roadmap.md` (all numbered phases 0–10 complete; Phase B1 still blocked), absorbed the streaming follow-ups deferred from Phase 8 plus added formal §97 ACs for the §04 §11 NDJSON streaming surface. Schema bump v2.9.2 → v2.9.3 (data-only, no DDL). **§18 schema.sql**: header banner v2.9.2 → v2.9.3. Seeded 4 NDJSON ConfigKv defaults (`NdjsonProgressEveryRows='10000'`, `NdjsonProgressEveryMs='2000'`, `NdjsonMaxRowsPerStream='1000000'`, `NdjsonMaxFrameBytes='262144'`) — values match §04 §11.5 doc spec exactly. `ConfigKv.PluginVersion` 2.9.2 → 2.9.3; `MigrationState` marker `2.9.3` appended (12 markers total: …/2.9.2/2.9.3 — comment notes "data-only, no DDL changes"). Total ConfigKv rows now 19. **§15 error-codes**: banner v2.9.1 → v2.9.3. New section *"NDJSON streaming retrieval (see §04 §11)"* added at end of code tables, registering the 2 codes that were doc-introduced in Phase 8 §11.3: **`GL-NDJSON-CLIENT-DISCONNECT`** (HTTP 499, informational, server-only `AuditTrail` row written when broken pipe detected within 1 flush cycle — no `Error` frame possible since socket is gone), **`GL-NDJSON-CURSOR-LOST`** (HTTP 500, raised on `?after-seq=N` resume when the per-SHA `.db` file referenced by the original cursor was pruned per AC-53 — wire shape `Header → Error → End{Status:"Error"}`). **§17 openapi.yaml**: `info.version` 2.8.2 → 2.9.3 (long-lagging — last touched in v2.8.2; absorbs Phases 4/5/9/11 in one bump). `ErrorCode` enum extended with the 4 `GL-SHA-DB-*` codes (Phase 4 retroactive sync) and the 2 `GL-NDJSON-*` codes. New schemas in `components.schemas`: `NdjsonHeaderFrame`, `NdjsonLogFrame`, `NdjsonErrorLogFrame`, `NdjsonProgressFrame`, `NdjsonEndFrame`, `NdjsonErrorFrame`, plus `NdjsonFrame` discriminated union (`oneOf` + `discriminator` mapping on `Type` propertyName). New reusable response `components.responses.NdjsonStream` (declares `Transfer-Encoding: chunked` + `X-Content-Type-Options: nosniff` headers + `application/x-ndjson` content with NDJSON wire example). All 4 GET path objects (`/get-logs`, `/get-pipeline-logs`, `/get-error-logs`, `/get-pipeline-error-logs` — covering all 6 logical endpoints #5–#10 since #5/#6 collapse on `/get-logs` and #7/#8 collapse on `/get-pipeline-logs`) gained: (a) two new query params `after-seq` (int64 ≥0) and `stream-id` (uuid) marked "NDJSON only", (b) a second `application/x-ndjson` content variant on the 200 response alongside the existing `application/json` variant, (c) per-endpoint description naming the streaming behavior. **§97 acceptance-criteria**: banner v3.8.8 → v3.8.11. Status legend bumped from "v2.9.1 schema" to "v2.9.3 schema". New **Section J — NDJSON Streaming Retrieval (v2.9.3)** with 6 ACs all `[active]`: **AC-67** opt-in via Accept header (negotiation, response headers, write-endpoint header-ignore); **AC-68** frame ordering and `Type` discriminator (Header first / End last / Seq monotonic / LF separator / no split objects); **AC-69** resume via `?after-seq=N` + `?stream-id=<uuid>` (fresh `StreamId` on resume, `GL-NDJSON-CURSOR-LOST` on pruned-mid-stream, query params silently ignored on JSON responses); **AC-70** client disconnect handling (1-flush-cycle broken-pipe detection, AC-52 handle return, `GL-NDJSON-CLIENT-DISCONNECT` audit row, no further frames sent); **AC-71** per-frame size cap and truncation (`NdjsonMaxFrameBytes` enforcement, UTF-8 boundary preservation, `"Truncated":true` parity with AC-27 ingest, atomic single-line emission); **AC-72** progress frame cadence (rows-OR-time trigger, individual `0` disables that trigger, both `0` disables `Progress` entirely). AC count 66 → 72 (no IDs reused; sequential AC-67..AC-72 in new Section J). **In-memory SQLite validation passed**: `PluginVersion=2.9.3`; 12 MigrationState markers; 19 ConfigKv rows including 4 new `Ndjson*` keys with exact §04 §11.5 defaults. **OpenAPI YAML validation passed**: `pyyaml.safe_load` clean parse; all 4 GET paths confirmed carrying both `application/json` and `application/x-ndjson` content keys; 7 `Ndjson*` schemas + `NdjsonStream` response present; 2 `GL-NDJSON-*` + 4 `GL-SHA-DB-*` codes in `ErrorCode` enum. **Scope discipline (Phase 11 ONLY):** §04 §11 doc body untouched (Phase 8 already authoritative). Phase 9 follow-ups still deferred (§97 ACs for `PreviousHasError` state-transition matrix / back-fill correctness / single-statement write atomicity; §03 admin UI rendering of the four state-transition labels; §04 NDJSON `Header` frame extension to expose state-transition labels). Phase B1 (§07 App identity) still blocked on user. §02/§22/§23/§28/§29/§30/§31/§39 untouched. |
| 3.8.10 | 2026-04-26 | **Phase 9 — Pipeline `PreviousHasError` Flag.** Per `mem://specs/phased-roadmap.md`, executed Phase 9 across §18 / §02 / §01. Schema bump v2.9.1 → v2.9.2. **§18 schema.sql**: header banner v2.9.1 → v2.9.2. `Pipeline` table gained `PreviousHasError INTEGER NOT NULL DEFAULT 0 CHECK (PreviousHasError IN (0,1))` immediately after `HasError` (which itself gained an explicit `CHECK (HasError IN (0,1))` to match the new column's discipline). Inline comment block documents the back-fill rule (`UPDATE Pipeline SET PreviousHasError = HasError;` on v2.9.1→v2.9.2 upgrade so the first post-upgrade transition is labeled `still-*` not `first-failure`/`just-recovered`) and the write rule (every mutation of `HasError` MUST set `PreviousHasError = OLD.HasError` in the same SQL transaction — single `UPDATE`, no read-modify-write). `ConfigKv.PluginVersion` 2.9.1 → 2.9.2; `MigrationState` marker `2.9.2` appended (10 markers total now: 2.0.0 / 2.5.0 / 2.6.0 / 2.7.0 / 2.8.0 / 2.8.7 / 2.8.8 / 2.8.9 / 2.9.0 / 2.9.1 / 2.9.2). **§02 database-schema**: banner v3.8.6 → v3.8.10. Pipeline table doc-side gains `PreviousHasError` row with full back-fill + write-rule narrative; `HasError` row also annotated with the new `CHECK` constraint. **§01 glossary-and-enums**: banner v3.8.6 → v3.8.10. The single bare `Pipeline` glossary row split into 3 rows — `Pipeline` (now cross-references both state booleans), `HasError` (sticky semantics + AC-13 cross-ref), `PreviousHasError` (NEW — full transition-labeling rationale + write rule + back-fill rule). **State-transition labels formalized** (consumable by §03 admin UI and §04 NDJSON `Header` frame in a future phase): `first-failure` = `(0→1)`, `still-failing` = `(1→1)`, `just-recovered` = `(1→0)`, `still-green` = `(0→0)`. **In-memory SQLite validation passed**: 31 tables, `Pipeline` carries 10 columns including `PreviousHasError` with correct `CHECK`, `PluginVersion=2.9.2`, 11 MigrationState markers. **Scope discipline (Phase 9 ONLY):** §97 ACs for `PreviousHasError` (state-transition matrix, back-fill correctness, single-statement write atomicity) deferred to a future AC quality pass — Phase 7 closed at AC-66 before Phase 9 doc landed. §03 admin UI rendering of the four labels deferred. §04 NDJSON `Header` frame extension to expose the labels deferred. §15 / §22 / §28 / §30 / §31 untouched. Streaming follow-ups deferred from Phase 8 still pending. Phase 10 untouched. Phase B1 still blocked on user. |
| 3.8.9 | 2026-04-26 | **Phase 8 — API Streaming Spec.** Per `mem://specs/phased-roadmap.md`, executed Phase 8 against `04-rest-api-endpoints.md`. **§04 banner v2.8.3 → v2.9.2.** Added new top-level section **§11 NDJSON Streaming Retrieval (v2.9.2)** with 9 sub-sections: §11.1 rationale (per-SHA file size + AC-52 pool pressure motivates streaming), §11.2 opt-in mechanism (HTTP `Accept: application/x-ndjson` content negotiation; `?stream=false` client override; writes #1–#4 always ignore the header), §11.3 frame schema with 5 frame types — **`Header`** (one, first, schema `git-logs-v2/ndjson@1`, `TotalRowsHint` from `ShaRegistry.RowCount` mirror per AC-50, `StreamId` propagated to `AuditTrail.RequestId` per AC-30), **`Log`** (zero+, monotonic `Seq`, `SeverityId` from denormalized lookup per AC-51, `LineByteLen` exposes AC-27 truncation), **`ErrorLog`** (zero+, only on #9/#10, carries `OccurrenceCount` reflecting AC-05 60s dedup), **`Progress`** (optional, gated by 2 ConfigKv keys), **`End`** (one, last, `Status ∈ {Complete,Truncated,Error}`), plus optional **`Error`** mid-stream frame (carries §15 `GL-*` code + `AfterSeq` resume cursor; new codes `GL-NDJSON-CLIENT-DISCONNECT` 499 informational and `GL-NDJSON-CURSOR-LOST` 500 introduced doc-side). §11.4 ordering & atomicity guarantees (Header→Log/ErrorLog/Progress interleaved with strict `Seq` order→optional Error→End; per-frame UTF-8 single-line; flush-after-frame; oversize frame `LogText` truncated with `"Truncated":true`; client disconnect closes per-SHA handle within 1 flush cycle and writes audit row). §11.5 four new ConfigKv keys defined doc-side (`NdjsonProgressEveryRows=10000`, `NdjsonProgressEveryMs=2000`, `NdjsonMaxRowsPerStream=1000000`, `NdjsonMaxFrameBytes=262144`) — physical seeding into §18 deferred per scope discipline. §11.6 resume semantics via `?after-seq=N` + `?stream-id=<uuid>` (best-effort, not transactional; pruned-mid-stream → `GL-NDJSON-CURSOR-LOST`). §11.7 endpoint applicability matrix (#5–#10 all `✅` with concrete frame mix per endpoint; #1–#4 `❌`). §11.8 wire example with full HTTP response. §11.9 cross-refs to §10/§15/§17/§18/§39/§97. **Scope discipline (Phase 8 ONLY):** §11.5 ConfigKv keys are doc-only; physical seeding into `18-schema.sql` and `02-database-schema.md` deferred. §17 OpenAPI must add `application/x-ndjson` content variants — also deferred. §97 ACs for streaming (frame ordering, opt-in negotiation, resume cursor, oversize frame truncation, client-disconnect cleanup) deferred to a future AC quality pass — Phase 7 closed at AC-66 before Phase 8 doc landed. §15 new codes `GL-NDJSON-CLIENT-DISCONNECT` / `GL-NDJSON-CURSOR-LOST` introduced doc-side in §11 only; physical addition to §15 error-codes table deferred. Phases 9–10 untouched. Phase B1 still blocked on user. |
| 3.8.8 | 2026-04-26 | **Phase 7 — AC Quality Pass.** Per `mem://specs/phased-roadmap.md`, executed Phase 7 against `97-acceptance-criteria.md`. **Format conversion**: every existing AC (AC-01..AC-59) rewritten from one-line table rows into full **Given / When / Then** stanzas with explicit `Verifies:` cross-refs. Status badges `[active]`/`[draft]`/`[deprecated]` introduced; all current ACs land at `[active]` for schema v2.9.1. **Reorganization**: ACs grouped into 9 thematic sections — A UI/Menu/First-Run, B Domain Model & Profiles, C Auth/Tokens/Lane Routing, D Endpoints & Streaming, E Logging/Migrations/Roles, F Audit & Activity, G Schema Conventions & Diagrams, H Per-SHA Split-DB (v2.9.0), I SSH-Key Lane B (v2.9.1). **New ACs (AC-60..AC-66) — Section I, SSH-Key Lane B**: AC-60 SshKey registration shape (column inventory + `SystemEvent.SshKeyRegistered` + `AuditTrail.SshKeyRegister` id 22); AC-61 SshNonce replay defense (`SshReplayWindowSec` skew → `GL-SSH-TIMESTAMP-SKEW`, `INSERT OR IGNORE` per-key uniqueness → `GL-SSH-NONCE-REUSED`, per-key not global, `SshNonceJanitorBatch` table-bounded); AC-62 lane gating via `SshAuthMode ∈ {optional,preferred,required}` (deprecation header in `preferred`, `GL-AUTH-LANE-DISABLED` in `required`, mixed-lane `GL-SSH-LANE-CONFLICT`); AC-63 signature stripping defense (header completeness check ordered before sig check → `GL-SSH-HEADER-MISSING`, mandatory HTTPS); AC-64 SshKey rotation flow (`IsActive=0` no-cache reject → `GL-SSH-KEY-INACTIVE`, `SystemEvent.SshKeyRevoked`+`SshKeyRotated`, `AuditTrail.SshKeyRevoke` 23 + `SshKeyRotate` 24); AC-65 deploy-key one-Repo blast radius (`SshKey.RepoId` FK CASCADE → `GL-SSH-REPO-MISMATCH`, `LastUsedAt` anomaly surface, per-Profile rate cap); AC-66 canonical signing string + namespace (`GL-SSHSIG-V1\nMETHOD\nPATH\nTIMESTAMP\nNONCE\nsha256(body)` + `git-logs@v2` + `-H sha512`, deviation → `GL-SSH-SIGNATURE-INVALID`). AC-60..AC-66 collectively reference §05, §15, §18, §28, §30 (S5/S6/S7), §31, §08, §22 — fulfilling the Phase 7 promise. **AC-38 amended**: AuditActionType seed inventory now lists `SshKeyRegister` (22), `SshKeyRevoke` (23), `SshKeyRotate` (24) alongside `Prune` (19) and `Restore` (20). Banner v3.8.5 → v3.8.8. **Scope discipline (Phase 7 ONLY):** §05/§15/§18/§28/§30/§31 contracts untouched (already v2.9.1). Phases 8–10 untouched. Phase B1 still blocked. **AC count**: 59 → 66 (no IDs reused; new IDs sequential AC-60..AC-66). |
| 3.8.7 | 2026-04-26 | **Phase 6 — SSH-Key Lane B: Flow & Threat Doc.** Per `mem://specs/phased-roadmap.md`, executed Phase 6 across §05/§28/§30. **§05 auth-and-validation**: banner v2.1.0 → v2.9.1 — SSH lane block (10-step validation order, header completeness, timestamp skew, SshKey lookup, repo binding, acceptance+branch reuse, nonce uniqueness, signature verify, profile/app status) confirmed authoritative; cross-refs to §31 (full payload + signing string), §15 (9 SSH lane error codes), §18 v2.9.1 (canonical SshKey/SshNonce DDL) verified. Coexistence rules (`X-GL-Auth-Mode` parse, `SshAuthMode` gate, `GL-SSH-LANE-CONFLICT`, `GL-AUTH-LANE-DISABLED`) preserved. **§28 example-github-actions**: banner v2.7.0 → v2.9.1 — drop-in `git-logs-ssh.yml` workflow confirmed authoritative (signing namespace `git-logs@v2`, four required headers `X-GL-Fingerprint`/`-Timestamp`/`-Nonce`/`-Signature`, canonical signing string `GL-SSHSIG-V1\nMETHOD\nPATH\nTIMESTAMP\nNONCE\nsha256(body)`, deploy-key rotation procedure, `~/.ssh-gitlogs` cleanup with `if: always()`); SSH-mode gotchas table covers all 7 SSH error codes + `GL-AUTH-LANE-DISABLED`; legacy TempToken workflow retained as deprecation reference. **§30 threat-model**: banner v2.7.0 → v2.9.1 — added 4 STRIDE Spoofing rows that the v2.7.0 summary already promised but never wrote: **S5 Signature replay** (window check on `SshReplayWindowSec`, per-key `(SshKeyId, Nonce)` uniqueness via `INSERT OR IGNORE`, `SshNonceJanitorBatch` table-bound); **S6 Private-key theft from CI runner** (deploy-key one-Repo blast-radius cap, immediate `IsActive=0` rotation with `AuditTrail.SshKeyRotate`, `LastUsedAt` anomaly surface, GH-Actions key-wipe `if: always()`, per-Profile rate-limit cap); **S7 Signature stripping / lane downgrade** (mandatory HTTPS, `SshAuthMode=required` hard reject, mixed-lane `GL-SSH-LANE-CONFLICT`, header-completeness check ordered before signature check, residual = TLS misconfig); **S8 Lane-mode forgery** (direct DB edit on `ConfigKv.SshAuthMode` falls under T1 file-perm defense, `AuditTrail.ConfigKvUpdate` for in-band changes). Closes the "S5–S8 SSH-lane additions" line in §30 summary that was previously a forward reference. **Scope discipline (Phase 6 ONLY):** AC additions for SshKey/SshNonce/replay/lane-downgrade are **deferred to Phase 7** (AC GWT pass). §15 / §18 / §31 untouched (already at v2.9.1 from Phase 5). Phases 7–10 untouched. Phase B1 still blocked on user. |
| 3.8.6 | 2026-04-26 | **Phase 5 — SSH-Key Lane B: Schema & Errors.** Per `mem://specs/phased-roadmap.md`, executed Phase 5 across §18/§01/§02/§15/§31. **§18 schema.sql**: added `CREATE TABLE SshKey` (PK `SshKeyId AI`; cols `Fingerprint TEXT UNIQUE NOT NULL`, `RepoId FK → Repo ON DELETE CASCADE`, `KeyType`, `PublicKey`, `Label`, `OwnedByProfileId FK → Profile`, `IsActive INTEGER CHECK 0/1 DEFAULT 1`, `LastUsedAt`, `CreatedAt`, `RevokedAt`) + indexes `IxSshKeyRepoActive(RepoId, IsActive)` and `IxSshKeyOwner(OwnedByProfileId)`; added `CREATE TABLE SshNonce` (PK `SshNonceId AI`; cols `SshKeyId FK → SshKey ON DELETE CASCADE`, `Nonce TEXT NOT NULL`, `SeenAt INTEGER NOT NULL`, `UNIQUE(SshKeyId, Nonce)`) + index `IxSshNonceSeenAt(SeenAt)`. Added 2 ConfigKv defaults: `SshAuthMode='optional'` (lane gate ∈ {optional,preferred,required}) and `SshNonceJanitorBatch='100'` (rows pruned per request). Bumped `ConfigKv.PluginVersion` 2.9.0 → 2.9.1; appended `MigrationState` marker `2.9.1`. Banner v2.9.0 → v2.9.1. **§01 glossary**: 3 new entries — `SshKey` (deploy-key model, full column list, Lane-B authoritative when `SshAuthMode=required`), `Ed25519Signature` (OpenSSH PEM-armored signature over `GL-SSHSIG-V1` canonical string, `ssh-keygen -Y sign -n git-logs@v2 -H sha512`, recommended KeyType), `SshNonce` (replay-defense semantics, prune cadence). Banner v3.8.3 → v3.8.6. **§02 database-schema**: banner v3.8.3 → v3.8.6 (already had `SshKey`/`SshNonce` doc-side sub-sections from earlier §31 doc work — now backed by canonical §18 DDL). **§15 error-codes**: banner v2.9.0 → v2.9.1 with note that the existing 9 SSH lane codes are now backed by canonical schema. **§31 ssh-key-auth**: banner v2.7.0 → v2.9.1 (canonical-DDL note). **AuditActionType seeds verified**: `SshKeyRegister` (22), `SshKeyRevoke` (23), `SshKeyRotate` (24) already present in §18 from prior pass. **In-memory validation passed**: 31 tables, `SshKey` + `SshNonce` present, `LogEntry`/`ErrorLogEntry` still absent, 15 ConfigKv keys, `PluginVersion=2.9.1`, 10 MigrationState markers, 3 SshKey* AuditActionType seeds. **Scope discipline (Phase 5 ONLY):** §05 SSH lane block insertion, §28 GH-Actions SSH-signed example, §30 STRIDE entries (replay/key-theft/sig-stripping/lane-downgrade) are **deferred to Phase 6** per the phased roadmap. AC additions for SshKey/SshNonce deferred to Phase 7 (AC GWT pass). Phases 7–10 untouched. |
| 3.8.5 | 2026-04-26 | **Phase 4 — Split-DB doc closure.** Per `mem://specs/phased-roadmap.md`, executed Phase 4 across §00, §97, root `spec-index.md`, and `26-gitlogs-diagrams/01-er-diagram.mmd` to close out the v3.8.3/v3.8.4 split-DB landing. **§00 overview**: §39 inventory row rewritten — old "NEW v3.8.0; logs/<RepoVersionId>/<GitSha256>.sqlite" replaced with v2.9.0-active wording (path layout `<dataDir>/<ShaLogsRoot>/<Sha[0:2]>/<Sha>.db`, new ConfigKv keys, cross-refs to §15/§22/§23/§29). Banner v3.8.0 → v3.8.5. **§97 ACs**: AC-49..AC-53 promoted from draft to **Active (v2.9.0)** and rewritten to match shipped DDL/runtime — AC-49 keys on `(PipelineId, Sha)` not `(RepoVersionId, GitSha256)`; AC-50 mirrors actual `ShaRegistry` columns (`RowCount`, `LastSeenAt`, `FileSizeBytes`, `Sha256`) instead of pre-v3.8.3 dashboard fields; AC-52 defaults corrected to `MaxOpenShaDbHandles=32` / `ShaDbIdleCloseSec=120` and ties `GL-SHA-DB-QUOTA-EXCEEDED` (HTTP 507); AC-53 expanded to cover §22 prune crash-safety (rename → delete-row → unlink, empty-shard cleanup, ShaLogsRoot preserved), §23 manifest `ShaFiles[]` schema + `.bak`/`logs.bak.<ts>/` rollback + `GL-SHA-DB-CHECKSUM-MISMATCH`, and §29 Wipe per-SHA-tree-first ordering. Banner v3.8.2 → v3.8.5. **`spec/spec-index.md`**: refreshed 9 version cells for §00 (3.8.0→3.8.5), §01 (3.8.1→3.8.3), §02 (3.8.0→3.8.3), §15 (2.8.7→2.9.0), §22 (2.5.0→2.9.0), §23 (2.5.0→2.9.0), §29 (2.5.0→2.9.0), §97 (3.8.2→3.8.5), §99 (3.8.2→3.8.5). **`26-gitlogs-diagrams/01-er-diagram.mmd`**: re-rendered for split-DB boundary — added top-of-file annotation declaring root-DB scope; removed stale `RepoVersion → ShaRegistry seenShas` and `LogSeverity → ShaRegistry lastSeverity` edges; collapsed `Pipeline → ShaRegistry lastSha` to canonical `Pipeline → ShaRegistry sha`; rewrote `ShaRegistry` entity block to match v2.9.0 §18 DDL (`PipelineId FK`, `Sha`, `DbFilePath`, `RowCount`, `FirstSeenAt`, `LastSeenAt`, `FileSizeBytes`, `Sha256`) — old fields `RepoVersionId/GitSha256/EntryCount/ErrorCount/LastSeverityId/LastStatus/LastFailureAt/LastSuccessAt` retired. **§99 consistency**: Phase 4 audit table appended; banner v3.8.4 → v3.8.5. **Scope discipline (Phase 4 ONLY):** Phases 5–10 untouched; Phase B1 (§07 App identity) still blocked on user. |
| 3.8.4 | 2026-04-26 | **Phase 3 — Split-DB error codes & cross-section updates.** Per `mem://specs/phased-roadmap.md`, executed Phase 3 across §15/§22/§23/§29 to wire the v3.8.3 split-DB DDL into runtime, lifecycle, and disaster-recovery surfaces. **§15 error-codes**: added new section *Per-SHA log storage (split-DB — see §39)* with 4 codes — `GL-SHA-DB-OPEN-FAILED` (503; pool open of registered file failed), `GL-SHA-DB-CREATE-FAILED` (500; first-write file/folder create failed), `GL-SHA-DB-CHECKSUM-MISMATCH` (500; backup/audit hash drift vs `ShaRegistry.Sha256`), `GL-SHA-DB-QUOTA-EXCEEDED` (507; pool at `MaxOpenShaDbHandles` with no idle-evictable handle). Banner v2.8.7 → v2.9.0. **§22 retention-and-pruning**: rewritten end-to-end around per-SHA files. Eligibility now keyed on `ShaRegistry.LastSeenAt` + `Pipeline.HasError` + history-window guard; row-level `DELETE FROM LogEntry`/`ErrorLogEntry` path **removed** (no longer applicable). New rename → delete-row → unlink crash-safety protocol with `*.pruning` recovery on startup. New exit code 4 for per-SHA file delete failures. Empty-shard `<aa>/` folder cleanup with `<ShaLogsRoot>` root preserved (only §29 Wipe deletes the root). Audit detail JSON keys renamed (`deletedShaCount`, `reclaimedBytes`, `errors[]`). Banner v2.5.0 → v2.9.0. **§23 backup-restore**: backup is now a **directory** (`<path>/git-logs.sqlite` + `<path>/manifest.json` + `<path>/logs/<aa>/<sha>.db…`); each per-SHA file copied via Online Backup API with own `wal_checkpoint(TRUNCATE)` + `integrity_check`. Manifest gains `ShaFiles[]` array (`PipelineId`, `Sha`, `DbFilePath`, `RowCount`, `FileSizeBytes`, `Sha256`) plus `ShaFileTotal` aggregate. Restore re-walks every entry, verifies integrity + sha256, and is all-or-nothing with `.bak`/`logs.bak.<ts>/` rollback. New `--skip-sha-checksum` flag. New cross-version row for pre-v2.9.0 → v2.9.0+ migration (split legacy rows into per-SHA files). `verify` extended to walk `ShaRegistry`. Banner v2.5.0 → v2.9.0. **§29 uninstall-policy**: lifecycle table gained a "Per-SHA tree" column. `Preserve` mode also preserves the `<ShaLogsRoot>/` tree + audit `Detail.ShaFileCount`; `Archive` renames the tree to `<ShaLogsRoot>.archive-<unix>/` alongside the root archive; **`Wipe` deletes the entire `<ShaLogsRoot>/` tree first, root DB last**, then `rmdir`'s the parent `git-logs/` folder. Documented per-SHA-tree-first ordering rationale. Banner v2.5.0 → v2.9.0. **Scope discipline (Phase 3 ONLY):** §00 inventory row for §39, §97 AC-49..AC-53 promotion, §99 audit table, root `spec-index.md` bump, and Mermaid re-render are **deferred to Phase 4** per the phased roadmap. |
| 3.8.3 | 2026-04-26 | **Phase 2 — Q3 Split-DB schema surgery (root DB).** Per `mem://specs/phased-roadmap.md`, executed Phase 2: drop `LogEntry`/`ErrorLogEntry` from root DDL, add `ShaRegistry` table to root DDL, register 3 new `ConfigKv` defaults. **§18 schema.sql**: deleted `CREATE TABLE LogEntry` + `IxLogEntryPipeline` and `CREATE TABLE ErrorLogEntry` + `IxErrorLogEntryPipeline` blocks (replaced with retirement comment block pointing at §39); inserted new `CREATE TABLE ShaRegistry` (PK `ShaRegistryId INTEGER PK AI`; cols `PipelineId FK → Pipeline ON DELETE CASCADE`, `Sha TEXT NOT NULL`, `DbFilePath TEXT NOT NULL`, `RowCount INTEGER NOT NULL DEFAULT 0`, `FirstSeenAt INTEGER NOT NULL`, `LastSeenAt INTEGER NOT NULL`, `FileSizeBytes INTEGER NOT NULL DEFAULT 0`, `Sha256 TEXT NULL`; `UNIQUE(PipelineId, Sha)`) plus indexes `IxShaRegistrySha(Sha)` and `IxShaRegistryLastSeen(LastSeenAt)`; appended 3 new `ConfigKv` seed rows (`ShaLogsRoot='logs'`, `MaxOpenShaDbHandles='32'`, `ShaDbIdleCloseSec='120'`); bumped `ConfigKv.PluginVersion` 2.8.9 → 2.9.0; appended `MigrationState` marker `2.9.0`; banner v2.8.9 → v2.9.0. **§02 database-schema**: banner 3.8.0 → 3.8.3; engine line clarified that root DDL no longer ships `LogEntry`/`ErrorLogEntry` and now ships `ShaRegistry` + 3 new ConfigKv keys; `ConfigKv` section gained a *v3.8.3 (Q3 Split-DB) — new keys* sub-table documenting `ShaLogsRoot` / `MaxOpenShaDbHandles` / `ShaDbIdleCloseSec` defaults + purpose. **§01 glossary**: banner 3.8.1 → 3.8.3; `ShaRegistry` row clarified ("One row per (PipelineId, Sha)"); two NEW glossary rows added — `PerShaDb` (defines the per-SHA SQLite file with full path layout `<dataDir>/<ShaLogsRoot>/<Sha[0:2]>/<Sha>.db`) and `ShaLogsRoot` (defines the ConfigKv key + sharded folder tree). **Scope discipline (Phase 2 ONLY):** §15 error codes (`GL-SHA-DB-*`), §22 prune walk, §23 backup manifest, §29 wipe, §97 ACs, §99 audit table, §00 inventory row for §39, and Mermaid re-render are **deferred to Phases 3–4** per the phased roadmap. No `Phase 5–10` work touched. |
| 3.8.2 | 2026-04-26 | **Q2 PipelineAction rename + SystemEvent landed in §18 + §03 + §01 + §97 (DDL + UI + glossary + ACs).** Fully retires the `Action`/`ActionType` names at the schema layer (queued in v3.8.0). **§18 schema.sql**: `CREATE TABLE ActionType` → `PipelineActionType`; `CREATE TABLE Action` → `PipelineAction` (PK renamed `ActionId` → `PipelineActionId`, FK column `ActionTypeId` → `PipelineActionTypeId`, added `RepoVersionId NOT NULL`, `ProfileId` FK, indexes `IxPipelineActionRepoVersion` + `IxPipelineActionType`); `History.ActionTypeId` → `History.PipelineActionTypeId`; new `CREATE TABLE SystemEventType` lookup + `CREATE TABLE SystemEvent` (`SystemEventId PK`, `SystemEventTypeId FK`, `ActorProfileId FK NULL`, `TargetType TEXT NULL`, `TargetId INTEGER NULL` (loose polymorphic, no FK CHECK), `Summary TEXT`, `DetailJson TEXT`, `OccurredAt INTEGER`) with three indexes (`SystemEventTypeId`, `ActorProfileId`, `(TargetType, TargetId)`); `INSERT INTO ActionType` seed → `INSERT INTO PipelineActionType` (4 rows: Append/Fixed/Clear/ClearAll); new `INSERT INTO SystemEventType` seed (16 rows: ProfileCreated, ProfileDeleted, ProfileStatusChanged, RoleAssigned, RoleRevoked, GitProfileCreated, GitProfileAcceptanceChanged, GitProfileBranchRestrictionChanged, AppCreated, AppStatusChanged, AppLinkAdded, AppLinkRemoved, SshKeyRegistered, SshKeyRevoked, SshKeyRotated, TempTokenRotated); `ConfigKv.PluginVersion` 2.8.8 → 2.8.9; `MigrationState` markers 2.8.8 (Q1) + 2.8.9 (Q2) appended; banner v2.8.8 → v2.8.9. **§03 admin-UI**: top-menu item 7 (History) gains "Activity tab" note for SystemEvent; item 8 (Action) reworded — UI label retained, backing table now `PipelineAction`; History column relabel `ActionType` → `PipelineActionType`; banner v2.1.0 → v2.2.0. **§01 glossary**: banner v3.8.0 → v3.8.1 (entries already correct from v3.8.0 doc pass). **§97 ACs**: added **AC-56** (no `Action`/`ActionType` tables; `History.PipelineActionTypeId`), **AC-57** (`SystemEvent` columns + 3 indexes + no FK on Target), **AC-58** (16 `SystemEventType` seeds in canonical order), **AC-59** (History Activity tab + Action menu wording). §97 banner v3.8.1 → v3.8.2. **§99 health**: Q2 row in v3.8.0 audit table flipped from queued → "landed v3.8.2"; new repair entry. Validated in-memory SQLite: `PipelineActionType=4`, `SystemEventType=16`, `AuditActionType=25`, `Permission=17`, `LogSeverity=6`, `Acceptance=3`, `AppStatus=3`, `AppLinkType=2`, `UserStatus=3`, `Provider=2`, `ConfigKv=10`, `MigrationState=8`, no `Action`/`ActionType`/`OwnerType` tables present. **Scope discipline**: this commit is Q2 ONLY — Q3 (LogEntry/ErrorLogEntry → ShaRegistry + per-SHA split-DB) and the §15 `GL-SHA-DB-*` codes remain queued per `mem://specs/git-logs.md`. ER diagram (`26-gitlogs-diagrams/01-er-diagram.mmd`) was already updated in v3.8.0 doc pass — no .mmd change required this commit. |
| 3.8.1 | 2026-04-26 | **Q1 IsOrganization landed in §03 + §16 + §18 (UI + seed-data + DDL).** Fully retires the `OwnerType` lookup at the schema layer (queued in v3.8.0). **§03 admin-UI**: GitProfile form now renders an explicit **Is organization** checkbox (default off) bound to `GitProfile.IsOrganization` 0/1; the old "OwnerType (derived)" row deleted; Profile-URL row note updated to explain the canonical URL flip (`github.com/$org/$repo` ↔ `github.com/$username/$repo`). §03 banner v2.0.0 → v2.1.0. **§16 seed-data**: `OwnerType (introduced 2.0.0)` section converted to a `OwnerType (retired in v3.8.0)` tombstone — no seed rows, points to §01 + §18. §16 banner v2.7.0 → v2.7.1. **§18 schema.sql**: `CREATE TABLE OwnerType` deleted (replaced with retirement comment); `GitProfile.OwnerTypeId INTEGER NOT NULL REFERENCES OwnerType(...)` → `IsOrganization INTEGER NOT NULL DEFAULT 0 CHECK (IsOrganization IN (0,1))`; `INSERT OR IGNORE INTO OwnerType` seed deleted (replaced with retirement comment); `ConfigKv.PluginVersion` `'2.8.7'` → `'2.8.8'`; banner `v2.8.7` → `v2.8.8 — Q1 IsOrganization`. **§97 ACs**: added **AC-54** (UI checkbox + binding) and **AC-55** (DDL constraint, no OwnerType table). §97 banner v3.8.0 → v3.8.1. **§99 health**: Q1 row in v3.8.0 audit table flipped from "queued §18 DDL" to "landed v3.8.1"; new repair entry. **Scope discipline**: this commit is Q1 ONLY — Q2 (Action→PipelineAction rename, SystemEvent table, 16 seeds), Q3 (LogEntry/ErrorLogEntry → ShaRegistry + per-SHA split-DB), and the §15 `GL-SHA-DB-*` codes remain queued per `mem://specs/git-logs.md`. No `MigrationState` row added (DDL change is column-rename + table-drop, handled by activator V3_8_1 stub when implementation lands). |
| 3.8.0 | 2026-04-26 | **Domain-model overhaul from user diagram review.** Three normative changes: **(1) `IsOrganization` boolean on `GitProfile`** — replaces the `OwnerType` lookup table (User\|Organization). Boolean drives URL canonicalization (`github.com/$org/$repo` vs `github.com/$username/$repo`) and the admin-UI "Is organization" checkbox directly. `OwnerType` lookup retired; entry kept in §01 as a tombstone. **(2) `Action` → `PipelineAction` rename + new `SystemEvent` table** — old name was misleading (table only records pipeline-bound CI events, not system-wide actions). `ActionType` lookup → `PipelineActionType`. New `SystemEvent` table holds non-Git business state changes (ProfileCreated, ProfileDeleted, RoleAssigned, GitProfileAcceptanceChanged, BranchRestrictionChanged, AppCreated, AppStatusChanged, AppLinkAdded, AppLinkRemoved, SshKeyRegistered, SshKeyRevoked, SshKeyRotated, TempTokenRotated) with loose polymorphic target (`TargetType TEXT NULL`, `TargetId INTEGER NULL` — no FK CHECK so audit history outlives the entity). New `SystemEventType` lookup with 16 seed values. §08 retitled "History, PipelineAction, SystemEvent, and AuditTrail" with a four-table responsibility matrix making each table's *answers* explicit. **(3) Split-DB log storage (NEW §39)** — applies the [05-split-db-architecture](../05-split-db-architecture/00-overview.md) pattern: root DB keeps only `ShaRegistry` (maps `(RepoVersionId, GitSha256) → ShaDbPath` plus rolled-up `EntryCount`/`ErrorCount`/`LastStatus`/`LastSeverityId`/`LastFailureAt`/`LastSuccessAt` so dashboards never open per-SHA files). `LogEntry` and `ErrorLogEntry` deleted from root DB and moved to per-SHA SQLite files at `wp-content/uploads/git-logs/logs/<RepoVersionId>/<GitSha256>.sqlite`. Each per-SHA file is self-contained: `LogEntry`, `ErrorLogEntry`, `PipelineRun` (one row per (Branch, Pipeline) execution), `StatusSnapshot` (single-row rolling aggregate answering last-status / failure-count / last-entry questions in O(1)), `ShaMeta` (single-row identity), denormalized `LogSeverity` copy. WAL mode mandatory; LRU pool capped at `ConfigKv.MaxOpenShaDbHandles=64`; idle close at `ShaDbIdleCloseSec=300`. Plugin's own internal Trace/Debug/Info/Warn/Error/Fatal logger stays in root DB (different concern). **Lockstep:** §00 inventory adds §39; §01 glossary updated (LogEntry/ErrorLogEntry note "lives in per-SHA file", ShaRegistry added, OwnerType marked retired, PipelineAction renamed, SystemEvent added, SystemEventType seeded); §02 schema rewritten — GitProfile gains `IsOrganization`, `OwnerType` removed from lookup list, `LogEntry`+`ErrorLogEntry` tables deleted, `ShaRegistry` added, `History.ActionTypeId` → `PipelineActionTypeId`, `Action` table → `PipelineAction`, `SystemEvent` table added; §08 rewritten as four-table model; §97 ACs: AC-07 reworded, AC-21 reworded for four-table model, **5 new ACs (AC-49…AC-53)** for split-DB storage; ER diagram (`26-gitlogs-diagrams/01-er-diagram.mmd`) and domain diagram (`02-domain-design.mmd`) regenerated to show split boundary. **Queued for follow-up (NOT in this commit, listed in mem://specs/git-logs queued decisions):** §18 schema.sql rewrite (drop OwnerType+LogEntry+ErrorLogEntry tables/seeds, add IsOrganization column, add ShaRegistry+SystemEvent tables, rename Action→PipelineAction, add 16 SystemEventType seeds, add 4 GL-SHA-DB-* error codes to §15, add `MaxOpenShaDbHandles`/`ShaDbIdleCloseSec`/`ShaLogsRoot` ConfigKv defaults); §22 prune walks ShaRegistry; §23 backup manifest lists per-SHA files; §29 Wipe deletes logs/ folder; §15 adds `GL-SHA-DB-CREATE-FAILED`/`GL-SHA-DB-OPEN-FAILED`/`GL-SHA-DB-CORRUPT`/`GL-SHA-DB-NOT-FOUND`. §26-gitlogs-diagrams banner bumped to v1.2.0. |
| 3.7.8 | 2026-04-26 | **§21 i18n removed at user request — slot retired.** Deleted `spec/22-git-logs-v2/21-i18n.md` (was Internationalization plan, last bumped in v2.4.0). Slot 21 is now an **intentional locked gap** — per project rule (file slots are immutable once shipped) the slot is NOT reused; future i18n work, if ever, ships in a new slot. References purged in lockstep: §00 inventory row replaced with retired-slot stub; §97 AC-41 reworded to drop `wp i18n make-pot` gate (only `wp plugin check` remains, §26 only); §26 release CI gate paragraph reworded; §37 blind-AI gap analysis tables (§"What v2 covers well" + "v1↔v2 feature delta") had i18n rows removed; §99 inventory marks `~~21-i18n.md~~ 🗑️ removed v3.7.8`; root `spec/spec-index.md` row removed; root `spec/dashboard-data.json` array entry removed. New §99 audit row added below changelog inventory. No DDL/error-code/AC-id renumbering — only AC-41's right-side cross-ref column changed. |
| 2.8.7 | 2026-04-25 | **§18/§15 audit alignment + stale-reference repair.** Cross-checked all `18-schema.sql` lookup seeds against `15-error-codes.md` runtime codes — full coverage confirmed: 25 `AuditActionType` rows cover all reject paths via LogPush/LogQuery/AuthFail; 17 `Permission` rows cover every `GL-AUTHZ-PERMISSION-DENIED` source; `AppStatus` (Active/Disabled/Archived) matches `GL-APP-NOT-ACTIVE`; `UserStatus` (Active/Suspended/Revoked) matches `GL-AUTH-PROFILE-SUSPENDED` + `GL-AUTH-PROFILE-INACTIVE`. Repaired drift introduced by v2.8.6 slot rename: `18-schema.sql` header reference `16-seed-data.md` → `37-seed-data.md`; seeds-comment reference updated. Banner refresh: `18-schema.sql` v2.7.0 → v2.8.7; `15-error-codes.md` v2.7.0 → v2.8.7; `ConfigKv.PluginVersion` seed `'2.7.0'` → `'2.8.7'`. Added `MigrationState` markers for `2.8.0` (doc-only consolidation) and `2.8.7` (audit alignment) — schema now reflects full v2.8.x version trail. No DDL changes; idempotent re-apply remains safe. |
| 2.8.6 | 2026-04-25 | **Version-coherence audit + slot-collision fix + §17 OpenAPI hardening re-applied.** Added §99 *Version coherence audit* table tracking leader/lockstep status of all 33 file banners. Resolved §16 slot collision by renaming orphan `16-seed-data.md` → `37-seed-data.md`. Re-applied §17 OpenAPI hardening lost to tool drift in v2.8.2: `info.version` 2.8.6, `ErrorCode` enum mirrors all 37 §15 runtime codes, `SshSignature` security scheme restored. Inventory in §00/§99 updated to reflect new slot mapping. Health 99 → 100. |
| 2.8.5 | 2026-04-25 | **Memory sync pass.** `mem://specs/git-logs.md` description refreshed from stale "v2.6 authoritative" to "v2.8.4 authoritative"; folder map extended to include §31–§36 and explicit lock note for §09–§13 intentional gap. §00/§99 banners + changelog kept in lockstep. No spec content changes. |
| 2.8.4 | 2026-04-25 | **§00 overview audit + open-question banner.** Added high-visibility banner at top of `00-overview.md` flagging the §07 open question (App identity fields) so future agents see the blocker immediately. Inventory refreshed to include §31–§36 (SSH auth, test plans, reference CI, release checklist). Formally documented §09–§13 intentional gap in inventory. Descriptions updated for §15 (catalog size), §17 (8-path/10-endpoint rule), §18 (seed counts). |
| 2.8.3 | 2026-04-25 | **§04 path-collapse rule documented + AC-11 hardened.** Added a new normative paragraph to `04-rest-api-endpoints.md` explaining that the 10 logical endpoints collapse to 8 HTTP paths in §17 because `?q=…` is a query parameter on the parent route, not a separate path. Rows #5/#6 share `/get-logs`; rows #7/#8 share `/get-pipeline-logs`. AC-11 reworded to reference the rule and cross-link both §04 and §17 sources. Implementation contract: route handlers MUST accept both the explicit `RepoUrl`+`Branch` shape AND the shorthand `q=…` shape on the same path. New logical endpoints MUST add a new path OR a new query-param shape — silent overloading forbidden. §04 version bumped 2.0.0 → 2.8.3 (was last-touched in v2.0.0). §99 cross-link entry added. |
| 2.8.2 | 2026-04-25 | **§17 OpenAPI hardening pass.** `info.version` bumped to 2.8.2 (was stale at 2.3.0). Added new `ErrorCode` schema with full 37-member `GL-*` enum mirroring §15 runtime codes (release-time `GL-RELEASE-*` codes intentionally excluded — never reach HTTP). `ErrorEnvelope.Code` now references the enum, removing free-text drift risk. Added third security scheme `SshSignature` for Lane B SSH sub-mode (X-GL-Signature header) with cross-link to §31 canonical signing string. YAML validated with pyyaml — 8 paths (10 logical endpoints fold to 8 paths via `?q=` query-param collapse, consistent with §04), 37 codes in enum, 3 security schemes. Drift between §15 and §17 closed: prior pass had only 1 of 36 codes referenced. |
| 2.8.1 | 2026-04-25 | **§36 follow-up: codes + ACs landed.** Added 5 new release/CI codes to §15 (`GL-RELEASE-VERSION-DRIFT`, `GL-RELEASE-README-INVALID`, `GL-RELEASE-WP-CHECK-FAIL`, `GL-RELEASE-POT-DIFF`, `GL-RELEASE-UPGRADE-FAIL`) under new section *Release / CI gates (build-time, never runtime)* — HTTP column = n/a. Added 7 new ACs to §97: AC-42–AC-46 (one per CI gate), AC-47 (tag-format hygiene), AC-48 (hotfix branch policy). §99 AC range bumped to AC-01..AC-48; inventory unchanged at 32 files. Health stays 99/100. |
| 2.8.0 | 2026-04-25 | **§36 release-checklist landed.** New `36-release-checklist.md` covers semver gates (Patch/Minor/Major), version-tag hygiene (4-location version stamp + `GL-RELEASE-VERSION-DRIFT` gate), pre-release checklist (spec/code/migration/WP.org/security/notes), release-day procedure, hotfix path, rollback procedure, 5 new CI gates queued (`version-drift-check`, `readme-validator`, `wp-plugin-check`, `pot-diff`, `upgrade-from-previous`) with 5 new `GL-RELEASE-*` codes queued for §15. Anti-patterns enumerated. Cross-links to §06/§18/§23/§26/§27/§29/§30/§35/§97/§98. §99 inventory extended to §36; health stays 99/100. |
| 2.7.1 | 2026-04-25 | **Legacy folder 21 deprecation pass.** Prepended uniform DEPRECATED banner to all 10 v1 spec files in `spec/_archive/21-git-logs-v1/` (00-overview, 01-glossary, 02-schema-erd, 05-auth-jwt, 08-allowlist, 11-error, 12-logging, 16-jwt-onboarding, 17-checklist, 97-AC). Banner cross-links to v2 canonical source. No content removed — historical reference preserved. |
| 2.7.0 | 2026-04-25 | **SSH-key Lane B + 7-stage CI test plan + DDL seed catch-up.** New §31 SSH-key auth (`SshKey` table keyed by SHA-256 fingerprint, sign-the-request-body proof-of-possession via `openssl_verify`, 5-min replay window, 10-min nonce LRU, deploy-key model: one key → one Repo, coexists with TempToken, v3.0 will deprecate TempToken). New §32 seven-stage CLI test plan (preflight → setup → build → unit → integration → verify → e2e). New §33 Bats CLI smoke skeleton (`tests/bats/` with helpers `wp.bash`/`ssh.bash`/`db.bash`, fixtures `expected-schema.sql` + `canonical-strings/*.txt`, one `@test` per `GL-*` code + per `wp git-logs` subcommand). New §34 PHPUnit unit skeleton (WP-decoupled, `BaseTestCase` + `BaseDbTestCase` with in-memory SQLite, 100 % coverage targets for `RepoUrlParser`/`SshSignatureValidator`/`Redactor`, PHPStan level=max gate, canonical-strings symlinked from §33 for byte-lock cross-layer). New §35 reference `ci.yml` (12-job full matrix PHP 8.1–8.3 × WP latest/previous × Single/Multisite on `main`; 3-job fast-feedback PR matrix incl. `sqlite-only`; stage→job mapping; drift-detection compares live SQLite schema vs §18, seed counts vs §16, `GL-*` codes vs §15; 30-day artifact retention; `act`-reproducible). New error codes (queued for §15): `GL-SSH-FINGERPRINT-UNKNOWN`, `GL-SSH-SIGNATURE-INVALID`, `GL-SSH-TIMESTAMP-SKEW`, `GL-SSH-REPLAY`, `GL-SSH-KEY-REVOKED`, `GL-SSH-REPO-MISMATCH`, `GL-AUTH-LANE-AMBIGUOUS`. **`18-schema.sql` updated**: `AuditActionType` rows 21–25 appended (`PluginUninstall`, `SshKeyRegister`, `SshKeyRevoke`, `SshKeyRotate`, `ConfigChange`); `ConfigKv` defaults appended (`MaintenanceMode='0'`, `UninstallMode='Preserve'`, `AllowedReadOrigins=''`, `SshReplayWindowSec='300'`); `PluginVersion` bumped to `2.7.0`; `MigrationState` markers added for 2.5.0/2.6.0/2.7.0. Schema validated in-memory (Python sqlite3): 25 AuditActionType rows, 10 ConfigKv defaults, 4 MigrationState markers — clean apply, no errors. §28 updated to dual-workflow (SSH-signed primary, TempToken legacy tab). §30 threat model: S1 TempToken-theft downgraded; new threats S5 SSH-key exfiltration from CI runner, S6 fingerprint collision (mitigated by SHA-256), S7 timestamp-skew abuse, S8 nonce-LRU bypass; new T6 signature-stripping/lane-downgrade mitigated by `GL-AUTH-LANE-AMBIGUOUS`. Housekeeping: 09–13 numbering gap reclassified as **intentional locked decision** (content distributed across §05/§16/§30/§31); §99 inventory extended through §35; health 96 → 99. §00/§02/§97/§99 banners bumped to 2.7.0; older files keep last-touched version per project policy. |
| 2.6.0 | 2026-04-25 | Added §27 WP-CLI reference, §28 GitHub Actions workflow example, §29 uninstall policy, §30 STRIDE threat model. Housekeeping pass: appended `Prune` (19) + `Restore` (20) `AuditActionType` seeds in `18-schema.sql`; added 4 new auth codes to `15-error-codes.md` (`GL-AUTH-NOT-LOGGED-IN`, `GL-AUTH-NO-PROFILE-LINK`, `GL-AUTH-PROFILE-SUSPENDED`, `GL-AUTH-WRONG-LANE`); extended `97-acceptance-criteria.md` from AC-25 to AC-41 covering §10–§26. New seeds queued for §29: `PluginUninstall` (21). |
| 2.5.0 | 2026-04-25 | Added §22 retention/pruning (`wp git-logs prune`), §23 backup/restore (SQLite Online Backup API + manifest), §24 multisite behavior, §25 headless auth notes (JWT/OAuth combos), §26 WP.org readme.txt + screenshot inventory. New seed `AuditActionType` rows planned: Prune (19), Restore (20). |
| 2.4.0 | 2026-04-25 | Added §17 OpenAPI 3.1 spec, §18 verbatim SQL DDL, §19 permission matrix, §20 observability (Site Health + Prometheus metrics), §21 i18n plan. |
| 2.3.0 | 2026-04-25 | Added §14 endpoint examples, §15 error code catalog, §16 test plan. §05 cross-linked to CI/CD pipeline workflows folder. Diagrams 07 + 08 rendered to SVG. |
| 2.2.0 | 2026-04-25 | Added §13 v1↔v2 mapping. Extended §97 AC (AC-26..AC-36). §12 cross-linked to PHP coding-standards folder. §03 gained First-run Bootstrap subsection. Folder 26 gained `07-rate-limit-flow.mmd` and `08-encryption-v3-flow.mmd`. |
| 2.1.0 | 2026-04-25 | Added §09 seed data, §10 rate limits/payload caps, §11 encryption deferred plan, §12 WP plugin scaffold. §06 amended with migration class layout. |
| 2.0.0 | 2026-04-25 | Initial parallel rewrite from verbatim brief. SQLite root DB, JWT removed, AppLink polymorphic, three-table audit model, 10 REST endpoints under `git-logs/v2`. |
