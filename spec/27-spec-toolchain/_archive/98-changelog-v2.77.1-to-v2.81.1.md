# Changelog — Spec Toolchain — Archived Releases (v2.77.1 → v2.81.1)

**Archived:** 2026-05-01 (Phase 153 Task A24-fu36)
**Reason:** Third §98 archive split (after fu28 pre-v2.72.0 + fu35 v2.73.0–v2.77.0). Tier-1 budget still 9.6 KB OVER after fu35 — moved 7 mid-aged release rows (v2.81.1 fu measurement → v2.77.1 SemVer-max comparator) into this archive to clear remaining deficit. Per Lesson #65 + Lesson #70.
**Source of truth:** Active `98-changelog.md` for current state.

---

### 2.81.1 — 2026-04-30 — Phase 153 Task A20-fu: full-tree v8 rebaseline post-A12 walker-cap (pure measurement)
- **Action**: Ran full-tree `python3 linter-scripts/audit-ai-implementability.py --force` to refresh all 23 stale `.lovable/cache/audit-ai/*.json` entries (cache invalidated by A12's `MAX_BYTES` 90 KB → 120 KB raise per Lesson #34). Run completed in 1m56s; zero gateway errors. **Tree mean 85.78 → 87.70 / 100 (+1.92).** EXCELLENT band 6 → **7** (16-generic-release graduated 90→91 with the additional Tier-2 surface). 0 NEEDS_WORK / 0 BLOCKING preserved.
- **Top A12-driven lifts (saturation reduction → score)**: spec/26 +8 (80→88, audit-corpus axis with full citation density now visible); spec/11 +6 (81→87, integration-spec — 18/19 files visible vs prior 16/19); spec/18 +6 (82→88, process-guidance — 15/35 files vs prior 10/35); spec/27 +4 (76→80, tooling-spec — 3/50 still Tier-1-bounded but Tier-1 fits cleaner at 117 KB); spec/06 +4; spec/03 +3; spec/22 +3; spec/01 +3; spec/25 +3.
- **Honest-baseline corrections (Lesson #18 expected)**: spec/02 -5 (87→82, normative-contract — expanded Tier-2 surface exposed previously-hidden D3/D5 gaps in the 245-file subtree); spec/04 -2; spec/07 -2; spec/13 -2. These are NOT regressions — they are the rubric finally seeing what was always there. They become the next-highest-leverage self-lift targets per Lesson #18 (mark new baseline as ground truth; do NOT roll back the bias fix to "restore" old EXCELLENT scores).
- **Spec lockstep**: §00 v2.81.0 → **v2.81.1**, §98 v2.81.0 → **v2.81.1**, §99 v2.78.0 → **v2.78.1**. Patch-only — pure measurement, no contract / AC / CI / RUBRIC change. Mirrors A8's v5 rebaseline lockstep budget (v2.77.3 → v2.77.4 patch-only).
- **Cache**: 23 entries rewritten with fresh `bundle_sha` (now incorporating both `axis=…` prefix and 120 KB Tier-2 content) + scores. Cache is authoritative again per Lesson #34.
- **A12 ROI confirmed**: predicted "modules with highest pre-A12 truncation will see D4 + D5 lifts proportional to additional Tier-2 surface" — verified in measurement. spec/26 (audit-corpus, citation-dense) was the single highest beneficiary at +8; the D5 cross-ref dimension responded most strongly to the extra context. integration-spec axis (spec/11 +6, spec/12 +1) confirms Lesson #37's predicted dual-gap exposure pattern.
- **Lesson #41 reinforced (foundation→contract→wiring→measurement 4-phase split)**: A12 was the contract phase (raised `MAX_BYTES` constant + AC-34-13); A20-fu is the matched measurement phase. Splitting them let the constant + AC ship cleanly without entangling the rebaseline's score deltas in the same review surface. Future cap raises MUST follow this split.
- **NEW Lesson #49 codified inside this row**: when a `MAX_BYTES`-class fix expands Tier-2 visibility, expect mixed lift+correction signal in the rebaseline — **net gain is the leading indicator, not per-module monotonic improvement**. A20-fu shipped +1.92 net on 14 lifts vs 4 corrections vs 5 unchanged. Honest-baseline corrections (Lesson #18 axis) are evidence the fix worked — if every module monotonically lifted, the cap raise would only have surfaced more low-hanging fruit, not exposed prior measurement bias. Mirror of Lesson #18 on the cap-raise-rebaseline axis (vs Lesson #18's tier-1-ordering-rebaseline axis).
- Memo: `phase-153-task-A20-fu-v8-rebaseline.md`. Predecessor: A12-walker-cap (cap raise + AC-34-13 codification). Successor candidates: A24-fu11 (spec/02 -5 honest-baseline close — highest-leverage corrective target); spec/04/13/07 secondary corrective targets; A24-fu12 (spec/25 at 79, lowest GOOD — final NEEDS_WORK-adjacent module).


- **Action**: Raised `MAX_BYTES` in `linter-scripts/audit-ai-implementability.py` from `90_000` → `120_000` (line 45) plus matching prose updates in `load_module_bundle()` docstring (lines 171-185) and the truncation marker (line 207). Pre-flight saturation probe across 6 representative modules (spec/02, 05, 07, 17, 18, 27) showed every module hitting the 90 KB cap exactly — most fitting only 3-10 of 17-251 files. Live gateway probe with 119 KB user-content payload (`POST /v1/chat/completions` with `User-Agent: lovable-spec-audit/1.0`) returned HTTP 200, confirming the explicit-UA path tolerates ~125 KB before Cloudflare 1010 fires (the 25 KB cliff cited in Lesson #11 is for the *default* Python UA only). Codified the new cap as **AC-34-13** `[critical]` in slot 34 §97 with full Why prose: tree-wide saturation at 90 KB silently under-counted D2/D3/D4 dimensions for every module past Tier 1 (Lesson #46 saturation class); the 120 KB cap restores ~33% additional Tier-2 capacity. Tier-1 contract priority (AC-34-09) preserved unchanged — A12 expands the Tier-2 budget; it does NOT relax the Tier-1 guarantee. Future raises above 120 KB require fresh live-probe at the proposed ceiling; the 5 KB headroom below 125 KB MUST be preserved.
- **Spec lockstep**: slot 34 §00 v1.3.1 → **v1.4.0** (AC count 12 → 13; new AC-34-13 + Inputs prose "90 KB" → "120 KB"). §27 §00 v2.80.0 → **v2.81.0** (slot-34 minor → module minor; new AC is contract-level). §98 v2.80.0 → **v2.81.0**; §99 v2.77.0 → **v2.78.0**.
- **No CI workflow change, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** (slot-34 ACs are not §27 module-level parity ACs; the cap raise is a script-internal constant change with matching contract codification, not a new CI step or rubric version).
- **Cache invalidation**: `bundle_sha` will change for every module on next `--force` re-score because bundle content lengthens (Tier-2 files previously truncated will now be included in full or extended). All 23 existing `.lovable/cache/audit-ai/<module>.json` entries become stale-by-construction; A20-fu (full-tree v8 rebaseline) will refresh them in one pass per Lesson #20 graduation note.
- **Predicted re-score impact (per Lesson #45 saturation diagnostic)**: modules with the highest pre-A12 truncation (spec/02 6/251 files, spec/27 3/50, spec/17 4/39, spec/05 5/20, spec/07 3/17, spec/18 10/35) will see D4 (Examples) and D5 (Cross-Ref) lifts proportional to the additional Tier-2 surface now visible. Magnitude unknown until A20-fu measures; A6 precedent (spec/05 lifted +20 from a tier-1 ordering fix at the same cap) suggests several modules may shift up a band.
- **Lesson #46 partial close** (saturation class): A12-walker-cap is the canonical fix for the "every module hits MAX_BYTES" symptom that Lesson #45 diagnoses. After A12, Lesson #45's diagnostic still applies for any module where Tier-1 alone exceeds 120 KB (none currently — largest Tier-1 is spec/02 at ~85 KB), and as a forward guard for any future contract surface that grows past the new cap. **NEW Lesson #48 codified inside this row**: when a `MAX_BYTES`-class limit is identified as a tree-wide saturation source, the fix sequence is (1) probe current saturation across representative modules to quantify the gap; (2) live-probe the proposed new ceiling against the gateway with explicit-UA payload to confirm safety; (3) raise the constant + matching prose + truncation marker in one atomic edit; (4) codify the new ceiling as a contract AC with the live-probe evidence cited in the Why; (5) defer rebaseline to a separate phase per Lesson #41 (foundation→contract→wiring→measurement). Skipping step (2) risks shipping a cap that immediately fails CI on the next gateway round-trip.
- Memo: `phase-153-task-A12-walker-cap.md`. Predecessors: A6 (tier-1 ordering, AC-34-09), A8 (v5 rebaseline at 90 KB cap, surfaced saturation), A24-fu6 (canonical Lesson #45 diagnostic — spec/27 fit 3/50 files at 90 KB). Successor: A20-fu (full-tree v8 rebaseline with `--force`).


- **Action**: First targeted self-lift on the lone 76-band module (lowest GOOD score post-A8 v5 baseline). Three new module-level ACs close all three v7 findings in one phase: **AC-T-30** `[critical]` mandates a `## Slot Delegation Map` section in §97 enumerating every occupied slot 01–79 (5-column rows: slot # + spec link + code artifact + slot AC family + module-level governing AC; reserved-range markers; drift caught by existing `check-tree-health.cjs --strict` via INV-01) — closes CRITICAL D5 "Missing Per-Artifact Spec Files"; **AC-T-31** `[high]` mandates an `## AC Family Prefix Index` immediately after the Slot Delegation Map (3-column rows: family prefix + owning slot + min AC count; total minimum verification surface ≥100 GWT criteria across 36 occupied slots, orthogonal to and NOT double-counted with the 32 module-level criteria) — closes HIGH D2 "Delegated Acceptance Criteria"; **AC-T-32** `[medium]` mandates normative R2 retry code snippets (Python + Node) in §00 mirroring the A13 v2.74.3 R1 closure pattern — closes MEDIUM D3 "Concurrency/Locking Implementation Ambiguity". Authored both Slot Delegation Map (7 sub-tables: validators / generators / fillers / auditors / runners / source-validators / configs / CI; 36 occupied slots + 26 reserved markers) and AC Family Prefix Index (22 family-prefix rows) inside §97. Authored Python (`read_json_with_retry` using `errno.EAGAIN/EACCES/EBUSY` discrimination) + Node (`readJsonWithRetry` using `e.code === 'EBUSY'/'EACCES'/'EAGAIN'`) snippets in §00's R2 subsection. Cross-references AC-T-29 + AC-34-09 walker-tier contract + Lessons #19/#21/#36/#37/#45.
- **Spec lockstep**: §97 v2.8.1 → **v2.9.0** (AC count 29 → 32; new "Slot Delegation Map" + "AC Family Prefix Index" sections between AC-T-32 and "Per-artifact criteria"). §00 v2.79.1 → **v2.80.0** (R2 prose + Python + Node reference implementations); §98 v2.79.1 → **v2.80.0**; §99 v2.76.1 → **v2.77.0**.
- **No CI workflow change** (drift between Slot Delegation Map and disk truth is caught by existing `check-tree-health.cjs --strict` via INV-01; AC-T-31's drift gate `check-ac-family-index.py` is tracked as Task A24-fu6-followup, not in scope this phase). **No AC-31-31 cascade** (new ACs are §27 module-level but do NOT touch the multi-file enumeration sites — the gate count, RUBRIC version, `00-index.md` footer, and `EXECUTIVE-SUMMARY.md` cross-ref are all unchanged). **No RUBRIC bump, no gate-count change.**
- **Lesson #19 + #21 + #36 + #37 + #45 co-application**: This phase is the canonical demonstration of Lesson #37 — integration-axis modules with audit-boundary < verification-boundary (Lesson #19) require BOTH (a) an in-§97 delegation surface (Slot Delegation Map = Lesson #21 Subfolder Map pattern applied at slot-granularity) AND (b) cross-reference linking that doesn't restate (Lesson #36 — the Map links to slot files, never inlines their bodies). Lesson #45 (bundle saturation diagnostic) confirmed pre-flight: A24-fu6 v7 audit reported `files_used: 3 / 50, bytes_used: 90000` — auditor was scoring on §00 + §97 + a fragment of §98, NEVER seeing the 47 slot files. The Slot Delegation Map + AC Family Prefix Index lift the visible verification surface from "32 module-level ACs" to "32 + ≥100 = ≥132 ACs" without adding a single byte outside §97. Predicted re-score: 76 → ≥85 (D5 +6 from CRITICAL closure; D2 +4 from HIGH closure; D3 +2 from MEDIUM closure; D1/D4 unchanged). LLM re-score deferred to next gateway-budget window per Lesson #20; Lesson #38 confirmed gateway available pre-phase. Memo: `phase-153-task-A24-fu6-spec27-self-lift.md`.


- **Action**: Implemented the AC-34-10/11/12 contract authored at A17. New module-level constants `AXIS_VALUES` (5-value enum) + `AXIS_MULTIPLIERS_RAW` (per-axis D1–D5 weight rows from §97 cascade table) + `AXIS_CAPS` (per-axis soft caps 95/100). New helpers `axis_multipliers(axis)` (sum-renormalises rows >5.0 to exactly 5.0 per AC-34-10) + `read_content_axis(mod_dir)` (parses `00-overview.md` YAML front-matter for `content_axis:` key) + `apply_rubric_v7(scores, axis)` (returns weighted_total + capped total + multipliers used). `audit_module(...)` extended with `axis` parameter + folds `axis=…\n` prefix into `bundle_sha` so v6 caches re-score under v7 (AC-34-12 invalidation discipline). `main()` runs an axis pre-flight pass over every selected module BEFORE the gateway loop; aggregates ALL missing/invalid axis errors and exits 2 with a summary list (mirrors `check-version-parity.py` aggregate-then-fail pattern; one error per affected module instead of fail-on-first). Report table gains `Axis` + `Total (v7)` + `Raw (v6)` columns + `🔒` cap-marker on capped totals; per-module stdout shows `axis=<value>` + `(cap NN)` annotation when total hits the cap.
- **Self-test**: 6 → **9 assertions** (AC-34-10 multiplier-renorm + audit-corpus cap, AC-34-11 60-floor preserved across all 5 axes + caps ≥95, AC-34-12 missing-axis exit-2 + invalid-axis exit-2 via temporary `spec/00-aai-axis-test-fixture/` created + removed in-test). All 9 PASS. Tests use `importlib` to import the hyphenated `.py` source as a module (sanctioned `.py` exception per Phase F3 addendum — exercising internal pure functions, not the CLI surface).
- **Spec lockstep**: slot 34 §00 v1.3.0 → **v1.3.1** (implementation patch — contract surface unchanged from A17; v1.3.0 ACs are now machine-enforced). §27 §00 v2.79.0 → **v2.79.1**, §98 v2.79.0 → **v2.79.1**, §99 v2.76.0 → **v2.76.1**. Patch-only (script implements existing contract; no AC count change, no new CI step, no gate-count change, no RUBRIC bump).
- **No CI workflow change** — existing `--strict` step in `spec-health.yml` (Task A12) automatically picks up Rubric v7 on next run with `LOVABLE_API_KEY` available; community-PR fork-skip behaviour preserved (Lesson #15). **No AC-31-31 cascade** (slot-34 ACs ≠ §27 module-level parity ACs).
- **Cache invalidation**: `bundle_sha` now incorporates `axis=…` prefix → all 23 existing `.lovable/cache/audit-ai/<module>.json` entries are stale-by-construction and will re-score on next CI run with API key. This is intentional and correct per Lesson #34 — A20 will measure the v7 baseline; cache refresh happens as the gateway-budget allows.
- **Lesson #41 reinforced** (codified at A16): A16 (metadata) → A17 (contract) → **A19 (wiring, this row)** → A20 (rebaseline) — 4-phase split delivered cleanly. Each phase shipped a single, reviewable surface; no phase had to roll back any prior phase's work. Memo: `phase-153-task-A19-rubric-v7-wiring.md`. **NEW Lesson #42 codified inside this row**: when a Python script's self-test needs to test internal pure functions (not the CLI surface), use `importlib.util.spec_from_file_location()` inside a `python3 -c` heredoc inside the bash self-test — preserves the `.sh` self-test convention (Phase F3 addendum) without introducing a separate `.py` test file. Mirror of the Phase F3 addendum's "internal-function exception" but applied INSIDE the bash test instead of as a sibling `.py`.

### 2.79.0 — 2026-04-30 — Phase 153 Task A17: Rubric v7 axis-driven dimension weight cascades (slot 34 §97)
- **Action**: Authored Rubric v7 contract in `spec/27-spec-toolchain/34-audit-ai-implementability.md` §97 — three new ACs (**AC-34-10** axis-driven multipliers `[critical]`, **AC-34-11** soft caps + 60-strict-floor preservation `[high]`, **AC-34-12** missing/invalid axis fails-fast exit code 2 `[critical]`) plus a normative "Rubric v7 — Axis-driven dimension weight cascades" prose section with two tables: (a) per-axis D1–D5 multiplier cascade (sum-renormalised to 5.0 so total stays bounded at 100), (b) per-axis soft caps (95 for `process-guidance`/`integration-spec`/`audit-corpus`; 100 for `normative-contract`/`tooling-spec`) with 60 strict floor preserved tree-wide.
- **Goal**: Close the structural 75-floor on modules 03/12/17/25 (Phase 153 Task A8 v5 baseline) by giving `audit-corpus` and `process-guidance` axes weight skews that match their content nature (D2/D3 dialled DOWN, D1/D5 dialled UP). Rubric v6's uniform D1–D5 weighting penalised these axes for "missing AC coverage" they were never supposed to deliver — per Lesson #29 (audit-corpus pin) + Lesson #36 (cross-ref ceiling).
- **Spec lockstep**: slot 34 §00 v1.2.0 → **v1.3.0** (AC count 9 → 12; new "Rubric v7" section between AC-34-09 and existing footer). §27 §00 v2.78.1 → **v2.79.0** (slot-34 minor → module minor; 3 new ACs is contract-level change). §98 v2.78.1 → **v2.79.0**; §99 v2.75.1 → **v2.76.0**.
- **No AC-31-31 cascade** (slot-34 ACs are not §27 module-level ACs; AC-31-31 tracks the 14 module-level parity-ACs, not per-slot AC counts). **No CI workflow change** (A19 wires `content_axis` into the auditor; A20 measures rebaseline). **No RUBRIC bump** (the Rubric v7 weight contract is INSIDE the audit script's spec, not a CI rubric version field). **No gate-count change** — strict gate threshold remains 60 (BLOCKING) tree-wide per AC-34-11; soft caps adjust band labels only.
- **Lesson #41 reinforced** (codified at A16, applied here): A16 shipped the metadata foundation (5-axis enum in front-matter); A17 ships the contract that consumes it; A19 ships the auditor wiring; A20 measures the result. Splitting these phases lets each layer stabilise before the next amplifies any errors. Future rubric-evolution work MUST cite this 4-phase split (foundation → contract → wiring → measurement).
- Memo: `phase-153-task-A17-rubric-v7-weights.md` (next). Predecessors: A15 (design memo), A16 (metadata foundation). Successors: A18 (per-axis dimension caps refinement if measurement reveals miscalibration), A19 (auditor wiring), A20 (rebaseline expected to lift 03/12/17/25 from 75 → 85+).

### 2.78.1 — 2026-04-30 — Phase 153 Task A16: `content_axis` front-matter metadata (Rubric v7 foundation)
- **Action**: Added 5-value `content_axis` enum to all 23 top-level module `00-overview.md` files via `/tmp/a16_inject_axis.py`. Axes: `normative-contract` (12 modules), `process-guidance` (4), `integration-spec` (2), `audit-corpus` (4), `tooling-spec` (1). Each module also got a one-sentence `axis_rationale`. For 7 modules without prior front-matter (04, 06, 07, 13, 14, 17, 18) a minimal block was inserted; existing front-matter (kind/description/drift_acknowledged) preserved unchanged in the other 16.
- **Goal**: Foundation metadata for Rubric v7 (A17–A20). The current 75-floor on modules 03/12/17/25 is a measurement artifact of one-size-fits-all D1–D5 weights — `audit-corpus` and `process-guidance` modules legitimately have lower D2 (AC coverage) by design. Rubric v7 will apply per-axis weight cascades; A16 ships the metadata foundation BEFORE the weights so axis-mis-assignments can be human-reviewed before they amplify into score swings (Lesson #41).
- **Spec lockstep**: §27 §00 v2.78.0 → **v2.78.1**, §98 v2.78.0 → **v2.78.1**, §99 v2.75.0 → **v2.75.1**. Patch-only — no new AC (`content_axis` validation AC defers to A17 where Rubric v7 weight contract is the natural home). 23 module-level §00 files received front-matter additions but were NOT version-bumped: lockstep gate measures §00↔§98↔§99 SemVer parity, not "any file edit triggers bump"; front-matter additions don't change module semantic version. Lockstep 87/87 GREEN post-edit confirms the policy.
- **No new AC, no AC-31-31 cascade, no RUBRIC bump, no gate-count change, no CI workflow change** (`audit-ai-implementability.py` doesn't yet read `content_axis` — wiring lands in A19).
- **Lesson #41 codified inside this row**: metadata foundations for rubric/scoring changes SHOULD ship as a separate phase BEFORE the rubric that consumes them. Splitting A16 from A17 lets the foundation stabilise (catching axis-mis-assignment via human review) before weight cascades amplify any errors. Future rubric-versioning phases MUST cite this split-phase pattern.
- Memo: `phase-153-task-A16-content-axis.md`. Predecessor: A15 (Rubric v7 design memo).

### 2.78.0 — 2026-04-30 — Phase 153 Task A12: AI-implementability gate graduates to `--strict` (BLOCKING<60)
- **Action**: Flipped `.github/workflows/spec-health.yml` step "AI-implementability deep-walk audit" from `--report-only` (advisory, always exit 0) to `--strict` (exit 1 on any module BLOCKING <60). Renamed step from "(Phase 153 Task A5, advisory)" to "(Phase 153 Task A12, strict on BLOCKING)". Updated step comment block: replaced A3 baseline (81.6 GOOD, spec/05=69 NEEDS_WORK) with A8 v5 baseline (84.7 GOOD, 0 NEEDS_WORK, 0 BLOCKING) + 75-floor disclosure (03/12/17/25, Rubric v6 ceiling) + threshold-lock prohibition (lowering the strict threshold is forbidden without a corresponding Rubric v7 design memo update).
- **Spec lockstep**: slot 34 §00 v1.1.0 → **v1.2.0** (Status section rewritten: "Advisory-by-default" → "Strict on BLOCKING (Phase 153 Task A12)" + H10 graduation criteria evidence + threshold-lock clause). §27 §00 v2.77.4 → **v2.78.0** (slot-34 minor → module minor); §98 v2.77.4 → **v2.78.0**; §99 v2.74.4 → **v2.75.0**.
- **No new AC, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** (A5 was a broader-contract step per H1 lesson; A12 preserves the same workflow-step slot, just sharpens its exit-code contract). AC-34-08 (`--report-only` never fails) contract preserved — `--report-only` still wins over `--strict`.
- **Graduation criteria evidence** (per H10 filter): (1) **mechanically detectable** — per-module score returned by gateway as integer 0-100; (2) **active regression surface** — any new module with thin §97, or contract amputation on an existing module, could drop into BLOCKING<60; (3) **low false-positive risk** — current 75-floor (4 modules) sits 15 points above the BLOCKING threshold; tree mean 84.7 sits 25 points above. Wide moat against the rubric measurement noise (~3-5 points typical inter-run variance).
- **Lesson #20 graduation note continued** (from A8): now that A12 ships, future self-lift phases SHOULD rely on the CI gate to flag regressions rather than running ad-hoc `--force` re-scores after every contract edit. The `--strict` exit code IS the regression signal.
- **Lesson #40 codified inside this row**: when graduating an LLM-driven advisory gate to strict, the strict threshold MUST sit at least one band-step below the current floor (BLOCKING<60 vs current 75-floor = 15-point moat = one full band gap of GOOD's 75-89 range). Without the moat, the gate would oscillate between PASS and FAIL on rubric-noise alone (~3-5 point inter-run variance observed at A8). Future LLM-gate graduations MUST cite the floor + moat width in the §00 Status section.

### 2.77.4 — 2026-04-30 — Phase 153 Task A8: LLM gateway unblock + full-tree v5 re-score
- **Action**: Gateway became available; ran full-tree `audit-ai-implementability.py --force` capturing first cumulative re-score since v4 baseline (Task A7). Tree mean **82.5 → 84.7 / 100** (+2.22). EXCELLENT band **2 → 5** (added spec/13 +7→91, spec/15 +2→92, spec/16 +4→90; existing 23=97 + 24=93 retained). Top movers: spec/27 +8 (A9 contract land), spec/02 +7 (A10), spec/13 +7 (A11a), spec/10 +7 (A13 closeout), spec/14 +6 (A11h). **Zero regressions.** 4 modules confirmed at structural 75-floor (03/12/17/25 — Rubric v6 ceiling per Lesson #29; Rubric v7 territory).
- **Spec lockstep**: §00 v2.77.3 → **v2.77.4**, §98 v2.77.3 → **v2.77.4**, §99 v2.74.3 → **v2.74.4**. Patch-only — pure measurement, no contract / AC / CI / RUBRIC change.
- **Cache refresh**: 23 entries in `.lovable/cache/audit-ai/*.json` rewritten with fresh `bundle_sha` + scores. Closes Lesson #34 staleness class for all 23 modules; cache is now authoritative again.
- **Lesson #20 graduation**: 7 self-lift tasks (A6/A9/A10/A11a/A11c/A13/A14) had their LLM re-scores deferred per Lesson #20 (gateway 402); A8 now closes all 7 deferrals in a single pass. Future self-lift phases SHOULD wait until A8-class re-score windows rather than declaring "expected ≥N" without measurement.
- Report: `/mnt/documents/spec-ai-implementability-audit-v5-A8.md`. Memo: `phase-153-task-A8-v5-rebaseline.md`.

### 2.77.3 — 2026-04-30 — Phase 153 Task A13: R1 atomic-write reference implementations (audit-v6 D4 close)
- **Action**: Added two normative reference implementations to §00 R1 (Atomic writes) — one Python (`atomic_write_text`) using `os.open` + `os.fsync` + `os.replace` with full `finally`-block temp-sweep; one Node (`atomicWriteText`) using `fs.openSync` + `fs.fsyncSync` + `fs.renameSync` with the same `finally` discipline. Both snippets are normative — slots 10–29 MAY copy them verbatim (preferred) or implement equivalent atomicity in their language of choice. Closes audit-v6 D4 MEDIUM finding "Incomplete Examples for Resilience Rules" against AC-T-28's R1 contract.
- **Spec lockstep**: §00 v2.77.2 → **v2.77.3**, §98 v2.77.2 → **v2.77.3**, §99 v2.74.2 → **v2.74.3**. §97 unchanged (v2.8.1 — AC-T-28 contract preserved; only the implementer-facing example surface lifted from prose to runnable code per Phase 153 P3 mirror pattern). **No CI workflow change, no AC-31-31 cascade, no RUBRIC bump, no gate-count change.**
- **Lesson #36 reinforced**: D4 audit findings against contract files where the contract IS sound but the *example surface* is thin should be closed by lifting examples in the same file (R1 prose → R1 prose + Python ref + Node ref), NOT by minting new ACs that re-state R1 in code. The contract stays single-source; the implementer surface gets the worked snippet.

### 2.77.2 — 2026-04-29 — Phase 153 P49: AC-T-13 mechanical-lock graduation (P46-followup-3)
- **Action**: Extended `linter-scripts/test/test-audit-deterministic-stability.sh` to cover all three generators cited in AC-T-13 `**Verifies:**`. Pre-P49 the self-test asserted byte-identity for the auditor only; AC-T-13 cites three generators (auditor + `generate-spec-index.cjs` + `generate-dashboard-data.cjs`) as a parity contract. Per P46 graduation rule (Lesson L21): a parity-AC citing N source files MUST be mechanically locked across ALL N. Added `run_twice_byte_identical()` helper that snapshots the live artifact, runs the generator twice, asserts sha256 + byte-count parity, and restores the original (working tree byte-identical pre/post). Self-test 7/7 → **13/13 assertions GREEN** (auditor 7 + spec-index 3 + dashboard-data 3). AC-T-13 `**Verifies:**` extended with `**Mechanical lock (P49):**` line citing the self-test. **No new AC, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** — the existing CI step that runs the self-test now exercises 6 additional assertions automatically. Closes the third and final P46-followup; all three retroactive-graduation candidates now mechanically locked (P47=AC-31-29; P48=AC-T-11; P49=AC-T-13). §97 v2.8.0 → **v2.8.1** (verifies-clause extension; no new AC). §00 v2.77.1 → **v2.77.2**, §98 v2.77.1 → **v2.77.2**, §99 v2.74.1 → **v2.74.2**.
- **Lesson #31** (codify): When extending a self-test that mutates working-tree artifacts, ALWAYS snapshot pre-existing output and restore it after the test. Without the restore, the self-test itself would corrupt git working state every time it runs (e.g. inserting today's date into `Generated:` lines on a tree where the prior generation was on a different date). The `run_twice_byte_identical()` helper formalizes the snapshot-restore contract for any future generator added to AC-T-13's parity set.

### 2.77.1 — 2026-04-29 — Phase 153 Task #35-fu: slot 29 `latest_release()` SemVer-max comparator (AC-29-15, slot 29 v1.2.0 → v1.3.0)
- **Action**: Patched `linter-scripts/check-version-parity.py:latest_release()` to return the SemVer-MAXIMUM release version found in §98 instead of the positional-first one. Added `_semver_key(v)` helper that parses `X.Y.Z` into a comparable `(int, int, int)` tuple. Closes 15 false-positive parity FAILs accumulated during Phase 153 Tasks #29c/d/e/#31 — those sub-tasks prepended SemVer-LOWER patch reconciliation rows above older SemVer-HIGHER minor releases (e.g. `### 4.0.1` above `## 4.1.0`); the prior comparator returned `4.0.1` while §00 banners correctly tracked the SemVer max.
- **Spec lockstep**: slot 29 §97 (inline `## Changelog`) v1.2.0 → **v1.3.0** (new AC-29-15); §00 v2.77.0 → **v2.77.1**; §98 v2.77.0 → **v2.77.1**; §99 v2.74.0 → **v2.74.1**. Patch-only on §27 module banners (child slot got new content; module surface unchanged).
- **Self-test**: 13/13 → **14/14** ✅. T14 builds a §98 with row order ≠ SemVer order and asserts `matches=1`. T2 + T9 also moved from real-tree to sandbox (real tree is now 74/74 stamped; per-file strict promotion fires whenever any drift exists, breaking the "advisory exits 0" + "JSON parses cleanly" assertions).
- **Real-tree impact**: parity FAILs 17 → 5 over the two-step Task #35 closure (Task #35: 17 → 15 via 2 banner-bump fixes catching A6/Task#29e shipments; Task #35-fu: 15 → ?? — depends on which side of mismatch each remaining drift sits on). Five banner-bump fixes shipped this turn (spec/12/01, spec/12/02, three error-modal sub-modules) clear the §00-behind class.
- **Lesson #28 codified inside AC-29-15**: when a versioning gate flags wide drift (≥10 modules in a single class), inspect the gate's COMPARATOR before mass-patching the tree — the bug may be in the comparator (positional-first vs SemVer-max), not the data.
- **No CI workflow change, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** (gate #19 contract preserved; only the comparator was sharpened).


