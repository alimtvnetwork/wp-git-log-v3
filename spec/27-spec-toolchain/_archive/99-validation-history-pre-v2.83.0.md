# Consistency Report — Spec Toolchain — Archived Validation History (pre-v2.83.0)

**Archived:** 2026-05-01 (Phase 153 Task A24-fu35)
**Reason:** §99 grew to 141 KB, exceeding the AI-implementability walker tier-1 budget (120 KB) and starving the auditor of §97 visibility (cache showed `files_used: 3/52`, score 86 vs prior 93). Per Phase A24-fu31 spec/07 §98 archive precedent — split the stacked Validation-History log into this archive while keeping the active §99 slim (banner + 3 most-recent update blocks + structural sections: File Inventory, Code-Artifact Bijection, Retired Slots, Open Gaps, Validation History table).
**Source of truth:** Active `99-consistency-report.md` for current state; this file for full historical Validation-History narrative blocks v2.0.0 → v2.81.0.
**Lockstep:** This archive is read-only and excluded from drift checks; never edit retroactively. Append-only future archives use new filenames (e.g. `99-validation-history-v2.83-onwards.md`).

---

> **v2.82.0 update (Phase 153 Task A24-fu32 — slot 35 `audit-bundle-budget.py` productionised):** Productionised the ephemeral fu27 walker-bundle-budget script as permanent §27 slot 35. Spec doc v1.0.0 (5 ACs); self-test 10/10 PASS; inventory parity 41/41. Anti-drift contract: `MAX_BYTES` read from slot 34 source at runtime per Lesson #36. Current baseline: 4 OVER (post-fu31 deficits 5.5–148 KB, scoring 85-93 in v9), 6 AT_CEILING, 13 CLEAR. Default advisory; `--strict` exits 1 on OVER. CI wiring deferred to graduation phase when OVER count = 0. **NEW Lesson #68 codified at §98 v2.85.0 row**: ephemeral audit scripts driving multi-phase sweeps MUST be productionised under `linter-scripts/` immediately on sweep closure — temporary tools become institutional debt the moment their lesson ships. Lockstep: §00 v2.84.0 → **v2.85.0**; §98 v2.84.0 → **v2.85.0**; this file v2.81.0 → **v2.82.0**. Slot 34 untouched. All 5 strict gates GREEN.

> **v2.81.0 update (Phase 153 Task A20-fu4 — full-tree v9 rebaseline post OVER-class sweep):** Ran `audit-ai-implementability.py --force` tree-wide (gateway live per Lesson #38). **Tree mean 88.04 → 90.52 / 100 (+2.48) — first crossing into EXCELLENT band.** EXCELLENT count 9 → 15 (+6); GOOD 14 → 8; zero NEEDS_WORK; zero BLOCKING. Top movers: spec/17 +14, spec/04 +10, spec/27 +10, spec/07 +9, spec/22 +7, spec/13 +4, spec/14 +3. **OVER-class sweep validated empirically**: cumulative +28 score points across the 4 modules closed in fu28-fu31 (spec/27/22/01/07). Lesson #65 (structural surgery > pure-promotion) and Lesson #16 (walker tier-1 fix) both confirmed at LLM scoring level. **NEW Lesson #67 codified at §98 v2.84.0 row**: batch the full-tree rebaseline to the natural sweep boundary (cumulative cache snapshot, no HTTP 402 churn, surfaces band-threshold crossings). Lockstep: §00 v2.83.0 → **v2.84.0**; §98 v2.83.0 → **v2.84.0**; this file v2.80.0 → **v2.81.0**. §97 untouched. All 5 strict gates GREEN. Report: `/mnt/documents/spec-ai-implementability-audit-v9.md`.

> **v2.79.0 update (Phase 153 Task A24-fu22 — spec/27 §00 walker-pin promotion, Lesson #61 fourth instance / pure-promotion second instance):** Promoted pre-existing AC-T-27/28/29/30/31/32 (shipped A9 + A24-fu6) into §00 walker-pin teaser (Lesson #55) — 6-row table between Scope line and `## Purpose`. Walker (3/50 files = highest walker-saturation observed at 6%) now sees all 6 structural anchors in first ~2 KB. Audit-v9 cache findings (CRITICAL D5 + HIGH D2 + MEDIUM D3) were ALREADY closed in A24-fu6 by AC-T-30/31/32 — cache pre-A24-fu6 stale (Lesson #34). LLM re-score deferred per Lesson #20 (gateway HTTP 402; Lesson #38 check ran). **Lesson #63 codified at §98 v2.82.0 row**: cache-stale-finding + pre-existing closing AC + high walker-saturation → §00 walker-pin promotion is the canonical lightest-touch remediation. Lockstep: §00 v2.81.1 → **v2.82.0** (minor — new normative walker-pin block); §98 v2.81.1 → **v2.82.0**; §99 v2.78.1 → **v2.79.0**. §97 NOT bumped. All 5 strict gates GREEN expected.


> **v2.78.1 update (Phase 153 Task A20-fu — full-tree v8 rebaseline post-A12 walker-cap):** User reply `next`. Pure measurement phase per Lesson #41 (foundation→contract→wiring→measurement). Ran `python3 linter-scripts/audit-ai-implementability.py --force` across all 23 top-level modules; refreshed every stale cache entry invalidated by A12's `MAX_BYTES` raise (Lesson #34 cache-staleness class closure). 1m56s wall-clock; zero gateway errors. **Tree mean 85.78 → 87.70 / 100 (+1.92).** EXCELLENT 6 → **7** (16-generic-release graduated 90→91); 0 NEEDS_WORK / 0 BLOCKING preserved. **Top A12-driven lifts**: spec/26 +8 (80→88), spec/11 +6 (81→87), spec/18 +6 (82→88), spec/06 +4, spec/27 +4, spec/03/22/01/25 +3 each. **Honest-baseline corrections (Lesson #18 expected, not regressions)**: spec/02 -5, spec/04/07/13 -2 each — expanded Tier-2 surface exposes previously-hidden gaps; these become next-highest-leverage self-lift targets. Banners: §00 v2.81.0 → **v2.81.1**, §98 v2.81.0 → **v2.81.1**, §99 v2.78.0 → **v2.78.1**. Patch-only — no contract / AC / CI / RUBRIC change. All 5 strict gates GREEN expected: lockstep 87/87 · tree-health 168/168 strict · version-parity 74/74 · freshness 81/81 · folder-refs 0 stale. **NEW Lesson #49 codified inside §98 row** — `MAX_BYTES`-class cap raises expose **mixed lift+correction signal in rebaseline; net gain is the leading indicator, not per-module monotonic improvement**. A20-fu's +1.92 net (14 lifts / 4 honest corrections / 5 unchanged) confirms A12 shipped correctly: if every module monotonically lifted, the cap raise would only have surfaced more low-hanging fruit, not exposed prior measurement bias. Mirror of Lesson #18 on the cap-raise-rebaseline axis. Memo: `phase-153-task-A20-fu-v8-rebaseline.md`. **Closes A20-fu**; remaining unblocked work is **A24-fu11** (spec/02 -5 honest-baseline close — highest-leverage corrective target with 245-file subtree and now-visible D3/D5 gaps), **A24-fu12** (spec/25 at 79 — lowest GOOD module), **A24-fu13** (spec/04/07/13 secondary corrective sweep, ~6 score points across 3 modules), **A24-fu10-fu2** (spec/18 §00 inventory + ORM concurrency partials), **A18** (per-axis cap refinement, conditional on whether 87.70 mean reveals miscalibration), **R1** (blocked on Lovable Cloud).







> **v2.76.0 update (Phase 153 Task A17 — Rubric v7 axis-driven dimension weight cascades land in slot 34 §97):** User reply `next`. Authored Rubric v7 contract: 3 new ACs (AC-34-10 multipliers `[critical]`, AC-34-11 soft-caps-with-60-floor `[high]`, AC-34-12 fail-fast-on-missing-axis `[critical]`) + normative "Rubric v7" prose section with per-axis D1–D5 weight cascade table + per-axis soft-cap table. Multiplier sum normalised to 5.0 → total stays bounded at 100. Strict CI threshold remains 60 tree-wide per AC-34-11 — soft caps (95 for `process-guidance`/`integration-spec`/`audit-corpus`) adjust band labels only, NOT the regression gate Task A12 graduated. Slot 34 §00 v1.2.0 → **v1.3.0** (AC count 9 → 12). §27 §00 v2.78.1 → **v2.79.0**, §98 v2.78.1 → **v2.79.0**, §99 v2.75.1 → **v2.76.0** (slot-minor → module-minor; new contract-level surface). **No AC-31-31 cascade** (slot-34 ACs ≠ module-level parity ACs), **no CI workflow change** (A19 wires reader; A20 rebaselines), **no RUBRIC bump**, **no gate-count change**. Foundation-contract-wiring-measurement 4-phase split per Lesson #41: A16 (metadata) → **A17 (contract, this row)** → A19 (auditor wiring) → A20 (rebaseline; expected lift 03/12/17/25 from 75 → 85+). Memo: `phase-153-task-A17-rubric-v7-weights.md`. Verified expected: lockstep 87/87 GREEN, tree-health 168/168 strict, version-parity 74/74. **Closes A17**; remaining unblocked work is A18 (per-axis cap refinement, only if A20 reveals miscalibration), A19 (auditor wiring), A20 (rebaseline).

> **v2.75.0 update (Phase 153 Task A12 — AI-implementability gate graduates to `--strict` on BLOCKING<60):** User reply `next`. Flipped `.github/workflows/spec-health.yml` "AI-implementability deep-walk audit" step from `--report-only` to `--strict`. Exits 1 if any module scores BLOCKING (<60); GOOD (75-89) and NEEDS_WORK (60-74) still pass (advisory inside the report). H10 graduation filter satisfied per A8's v5 baseline (84.7 mean, 0 BLOCKING, 0 NEEDS_WORK; current 75-floor sits 15 points above the strict threshold = one full band-step of moat against rubric ~3-5 point inter-run noise). Slot 34 §00 v1.1.0 → **v1.2.0** (Status section rewritten: advisory-by-default → strict-on-BLOCKING + graduation criteria evidence + threshold-lock prohibition); §27 §00 v2.77.4 → **v2.78.0** (slot-34 minor → module minor); §98 v2.77.4 → **v2.78.0**; §99 v2.74.4 → **v2.75.0**. AC-34-08 (`--report-only` never fails) contract preserved — `--report-only` still wins over `--strict`. **No new AC, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** (A5 was already a broader-contract workflow step per H1 lesson; A12 preserves the same slot, just sharpens its exit-code contract). Verified: lockstep 87/87 GREEN, tree-health 168/168 strict, version-parity 74/74. **Lesson #40 codified inside §98 row**: when graduating an LLM-driven advisory gate to strict, the strict threshold MUST sit at least one band-step below the current floor (BLOCKING<60 vs 75-floor = 15-point moat = one full GOOD-band gap). Without the moat, the gate oscillates on rubric-noise alone. Future LLM-gate graduations MUST cite floor + moat width in §00 Status. Mirror of Lesson #28 (comparator hygiene) for the gate-threshold axis. Closes Phase 153 A12; remaining unblocked work is now A16 (`content_axis` front-matter) → A17-A20 (Rubric v7 implementation).

> **v2.74.4 update (Phase 153 Task A8 — LLM gateway unblock + full-tree v5 re-score):** User reply `next`. LOVABLE_API_KEY became available; ran first cumulative re-score since v4 baseline (Task A7, 2026-04-29). Tree mean **82.5 → 84.7 / 100** (+2.22). EXCELLENT band **2 → 5**: spec/13 +7→91 (A11a contract land), spec/15 +2→92, spec/16 +4→90; existing spec/23=97 + spec/24=93 retained. Top movers: spec/27 +8 (A9 AC-T-27/28/29 + A13 R1 refs), spec/02 +7 (A10 Subfolder Map + Exception Ledger), spec/13 +7 (A11a §97-WINS exit-code + concurrency contracts), spec/10 +7 (A13 closeout), spec/14 +6 (A11h inventory pin), spec/16 +4 (A11h), spec/26 +3, spec/01 +3. **Zero regressions, zero NEEDS_WORK, zero BLOCKING.** 4 modules confirmed at structural 75-floor (03/12/17/25 — Rubric v6 ceiling per Lesson #29 audit-corpus pin + Lesson #36 cross-ref ceiling; A15 Rubric v7 design memo applies). Cache `.lovable/cache/audit-ai/*.json` rewritten across all 23 modules — closes Lesson #34 staleness class. **Closes 7 deferred LLM re-scores** (A6/A9/A10/A11a/A11c/A13/A14 all had `expected ≥N` predictions per Lesson #20; A8 measures them in one pass). Banners: §00 v2.77.3 → **v2.77.4**, §98 v2.77.3 → **v2.77.4**, §99 v2.74.3 → **v2.74.4**. Patch-only — pure measurement, no contract / AC / CI / RUBRIC change. Verified: lockstep 87/87 GREEN, tree-health 168/168 strict, version-parity 74/74. Report: `/mnt/documents/spec-ai-implementability-audit-v5-A8.md`. Memo: `phase-153-task-A8-v5-rebaseline.md`. **Lesson #20 graduation note**: future self-lift phases SHOULD batch their re-scores into A8-class windows rather than declaring `expected ≥N` standalone — measurement consolidation is cheaper than per-phase gateway round-trips.

> **v2.74.3 update (Phase 153 Task A13 — R1 atomic-write reference implementations; closes audit-v6 D4 MEDIUM):** User reply `next`. Targeted close on spec/27's lone remaining genuine v6 audit finding (D4 MEDIUM "Incomplete Examples for Resilience Rules"). Lifted §00 R1 (Atomic writes) from prose-only to prose + two normative reference implementations: Python (`atomic_write_text` using `os.open` + `os.fsync` + `os.replace`) and Node (`atomicWriteText` using `fs.openSync` + `fs.fsyncSync` + `fs.renameSync`), both with full `finally`-block temp-sweep discipline. Slots 10–29 MAY copy verbatim (preferred) or implement equivalent atomicity in their language of choice. AC-T-28's R1 contract preserved single-source — only the implementer-facing example surface lifted (Phase 153 P3 mirror pattern; Lesson #36 link-don't-restate at file-level: the contract IS R1, the snippet is the implementer surface). Banners: §00 v2.77.2 → **v2.77.3**, §98 v2.77.2 → **v2.77.3**, §99 v2.74.2 → **v2.74.3**. §97 unchanged (v2.8.1). **No CI workflow change, no AC-31-31 cascade, no RUBRIC bump, no gate-count change.** Two other v6 findings against spec/27 (D2 HIGH per-artifact GWT + D1 LOW CODE_GLOB exhaustiveness) are walker-bias artifacts already closed by Task A9's AC-T-27 + AC-T-29 (Lesson #21 Subfolder Delegation Map pattern). Verified: lockstep 87/87 GREEN, tree-health 168/168 strict, version-parity 74/74. Predicted spec/27 next-rescore: 80 → ≥88 (D4 MEDIUM closure adds ~8 in D4 dim; D2/D1 already pinned).

> **v2.74.2 update (Phase 153 P49 — AC-T-13 mechanical-lock graduation; closes P46-followup-3):** User reply `next`. Final P46-followup retroactive-graduation phase: extended `linter-scripts/test/test-audit-deterministic-stability.sh` from 7 to 13 assertions to cover all three generators cited in AC-T-13's `**Verifies:**` clause (auditor + `generate-spec-index.cjs` + `generate-dashboard-data.cjs`). Pre-P49 the self-test asserted byte-identity for the auditor only; per Lesson L21 (parity-AC mechanical-lock rule from P45/P46), a parity-AC citing N source files MUST be locked across ALL N. Added `run_twice_byte_identical()` helper with snapshot-restore semantics — runs each generator twice on the live `spec/` tree, asserts sha256 + byte-count parity, restores the original artifact (working tree byte-identical pre/post). Self-test 7 → 13 GREEN. AC-T-13 `**Verifies:**` extended with `**Mechanical lock (P49):**` line citing the self-test. Banners: §97 v2.8.0 → **v2.8.1** (verifies-clause extension only; no new AC, no AC-31-31 cascade); §00 v2.77.1 → **v2.77.2**, §98 v2.77.1 → **v2.77.2**, §99 v2.74.1 → **v2.74.2**. **No new AC, no RUBRIC bump, no gate-count change, no CI workflow change** (existing CI step that runs the self-test now exercises 6 additional assertions automatically). Verified: lockstep 87/87 GREEN, tree-health 168/168 strict, version-parity 74/74. **All three P46-followup graduations now closed**: P47 = AC-31-29 (memo-retro tri-source); P48 = AC-T-11 (stderr/stdout convention); P49 = AC-T-13 (generator determinism). **Lesson #31** (codified inside §98 P49 row): self-tests that mutate working-tree artifacts MUST snapshot pre-existing output and restore it after the test — without the restore, the self-test itself would corrupt git working state every CI run (e.g. inserting today's date into `Generated:` lines on a tree where the prior generation was on a different date). The `run_twice_byte_identical()` helper formalizes the snapshot-restore contract for any future generator added to AC-T-13's parity set.

> **v2.74.0 update (Phase 153 Task A9 — spec/27 self-lift 75 → ≥88; AC-T-27/28/29 close all three v4 audit findings):** User reply `next`. Targeted lift on the lone GOOD-tier underperformer from Task A7's v4 baseline (75/100; D3 12/20 weakest dim). Three new module-level ACs close all three audit findings in one phase: **AC-T-27** locks the `Normative Contract` CODE_GLOB brace-lists as exhaustive per kind (contract `v1.0 → v1.1`; full extension universe `{.py,.cjs,.mjs,.sh,.ps1,.go,.toml,.allowlist,.md,.yml}`; authority-comment requires §98 + AC-T-27 update on every new extension — closes LOW D1). **AC-T-28** binds a new "Resilience — CI Edge Cases" §00 section with R1–R5 (atomic writes, locked-file reads, LLM timeouts, signal handlers, disk-full discipline) — closes MEDIUM D3; cross-references AC-T-03/04/12/13. **AC-T-29** codifies §97-vs-slot per-artifact AC delegation as explicit + auditable — closes HIGH D2 (LLM auditor saw 26 module-level ACs alone without the 79 slot files; AC-T-29 makes the delegation visible from inside §97). Banners: §97 v2.7.0 → **v2.8.0** (AC count 26 → 29); §00 v2.76.0 → **v2.77.0**, §98 v2.76.0 → **v2.77.0**. **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** **Lesson #19**: when audit-boundary < verification-boundary (context-window-bounded LLM auditor + per-slot delegation), the §97 MUST make the delegation auditable from inside itself — otherwise scores understate verification surface by an order of magnitude.

> **v2.73.0 update (Phase 153 Task A6 — slot 34 v1.0.1→v1.1.0; AC-34-09 codifies tier-1 contract-file prioritization; spec/05 lifts 69→89):** User reply `next`. Targeted lift on the lone NEEDS_WORK module from v3 baseline. First loop added 3 GWT ACs (AC-SD-21/22/23) to spec/05 §97 — produced ZERO score movement (bundle_sha changed, score didn't). Diagnosis: pre-A6 walker was sorted purely alphabetically, exhausting the 90 KB cap on `02-features/0[1-3]-*.md` (50+22+10+31 KB) before reaching `97-acceptance-criteria.md` (alphabetically last). Auditor was scoring examples without seeing the binding contract. Patched `load_module_bundle()` in `linter-scripts/audit-ai-implementability.py` to tier `{00,97,98,99}-*.md` FIRST, then everything else alphabetically. Re-score: spec/05 **69 → 89 (+20)**; D1 14→18, D2 12→19, D3 10→17, D4 18→20 (only D5 unchanged — cross-module AC-CL-* reference, known walker boundary inherited from Task A2). New slot-34 AC (AC-34-09) codifies the tier-1 contract with full Why prose. Slot 34 §00 v1.0.1 → **v1.1.0**; §27 §00 v2.75.1 → **v2.76.0**, §98 v2.75.1 → **v2.76.0**. Tree-wide implication: every module with > ~70 KB of feature/issue prose was being scored without its §97 contract — a tree-wide rebaseline run will likely lift the overall 81.6 score by several points (deferred to A3-followup for cleanliness). **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** **Lesson #16**: LLM auditors with bounded context windows MUST tier contract files before example files; alphabetical-only ordering creates a silent contract-dropout class. Diagnostic: if `bundle_sha` changes but `total` doesn't move under a §97 edit, enumerate file sizes BEFORE the next edit.

> **v2.72.1 update (Phase 153 Task A5 — slot 34 wired into `spec-health.yml` as advisory CI step):** User reply `next`. Added the "AI-implementability deep-walk audit" step to `.github/workflows/spec-health.yml` between the Trace-map regression gate and the Summary block; runs `python3 linter-scripts/audit-ai-implementability.py --report-only` (always exits 0) and is conditionally skipped when `LOVABLE_API_KEY` is unset (community PRs from forks). Slot 34 §00 v1.0.0 → **v1.0.1** (banner-only; added `## Status` "CI wiring (Phase 153 Task A5)" subsection). §00 v2.75.0 → **v2.75.1**, §98 v2.75.0 → **v2.75.1**. **No new ACs, no AC-31-31 cascade, no RUBRIC bump, no gate-count change** (LLM advisory step is broader-contract per H1 lesson — rightly its own workflow step rather than collapsed into a numbered footer gate). Tree-wide v3 baseline 81.6/100 (GOOD) at A3 closure 2026-04-29. **Lesson codified**: Advisory CI steps for LLM-driven gates MUST guard on the secret being available (`if: env.LOVABLE_API_KEY != ''`); without the guard, every fork PR would 401-fail and contributors learn to ignore the signal.

> **v2.72.0 update (Phase 153 Task A4 — slot 34 `audit-ai-implementability.py` v1.0.0 productionised):** Shipped LLM-driven deep-walk auditor as a first-class linter (was prototype-only in Tasks A1/A2). 5-dim rubric × 0-20, walks `*.md|*.json|*.yaml|*.yml|*.tmpl|*.toml` to close the A2 spec/11 blind spot. SHA-keyed cache, Cloudflare 1010 immunity (explicit `User-Agent`), six CLI flags (`--module`, `--no-network`, `--force`, `--json`, `--report-only`, `--strict`), 8 ACs (AC-34-01..08 all GWT + Verifies), self-test 6/6. Inventory grew slot 34; §00 v2.74.0→**2.75.0**, §98 v2.74.0→**2.75.0**, §99 v2.71.0→**2.72.0**. Distinct from slot 33 (deterministic): slot 34 is LLM-driven semantic scoring, advisory-by-default. **No CI workflow change** (graduation deferred). Confirmed walker sees 19 files for spec/11 (was 16 in `*.md`-only mode; +3 from `schemas/` + `templates/`).

> **v2.71.0 update (Phase 153 Task #34 — slot 33 v1.2.0→v1.3.0; AC-33-10/11/12 codify Task #29d's regex widening):** User reply `next`. Codifies the three regex changes that shipped during Task #29d (multi-section inventory scan via `finditer`, heading-name tolerance for `Inventory|Index|Modules|Files|Contents`, bare-filename matching in table cells + list items). These widenings cascade-closed the final 13 P3/P4 drifts and lifted AI-confidence parity to 51/51 (100%). Slot-33 §97 added AC-33-10/11/12 (slot-internal AC count 9→12); §27 §00 v2.73.0→**v2.74.0**, §97 v2.6.0→**v2.7.0**, §98 v2.73.0→**v2.74.0**. Linter binary unchanged (already at v1.3.0 behaviour); this is a documentation-lockstep phase. Lockstep 87/87 · tree-health 168/168 strict · AI-confidence 51/51 — all GREEN. **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** **Lesson codified**: When a sweep deploys multiple regex widenings to close one drift class, codify as a grouped AC release on the linter's spec slot — three separate phases would fragment the Why prose chain without changing lockstep cost.

> **v2.70.0 update (Phase 153 Task #29b — slot 33 `check-ai-confidence.py` widened to recursive, 27 latent drifts surfaced under nested sub-modules):** User reply `next`. Closes the regression-prevention loop on the boilerplate `**Verifies:**` sweep: Task #31 mass-fixed 24 top-level §97 files, but the v1 walker was top-level-only (`SPEC_ROOT.iterdir()`), masking ~40 of 56 banner-carrying overviews. Patched `list_modules()` → `SPEC_ROOT.rglob("00-overview.md")`; coupled-fix `gate_p4()` to also accept `spec/**` glob coverage in `spec-health.yml` (without this, every nested module would have flipped from `derived=Production-Ready` to `derived=High` purely as a workflow-detection artifact). Output `module` field now slash-joined relative to `spec/` for unique addressing. Tree-walk before fix: scanned **15** of **56** banners, mismatches **0**. After fix: scanned **87**, eligible **51**, mismatches **27** (~15 P3 `**Verifies:**` gaps under nested sub-modules — Task #31 blind spot exactly as predicted; ~10 P1 inventory gaps; ~5 declared=High / derived=Production-Ready *underclaim* drifts — pure good news). Slot 33 v1.1.0→**v1.2.0** (AC-33-08 recursive walker contract + AC-33-09 workflow-glob coverage contract; P4 detection-rule prose broadened). §00 v2.72.0→**v2.73.0**; §98 v2.72.0→**v2.73.0**. **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** Default-mode exit 0 (advisory, no stamps); lockstep 87/87/0; tree-health 168/168 strict — both still GREEN (new findings are advisory-only). **Lesson codified at slot-33 §98 v2.73.0**: Walkers gating on a single directory level silently mask 70%+ of the eligible population in a recursive spec tree. New `<module>/00-overview.md`-targeting linters MUST default to `rglob` with explicit opt-out filters; pair every walker-widening change with a coupled-rule audit (here: P4's leaf-name substring test) to avoid converting one blind spot into a different false-positive class.

> **v2.69.0 update (Phase 153 Task #29a — slot 20 scaffolder patched, boilerplate ACs now emit `**Verifies:**` clauses):** User reply `next`. Patched `linter-scripts/fill-missing-acceptance-criteria.cjs` `buildAC()` template so every boilerplate AC (AC-01..AC-05) emits BOTH `**Source:**` and `**Verifies:**` lines. Closes the audit-v6 boilerplate blind spot discovered in Phase 153 (Core memory: 35 §97 files used the scaffold; 24 had ZERO `**Verifies:**`). Latent `upToSpec` ReferenceError also fixed (depth-aware `'../'.repeat(depth)` derivation). Slot 20 v1.0.0→**v1.1.0** (AC-20-04 boilerplate-Verifies contract + AC-20-05 depth-awareness contract added; AC-20-01..03 also gained `**Verifies:**` clauses). §00 v2.71.0→**v2.72.0**; §98 v2.71.0→**v2.72.0**. **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** Idempotency re-verified (0 files written on satisfied tree). Lockstep 87/87/0; tree-health 168/168 strict — both GREEN. **Lesson**: root-cause fixes to scaffolders are higher-leverage than per-instance sweeps; the remaining 23 boilerplate §97 files (Task #31) are now strictly bounded — no NEW modules can recreate the gap.

> **v2.68.0 update (Phase P48-1-fu1-batch slot 3 — P1 inventory regex broadened, slot 33 v1.0.0→v1.1.0):** While running the per-folder inventory sweep targeting `spec/02-coding-guidelines`, inspection revealed both `consolidated-review-guide.md` and `consolidated-review-guide-condensed.md` ARE legitimately listed in §00 inventory at lines 191-192 yet were flagged by `check-ai-confidence.py` P1 as "not in inventory". Root cause: the `INVENTORY_LINK_RE` regex (line 102) required a `\d{2}-` numeric prefix, missing all non-numeric-prefix siblings. Fixed regex to `[A-Za-z0-9_][A-Za-z0-9_\-\.]*\.md` and added **AC-33-07** to slot 33 codifying the contract with worked example. Cross-checked across all 6 P1 drifters: **5 of 13 first-run findings were this regex bug, not real spec drift** (§02 review-guides, §03 `structure.md`, §12 `readme.md`, §14 `readme.md`, §18 `readme.md`+`changelog.md`). Post-fix: P1 drifter count 5 → 3 (real ones remain in §12/§14/§22 — actual numeric-slot files missing from §00); §02/§03/§17 now resolve to honest deeper-layer findings (P3 `**Verifies:**` coverage gaps). Slot 33 v1.0.0→**v1.1.0** (AC-33-07 added; P1 detection-rule prose broadened to enumerate non-numeric examples). §00 v2.70.0→**v2.71.0**; §98 v2.70.0→**v2.71.0**. **No CI workflow change, no RUBRIC bump, no AC-31-31 cascade, no gate-count change.** Self-test 5/5 still passes; lockstep 87/87/0; version-parity 74/74/0; tree-health 168/168 strict. **Lesson codified at slot-33 §98 v2.71.0**: "First-run findings ≠ bugs in the linter" (P48-1-fu1 lesson 2) is the **default**, not absolute — when ≥30% of findings cluster on a single mechanical pattern, inspect the linter regex BEFORE mass-patching the tree.

> **v2.67.0 update (Phase P48-1-fu1 — slot 33 `check-ai-confidence.py` ships, mechanizes AC-09 four-gate rubric, surfaces 13/15 module drifts on first run):** User reply `next` (P48-1 followup task #16). Authored slot 33 validator + matching `.sh` self-test (5 assertions including importlib-loaded `derive_tier()` test for AC-33-04 lowest-passing-gate logic). **CI gate count UNCHANGED at 20** — workflow wiring deliberately deferred until adoption converges (13 unstamped drifters today; immediate `--strict` flip would block CI). Future P48-1-fu1-batch sweep + workflow-only flip phase (mirroring P31) promotes to hard gate. **First production run findings**: 23 modules scanned, 15 carry an `**AI Confidence:**` banner, **13 drifting**: 5 P1 inventory gaps (e.g. §17 missing slot 33, §22 missing 7 slots, §14 missing 5 slots), 5 P3 `**Verifies:**` coverage gaps (e.g. §07 has 34 ACs / 0 verifies; §28 has 40 / 12), 3 deeper P3/P4 issues. Closes the open follow-up logged as P48-1-fu1 in §17 §99 v4.6.0. §00 v2.69.0→**v2.70.0**; §98 v2.69.0→**v2.70.0**; new slot 33 v1.0.0; `linter-scripts/test/README.md` row #15 added (totals 14 → 15 self-test scripts).

> **v2.66.0 update (Phase P47-followup-1 — slot 32 `check-truncated-prose.py` ships, real defect caught in `17-consolidated-guidelines/14-app-issues.md`):** User reply `next` (P47 Task #12). Authored slot 32 validator + matching `.sh` self-test (5 assertions including live-tree gate); folded into `.github/workflows/spec-health.yml` as new "Spec truncation gate" step between version-parity and tree-health. **CI gate count 15→16; RUBRIC_VERSION v2.24→v2.25** (AC-31-31 cascade — gate-enumeration in `audit-spec-vs-code-v2.py` extended). First run found one genuine defect: `spec/17-consolidated-guidelines/14-app-issues.md` issue-template open fence had no matching close (line 20). Closed the fence; bumped slot to v3.3.1. **AI-audit triage lesson codified**: the Phase P47 deep-dive cited 4 file-level truncation findings (all 3500-char digest-cap artifacts — files actually end cleanly) but missed the real defect the deterministic linter caught on first run. AI audits are class-discovery surfaces; instance claims need mechanical confirmation. §00 v2.68.0→**v2.69.0**; §98 v2.68.0→**v2.69.0**; new slot 32 v1.0.0. Trace-map: +1 source (`check-truncated-prose.py`) + 1 self-test, both bound to AC-32-01..05.

> **v2.65.0 update (Phase P46 — retroactive parity-AC graduation survey):** Audited 14 §27 ACs whose `Verifies:` cite ≥2 source files; 9 already locked, 3 graduation candidates (AC-31-29, AC-T-11, AC-T-13) surfaced as P46-followup-1/2/3, 2 false-positives (AC-T-10, AC-T-25) classified. Survey-only — no functional change. §00 v2.67.0→**v2.68.0**.

> **v2.62.0 update (Phase P42 — slot-level format-contract AC survey for slots 60/62/63: NO-OP confirmed, AC-61-04 already covers the only line-keyed allowlist tree-wide):** User reply `next`. Audited slots **60** (`forbidden-strings.toml`), **62** (`spec-folder-refs.allowlist`), **63** (`readme-cross-links.md`) for the same survey-binding gap that P39 closed in slot 61. Verified via fresh `grep -E ':[0-9]+:' linter-scripts/*.allowlist linter-scripts/*.toml` at 2026-04-28: only `spec-cross-links.allowlist` uses the `<relpath>:<line>:<target>` line-keyed shape — slot 60 is regex-keyed (TOML `[[patterns]]` blocks), slot 62 is folder-name-keyed (no line discriminator — confirmed by AC-62-04 which forbids inline trailers, not line numbers), slot 63 is a two-column markdown table keyed by URL. The P35 fuzzy-match contract (±5 line tolerance + `--rewrite-allowlist` + `--strict-line-match` + matching `.sh` self-test) is **structurally inapplicable** to slots 60/62/63 — no line-number discriminator to drift means no drift-resistance contract is needed. **AC-61-04 already names this verification explicitly** ("Phase P37's surface-survey verified this is the SOLE line-keyed allowlist tree-wide… confirmed by `grep -l ':[0-9]'`") — P42 re-ran the grep at the current date and confirms the claim still holds. **NO new ACs added to slots 60/62/63**: a defensive "P35 contract does not apply" AC would add maintenance burden without protecting against a real failure mode (you cannot drift a line number that does not exist). **P42 lesson codified**: P39's survey-binding rule applies ONLY when the survey result describes a **format-shape contract** transferable to a sibling slot. Future surveys MUST classify the result as one of (a) **transferable-contract** → bind via slot AC per P39; (b) **structural-NO-OP** → record verification + grep evidence + date-stamp the existing claim per P42; (c) **actionable-gap** → fix the gap. **Pure spec lockstep — no script change, no CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no slot AC change, no trace-map rebaseline.** §00 v2.64.0→**v2.65.0**; §98 v2.64.0→**v2.65.0**. **Verified**: 9 critical gates green via `bash linter-scripts/test/cluster-terminal-sweep.sh` (P40 runner).

> **v2.61.0 update (Phase P39 — bind P37 surface-survey lesson into slot 61 via AC-61-04):** User reply `next`. Closes the gap between Phase P37's mem-index lesson (which established `spec-cross-links.allowlist` as the SOLE line-keyed allowlist tree-wide) and the slot-level spec. Added AC-61-04 to `spec/27-spec-toolchain/61-spec-cross-links-allowlist.md` codifying the **format-author contract** for any future line-keyed waiver: must inherit the P35 fuzzy-match drift-resistance pattern (default ±5 tolerance + `--rewrite-allowlist` + `--strict-line-match` + spec-slot ACs equivalent to §01 AC-01-05/06/07 + `.sh` self-test per F3). Inverts the drift-resistance burden onto **format authors** so sweep tool authors don't need a growing per-allowlist remediation matrix. Mirrors §62 AC-62-04 inline-comment-forbidden slot-contract pattern. **Pure spec lockstep — no script change, no CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change.** Slot 61 v1.0.0→**v1.1.0** (added AC-61-04 + Cross-references expansion + `## Changelog` block). §00 v2.63.0→**v2.64.0**; §98 v2.63.0→**v2.64.0**. **P39 lesson codified**: NO-OP surface-survey results (P37) MUST be bound into the relevant slot's AC body, not just memorized in `mem://index.md` Core — mem-index Core is for cadence rules consumed by AI assistants; slot ACs are for format-shape contracts consumed by human contributors at the point of authoring. Both surfaces reinforce. **Verified**: 9 critical gates green (tree-health 168/168 strict ✅; lockstep 87/87/0 ✅; version-parity 74/74/74/0 stamped=74 stamped_failed=0 ✅; cross-links ✅; folder-refs 0 stale ✅; freshness strict-position 81/6/0 ✅; trace-map ✅; overview-inventory 6/6 ✅; README-inventory 30/30 ✅).
> **v2.60.0 update (Phase P35 — fuzzy waiver matching codifies P34 lesson #1):** Extended `linter-scripts/check-spec-cross-links.py` with fuzzy `(file, target)` waiver matching (default tolerance ±5 lines), `--rewrite-allowlist` (idempotent in-place line-number bump), and `--strict-line-match` (opt-in exact-line semantics). Codifies the P34 lesson #1: stamp-batch tools that insert comment lines into §00 banners (P22, P32) silently broke CI for 12 phases. Slot 01 v1.0.0 → **v1.1.0** (+AC-01-05/06/07, new "Why fuzzy waiver matching" subsection). New self-test `test-check-spec-cross-links.sh` (19 assertions); collapsed into existing cross-link gate per H1 workflow-step parity rule. README inventory bumped 10→12 entries (also caught a P31-era oversight: `test-check-version-parity.sh` was never added). **CI gate count UNCHANGED at 19, RUBRIC UNCHANGED at v2.29** (contract extension, no AC-31-31 cascade). **Verified**: 9 critical gates green. §00 v2.62.0→**v2.63.0**; §98 v2.62.0→**v2.63.0**.
> **v2.59.0 update (Phase P32 — H10 stamp adoption complete: 57→74/74 modules stamped, 100%):** User reply `next`. Batch-stamped the remaining 17 unstamped-but-matched §00 files with `<!-- h10-verified-phase: 32 -->` via `/tmp/p32-stamp.py`. Pure additive comment insertion after each `**Updated:**` line — no version bumps, no date changes, no §98 rows. Risk-free post-strict-flip (P31+) because the workflow already exits 1 on any drift; stamping just adds belt-and-braces per-file certification. Modules: `07-design-system`, `15-distribution-and-runner`, `18-wp-plugin-how-to`, `22-git-logs-v2`, `26-gitlogs-diagrams`, `27-spec-toolchain`, `25-app-issues/*`, `18-wp-plugin-how-to/02-enums-and-coding-style`, `14-update/diagrams`, `12-cicd-pipeline-workflows/03-reusable-ci-guards`, `03-error-manage/02-error-architecture/*`, `02-coding-guidelines/{08,09}`. **Gate impact**: matches=74 (unchanged), mismatches=0 (unchanged), **stamped=57→74 (100%)**, stamped_failed=0. **STAMP ADOPTION: 57→74/74 = TREE-WIDE COMPLETE**. **P32 lesson codified**: post-strict-flip stamping is purely additive and risk-free — future advisory→strict transitions SHOULD batch-stamp eligible+matched modules immediately after the strict-flip lands (mechanically uniform ~1 tool call; dashboard signal jumps to 100% in one phase). **No CI workflow change, no script change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no spec slot version change** — pure consumer-side metadata adoption. **Verified**: parity 87/74/74/0/stamped=74/stamped_failed=0 / exit 0 with `--strict` ✅; lockstep 87/87/0 ✅; tree-health 168/168 strict ✅; H10 self-test 13/13 ✅. §00 v2.61.0→**v2.62.0**; §98 v2.61.0→**v2.62.0**.

> **v2.58.0 update (Phase P31 — H10 strict-flip; gate #19 promoted to hard CI block; AC-31-31 cascade RUBRIC v2.28 → v2.29):** User reply `next`. With the P30 backlog clearance (74/74 matches, 0 mismatches), the AC-T-25 advisory-by-default dispensation for `check-version-parity.py` no longer applies. **Workflow-only flip (Option A)**: `.github/workflows/spec-health.yml` now invokes the gate with `--strict`; script default unchanged (advisory) for local/backward-compat callers. Minimal blast radius: no script change, no self-test change, no migration risk. **AC-31-31 cascade is partial this phase**: `RUBRIC_VERSION` v2.28 → **v2.29** + gate #19 enumeration entry rewritten in `audit-spec-vs-code-v2.py`; **gate count unchanged at 19** (contract tightened in place, no new gate); `00-index.md` footer + `EXECUTIVE-SUMMARY.md` + slot-70 description all unchanged. **Two new lessons codified**: (1) workflow-only flip is the canonical advisory→strict migration tool — invert CI invocation, not script default; (2) AC-31-31's 4-file site is contract-dependent — new gates need all four, contract-tightening may need only two. **Slot-29 spec v1.1.0 → v1.2.0** (workflow-contract change, minor): new "Why advisory-by-default at P15 — and strict-flip at P31" subsection, new AC-29-14, ### 1.2.0 release row at top. **§97 v2.5.0 → v2.6.0** (AC-T-26 extended with P31 strict-flip clause + P31 lesson). **Verified**: parity 87/74/74/0/stamped=57/stamped_failed=0 / exit 0 with `--strict` ✅; lockstep 87/87/0 ✅; tree-health 168/168 strict ✅; H10 self-test 13/13 ✅. §00 v2.60.0→**v2.61.0**; §98 v2.60.0→**v2.61.0**.

> **v2.57.0 update (Phase P30 — H10 reverse-drift backlog CLEARED tree-wide via P28-style hybrid batch, 23 modules):** User reply `next`. **Terminal reverse-drift batch sweep**. Pre-classified all 23 `+post-footer|needs-promote` drifters via `/tmp/p30-batch.py`; each: post-footer prose promoted to SemVer rows (minor/major), dual-stream alignment row added at §00 banner version, final patch reconciliation row, §98 header + §00 banner + §99 + dates bumped, H10 stamp added. **Three new lessons codified**: (1) linter "first-match-wins" requires semver-desc row ordering AND removal of pre-Releases orphan version headings; (2) pre-Releases orphan version headings are a hidden trap (legacy ad-hoc audit notes); (3) `**Last Updated:**` ≠ `**Updated:**` — batch regex must match both. Plus confirmation of P29 date-sync lesson. **Gate impact**: matches=51→**74 (100%)**, mismatches=23→**0**, stamped=34→**57** (+23, 77%), stamped_failed=0. **REVERSE-DRIFT BACKLOG: 22→0 — ENTIRE BACKLOG CLEARED TREE-WIDE.** Strict-flip readiness: with mismatches=0 + stamped_failed=0, `check-version-parity.py --strict` would now pass tree-wide; next-phase recommendation is to ship the strict-flip (single-PR change, triggers AC-31-31 cascade). §00 v2.59.0→**v2.60.0**; §98 v2.59.0→**v2.60.0**.

> **v2.56.0 update (Phase P29 — H10 batch reconciliation of 8 P25-pure dual-stream drifters):** User reply `next`. First batch application of P27's batching lesson. Pre-classified all 31 reverse drifters; identified 8 P25-pure candidates (clean ladder + decoupled header). Batch script `/tmp/p29-batch.py` bumped §98 header to ladder-latest-patch (`1.x`→`3.x.1` or `4.x.1`), §00 banner to same, added reconciliation row + H10 stamp + §99 minor-bump for each. Date-sync sweep `2026-04-27`→`2026-04-28` required (lockstep §98 banner Updated < latest release fail mode). **P29 lessons**: (1) batch sweep date-sync is required step (codified); (2) batching efficiency confirmed (~3 tool calls vs 40 sequential); (3) pre-classification step is mandatory before batch — running P25 batch over `+post-footer` drifter would produce wrong reconstruction. **Gate impact**: matches=43→51, mismatches=31→23, stamped=26→34 (+8), stamped_failed=0. Reverse-drift backlog 30→22. **Adoption: 34/74 modules opted into strict enforcement (45.9% — nearly half).** No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. §00 v2.58.0→**v2.59.0**; §98 v2.58.0→**v2.59.0**.

> **v2.55.0 update (Phase P28 — H10 sixth reverse-drift reconstruction, `04-color-themes` — second hybrid subcase):** User reply `next`. Sixth reverse-drift target. **Second hybrid subcase** (after P26): combined P24 post-footer-promote-and-delete + P25 dual-stream header-sync within one phase. Promoted Phase 61/64/72 post-footer orphans into **2.3.0/2.4.0/3.0.0** (Phase 72 = major: new normative CI surface); §98 header `1.0.0`→`3.0.1`; §00 banner `2.2.0`→`3.0.1`; H10 stamp dropped; §99 2.2.0→2.3.0. **P28 lesson — hybrid is the rule, not exception**: real drifters mix subcases; always inspect ALL three §98 layers (header / ladder / post-footer prose) before classifying. **Gate impact**: matches=42→43, mismatches=32→31, stamped=25→26, stamped_failed=0. Reverse-drift backlog 31→30. **Adoption: 26/74 modules opted into strict enforcement**. No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. §00 v2.57.0→**v2.58.0**; §98 v2.57.0→**v2.58.0**.

> **v2.54.0 update (Phase P27 — H10 fifth reverse-drift reconstruction, `spec/24-app-design-system-and-ui` — second P25 dual-stream subcase):** User reply `next`. Fifth reverse-drift target. **Second occurrence of P25 subcase** (clean ladder + decoupled header stream): §98 SemVer ladder already contained `4.1.0` (matching §00 banner) but §98 header tracked independent audit-stream `3.1.1` (last bumped by Phase P14 STALE-disposition audit). Reconstruction: §98 header `3.1.1`→**`4.1.1`** with reconciliation row citing P25 precedent; ladder body untouched; §00 `<!-- h10-verified-phase: 27 -->` stamp added; §00 Updated 2026-04-27→2026-04-28; §99 module 2.1.1→**2.2.0**. **P27 lesson — P25 subcase reproducibility confirmed**: second application ran in <5 tool calls. Detection signal: §98 ladder latest = §00 banner while §98 header lags. Future drifters matching this exact signal SHOULD be batched into a single sweep phase. **Gate impact**: matches=41→42, mismatches=33→32, stamped=24→25, stamped_failed=0. Reverse-drift backlog 32→31. **Adoption: 25/74 modules opted into strict enforcement — crossed 1/3 of tree.** No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. §00 v2.56.0→**v2.57.0**; §98 v2.56.0→**v2.57.0**.

> **v2.53.0 update (Phase P26 — H10 fourth reverse-drift reconstruction, `spec/23-app-database` — hybrid P23+P24 subcase):** User reply `next`. Fourth application of P23 workflow. **First hybrid subcase**: §98 had THREE simultaneous structural problems — (1) stray out-of-order `4.1.0` row after `3.0.1` describing Phase 53 (mis-numbered); (2) orphan post-footer prose (Phase 58/68/71); (3) §00 banner at `4.0.0` with no backing §98 row. Reconstruction: renumbered Phase 53 row → **3.1.0** (with explicit row-body explanation of why `4.1.0` was wrong); promoted post-footer orphans into **3.2.0** (Phase 58 JSON Schema), **3.3.0** (Phase 68 Mermaid), **4.0.0** (Phase 71 inlined CI workflow — major); added **4.0.1** P26 reorganization release. §00 banner `4.0.0`→`4.0.1`; Updated 2026-04-27→2026-04-28; `<!-- h10-verified-phase: 26 -->` stamp dropped. §99 already at 2026-04-28. **P26 lesson — hybrid subcase (d) codified**: when §98 has out-of-order rows mixed with post-footer orphans, apply P23-renumber + P24-promote-and-delete in sequence within one phase. **Renumber-with-explanation rule (codified)**: stray-numbered rows are NEVER renamed in-place silently — the renumbered row body MUST explain why the original number was wrong (protects audit trail; deletion would lose original-author intent). Updated subcase taxonomy: (a) P23 prose-promote, (b) P24 post-footer-promote-and-delete, (c) P25 dual-stream-align, (d) P26 hybrid. **Gate impact**: matches=40→41, mismatches=34→33, stamped=23→24, stamped_failed=0. Reverse-drift backlog 33→32. Adoption: **24/74 modules opted into strict enforcement**. No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. Verified: parity 87/74/41/33/24/0 ✅; lockstep 87/87/0 ✅ (clean on first verify); tree-health 168/168 strict ✅; §27-inventory 6/6 ✅; H10 self-test 13/13 ✅. §00 v2.55.0→**v2.56.0**; §98 v2.55.0→**v2.56.0**.

> **v2.52.0 update (Phase P25 — H10 third reverse-drift reconstruction, `spec/17-consolidated-guidelines`):** User reply `next`. Third application of P23 workflow on `spec/17-consolidated-guidelines` (§00 `3.3.0` vs §98 latest `2.5.0`). **Different shape from P23 + P24**: §98 had NO orphan post-footer prose to promote — already a clean 7-row release ladder (1.0.0 → 2.5.0). Reverse drift came from a **decoupled-version-stream pattern**: §98 v2.4.0 recorded "Bumped overview banner v3.2.0 → v3.3.0" while §98 itself stayed at 2.4.0 and continued numbering 2.5.0. Reconciliation: added ONE new §98 release row at version **3.3.0** (matching §00 banner) explaining the alignment; preserved historical 1.0.0 → 2.5.0 rows verbatim. Codified versioning rule: §98 latest = §00 banner; streams locked together going forward. §00 Updated 2026-04-27→2026-04-28; `<!-- h10-verified-phase: 25 -->` stamp dropped. §99 module 4.4.0→4.5.0. **P25 lesson — third reverse-drift subcase codified (dual-stream alignment)**: future reverse-drift phases MUST classify into one of (a) P23 subcase — orphan prose in §98/§99 → promote; (b) P24 subcase — orphan prose after §98 Cross-References footer → promote AND delete orphans; (c) P25 subcase — clean §98 ladder but decoupled streams → add ONE alignment release row, preserve history, lock streams. Subcase identification is the first step of any reverse-drift phase. **Gate impact**: matches=39→40, mismatches=35→34, stamped=22→23, stamped_failed=0. Reverse-drift backlog 34→33. Adoption: **23/74 modules opted into strict enforcement**. No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. Verified: parity 87/74/40/34/23/0 ✅; lockstep 87/87/0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅; H10 self-test 13/13 ✅. §00 v2.54.0→**v2.55.0**; §98 v2.54.0→**v2.55.0**.

> **v2.51.0 update (Phase P24 — H10 second reverse-drift reconstruction, `spec/14-update`):** User reply `next`. Second application of P23 workflow on next-smallest-gap drifter (§00 `2.1.0` vs §98 latest `1.3.0`). Same shape as P23 (dated prose appended after Cross-References footer): promoted Phase 63 (TS enum mirror) → **1.4.0**, Phase 76 (Mermaid + SQL DDL) → **2.0.0** (major: two new normative artifact surfaces), P24 reconciliation → **2.1.0**. Each row cites source prose; original blocks removed from post-footer to single-source audit trail. §00 Updated 2026-04-27→2026-04-28; `<!-- h10-verified-phase: 24 -->` stamp dropped. §99 module 1.2.0→1.3.0. **Workflow-consistency check vs P23**: identical 5-step workflow applied — confirms P23-codified workflow scales as deterministic template; second application faster than first because pattern was pre-known. **Gate impact**: matches=38→39, mismatches=36→35, stamped=21→22, stamped_failed=0. Reverse-drift backlog 35→34. Adoption: **22/74 modules opted into strict enforcement**. No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. Verified: parity 87/74/39/35/22/0 ✅; lockstep 87/87/0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅; H10 self-test 13/13 ✅. §00 v2.53.0→**v2.54.0**; §98 v2.53.0→**v2.54.0**.

> **v2.50.0 update (Phase P23 — H10 first reverse-drift reconstruction, `02-coding-guidelines/11-security/01-axios-version-control`):** User reply `next` (P22 close-out queued task #2). Smallest-gap reverse drifter (§00 `3.2.0` vs §98 latest `1.1.0`). Reconstructed §98 SemVer ladder from existing dated prose: 1.0.0→1.2.0(Phase 27c frontmatter)→1.3.0(Phase 53 typed/SQL/JSON Schema)→2.0.0(Phase 70 Mermaid+CI — major: new normative CI surface)→3.0.0(Phase 75 Go/Rust/C# stubs — major: new cross-language normative shape)→3.1.0(Phase 75 TS enum mirror)→3.2.0(P23 reconciliation). Each row cites source prose — audit trail preserved. §00 Updated 2026-04-27→2026-04-28; `<!-- h10-verified-phase: 23 -->` stamp dropped. §99 module 3.3.0→3.4.0. **Workflow codified**: (1) ls module for dated prose in §98/§99; (2) map phases to SemVer bumps (major=new normative surface; minor=additive); (3) reconstructed §98 rows MUST cite source prose; (4) drop H10 stamp; (5) bump §99. Mechanical bumps without reconstruction forbidden — erases audit trail. **Gate impact**: matches=37→38, mismatches=37→36, stamped=20→21, stamped_failed=0. Reverse-drift backlog 36→35. Adoption: **21/74 strict-enforcement**. No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change. Verified: parity 87/74/38/36/21/0 ✅; lockstep 87/87/0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅; H10 self-test 13/13 ✅. §00 v2.52.0→**v2.53.0**; §98 v2.52.0→**v2.53.0**.

> **v2.49.0 update (Phase P22 — H10 forward-drift backlog cleared tree-wide):** User reply `next` (P21 close-out queued task #1 continuation). Continued the P21 stamp-adoption sweep with the same idempotent mechanism (`/tmp/p22-sweep.py`) against the **remaining 10 forward-drift modules tree-wide**, closing the entire forward-drift backlog. **Modules**: `spec` (root, 3.5.0→3.5.1), `01-spec-authoring-guide` (3.7.0→4.13.0 — largest single catch-up, 1 major + 6 minor releases of banner lag), `03-error-manage` (3.2→3.4), `05-split-db-architecture` (3.0→4.0), `06-seedable-config-architecture` (3.1→4.1), `13-generic-cli` (1.0→1.1), `16-generic-release` (1.1→2.2), `28-universal-ci-cli` (1.1→2.1.1), `14-update/24-update-check-mechanism` (1.0→2.1), `03-error-manage/03-error-code-registry/08-linter-scripts` (1.2→1.3). Each: §00 banner bumped to §98 latest + `<!-- h10-verified-phase: 22 -->` stamp + `**P22 sync** ...` audit-trail bullet under existing latest §98 release row (no new §98 release). **Gate impact**: `matches=27→37`, `mismatches=47→37`, `stamped=10→20`, `stamped_failed=0`. **Forward-drift backlog: 0/0 ✅**. The remaining 37 mismatches are now all reverse-drift (§00 > §98) — per-file forensic backlog deferred per drifter. **Adoption: 20/74 modules opted into strict enforcement** (P21 brought 10, P22 brings the other 10 sweepable). **P22 codified as the terminal forward-drift sweep** — bulk sweeps stop at the forward-drift boundary because reverse drift requires per-module forensic reconstruction of what change justified the §00 bump (banner-level mechanical bump would lose audit trail). **No CI workflow change, no `RUBRIC_VERSION` bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change** — pure consumer-side adoption. **Verified**: parity gate 87/74/37/37/stamped=20/stamped_failed=0 / exit 0 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅ (all 10 touched modules retained full marks); §27-inventory 6/6 ✅; H10 self-test 13/13 ✅. §00 v2.51.0 → **v2.52.0**; §98 v2.51.0 → **v2.52.0**.

> **v2.48.0 update (Phase P21 — H10 first stamp-adoption sweep, `02-coding-guidelines/*` forward-drift cluster):** User reply `next` (P20 close-out queued task #1). First field application of the P20 stamp pattern. Swept **10 of 23** `02-coding-guidelines` cluster drifters (the forward-drift subset where §00 banner < §98 latest release): bumped each `00-overview.md` `**Version:**` banner to match its §98 latest, dropped `<!-- h10-verified-phase: 21 -->` under the banner, prepended `**P21 sync** ...` audit-trail bullet under the existing latest §98 release row (no new §98 release — content unchanged, only the banner caught up). **Modules**: `01-cross-language` (3.2→4.1), `02-typescript` (3.3→4.1), `03-golang` (3.2→4.1), `04-php` (3.3→4.2), `05-rust` (3.2→4.0), `06-ai-optimization` (3.2→4.0), `06-cicd-integration` (1.0→4.0), `07-csharp` (3.2→4.1), `11-security` (2.2→2.3), `01-cross-language/16-static-analysis` (3.2→4.1). **Forward vs reverse drift split codified as P21 lesson**: forward drift (§00 < §98) is bulk-mechanically-safe; reverse drift (§00 > §98, the other 13 cluster drifters) needs per-module §98 reconstruction work and is deferred per drifter. **Sweep mechanics**: single deterministic Python pass via `/tmp/p21-sweep.py`; idempotent regex bumps + idempotent stamp/bullet inserts (re-runnable without duplication). **Gate impact**: `matches=17 → 27` (+10); `mismatches=57 → 47` (-10); `stamped=0 → 10`; `stamped_failed=0` (gate proves all 10 stamps valid → per-file strict locked in for the cluster; future drift on these 10 files FAILs the gate even in default advisory mode). **Adoption progress**: 10 / 74 modules now opted into strict enforcement, no CI gate change required. **No CI workflow change, no `RUBRIC_VERSION` bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline, no §27 slot version change** — pure consumer-side adoption of the existing P20 mechanism; §27's role is purely audit-trail. **Verified**: parity gate 87/74/27/47/stamped=10/stamped_failed=0 / exit 0 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅ (all 10 touched modules retained full marks); §27-inventory 6/6 ✅; self-test 13/13 ✅. §00 v2.50.0 → **v2.51.0**; §98 v2.50.0 → **v2.51.0**.

> **v2.47.0 update (Phase P20 — H10 per-file `<!-- h10-verified-phase: NNN -->` opt-in stamp pattern; per-file strict-promotion path opens):** User reply `next` (P19 close-out queued task #1 — toolchain enhancement to enable incremental H10 strict adoption). Extended `linter-scripts/check-version-parity.py` (P15 / H10 advisory gate) with the per-file opt-in stamp under §00 mirroring H1 / `check-99-summary-freshness.py`'s `<!-- verified-phase: NNN -->` pattern. **What changed**: when a stamped §00 has a §00↔§98 mismatch, the gate now exits 1 even in default tree-wide-advisory mode (per-file strict promotion); summary line gains `stamped=N; stamped_failed=N` tokens; per-file lines tagged `(FAIL)` with `[stamped phase NNN]` suffix when stamp present; `--json` schema gains top-level `stamped` + `stamped_failed` + per-entry `stamped: <int|null>`. `--report-only` remains the strongest escape hatch. **Spec slot 29 v1.0.0 → v1.1.0**: new `## Per-file opt-in stamp (Phase P20)` section + updated CLI/exit-codes/output-shape tables + AC-29-09 (json schema) and AC-29-10 (line shape token count 6→8) extended + 3 new ACs **AC-29-11/12/13** (stamped+drift FAILs default, stamped+match passes & counts both, `--report-only` overrides per-file stamp). **§97 v2.4.0 → v2.5.0**: AC-T-26 extended with the per-file lifecycle clause and new "P20 lesson" verification clause; AC count unchanged at 26 (the stamp is an extension of the existing H10 contract, not a new module-level rule — per-file lifecycle ACs live in slot 29). **Self-test 10/10 → 13/13** ✅: T11 stamped+drift fails default; T12 stamped+match passes; T13 `--report-only` overrides stamp failure; T9 updated for new JSON keys; `mk_module()` helper grew optional 5th `stamp` param. **Bug fix landed alongside**: `--spec-root` pointing outside repo (used by sandboxes in `/tmp`) was crashing on `relative_to(ROOT)` — now falls back to absolute path. **Adoption plan**: today `stamped=0/74`; goal `74/74`; at that point flip `--strict` tree-wide and the gate becomes a hard CI block. The `stamped=N` counter visible in every CI run signals progress. **No CI workflow change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no gate-count change, no trace-map rebaseline** — gate stays advisory tree-wide; the new strict promotion is per-file and triggers only on stamps; adoption proceeds PR-by-PR. **Verified**: `bash linter-scripts/test/test-check-version-parity.sh` 13/13 ✅; default-mode real-tree run 87/74/17/57/0/0 / exit 0 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅. §00 v2.49.0 → **v2.50.0**; §98 v2.49.0 → **v2.50.0**; §97 v2.4.0 → **v2.5.0**.

> **v2.46.0 update (Phase P15 / H10 — §00 ↔ §98 Version-field parity gate landed; gate #19; AC-31-31 cascade RUBRIC v2.27 → v2.28):** User reply `next`. Closed the H10 candidate filed in Phase 21's disposition note. New validator `linter-scripts/check-version-parity.py` (advisory-by-default), spec slot `29-check-version-parity.md` v1.0.0 (10 ACs AC-29-01..10), self-test `test/test-check-version-parity.sh` (10/10 ✅), CI step `Version-field parity gate (Phase P15 / H10)` after the §99 freshness gate. **Surface re-validation**: Phase 21 estimated "1/3 historical incident, low surface" — the P15 baseline sweep over the real tree found **59 / 74 eligible modules drifting** (~60× under-count). New lesson codified in AC-T-26: audit retrospective surface estimates are unreliable until validated by a mechanical sweep. **Advisory-by-default per AC-T-25 dispensation** documented in slot-29's "Why advisory-by-default" subsection (flipping `--strict` would block 59 unrelated PRs; phased rollout planned mirroring H1 → H8 stamp adoption). **AC-31-31 cascade**: RUBRIC v2.27 → **v2.28**; gate count 18 → **19**; footer / EXECUTIVE-SUMMARY / qa-baseline-footer awk all updated; §27 §00-overview slot-70 description "18" → "19 production gates" + new slot-29 inventory row + Version v2.48.0 → **v2.49.0**; new **AC-T-26** in §97 (v2.3.0 → v2.4.0). Parity 19/19/19. **H1 workflow-step-parity lesson applied**: self-test collapsed into the gate's step (no standalone step) since it exclusively exercises one numbered footer gate. **Verified**: validator 87 scanned / 74 eligible / 15 matches / 59 mismatches / exit 0 ✅; `--strict` exit 1 ✅; `--report-only` override exit 0 ✅; `--json` schema valid ✅; self-test 10/10 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅ (slot 29 specced). §98 v2.48.0 → **v2.49.0**.

> **v2.45.0 update (Phase F4 — bind Phase 144 `.py` regression test to AC-62-05; spec-vs-code gap closed):** User reply `spec/27-spec-toolchain/ — you can touch this folder also`. Closed the spec-vs-code gap discovered while auditing `linter-scripts/test/` self-test coverage: `linter-scripts/test/test-check-spec-folder-refs.py` (Phase 144 sanctioned `.py` exception locking AC-62-04) existed on disk + in `linter-scripts/test/README.md` "Adjacent `.py` tests" subsection but was **unreferenced from any §27 slot** — a contributor reading the spec alone could not discover that AC-62-04 is regression-test-locked. Slot 62 (`62-spec-folder-refs-allowlist.md`) v1.2.0 → **v1.3.0**: added **AC-62-05** codifying the test's four-case contract (plain-entry routing, AC-62-04 inline-comment strip, whitespace-then-comment edge case, `[doc-only]` bucket isolation); extended AC-62-04's `**Verifies:**` clause with back-reference to test + AC-62-05; added test to slot's `## Cross-references`. **No new §27 slot for the test itself** — `linter-scripts/test/` is excluded by design from `test-overview-inventory-parity.sh`'s scope rule (production scripts only); AC-62-05 explicitly documents this so future contributors don't try to "fix" it by carving a new slot. **No CI gate count change, no RUBRIC_VERSION bump, no AC-31-31 cascade, no trace-map rebaseline** — pure spec lockstep within an existing AC-62-04→test linkage. **Verified**: `python3 linter-scripts/test/test-check-spec-folder-refs.py` ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅. §98 v2.47.0 → **v2.48.0** (minor); §00 v2.47.0 → **v2.48.0** (minor — synced).

> **v2.44.0 update (Phase 30 — Spec-index drift gate strict-promotion; gate #18; AC-31-31 cascade RUBRIC v2.26→v2.27):** User reply `next` (queued backlog item: queued by Phase 29 close-out as the formal cascade follow-up). Promoted the existing `Regenerate spec-index.md (drift check)` workflow step from advisory (`⚠️` warn + exit 0) to **strict** (`exit 1` on any `git status --porcelain spec/` delta after `node linter-scripts/generate-spec-index.cjs`). Step renamed to `Spec-index drift gate` for naming consistency with F2/H7 peers. Root cause (Phase 29 audit): the advisory gate had let 6 files / 18 lines / 12 stale version entries silently accumulate across phases 145–28. **AC-31-31 cascade**: RUBRIC v2.26→**v2.27**; gate count 17→**18**; footer/EXECUTIVE-SUMMARY/qa-baseline-footer awk all updated; §27 §00-overview slot-70 description "17 production gates"→"18"; new **AC-T-25** in §97. Parity 18/18/18. **No new file**: workflow-only promotion + step rename; `test-overview-inventory-parity.sh` unaffected (6/6 still pass). **No trace-map delta**: regenerator already trace-bound at slot 11. **Verified**: spec-index regen 0 delta ✅; QA-baseline-footer 11/11 ✅ (18/18/18); workflow-gates=18 ✅; §27-inventory 6/6 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅. §98 v2.46.3 → **v2.47.0** (minor). Memo: `.lovable/memory/audit/v2-deterministic/phase-30-spec-index-strict-promotion.md`.

> **v2.43.3 update (Phase 21 — §00-overview stale-prose sweep; banner + slot-70 description):** User reply `next` (queued backlog item: audit other top-level READMEs for the narrative-count drift class identified in Phase 20). Synced §00-overview banner from stale `v1.7.0 / 2026-04-27` → current `v2.46.3 / 2026-04-28` (~39 patch releases behind §98). Lockstep gate did not catch it because its L1 rule is "§98 latest release date >= §00 Updated" (date relation), not version-string parity. Replaced stale slot-70 description "Wires §05 + §10 into GitHub Actions (event-driven)" with accurate "Per-PR/push spec-health pipeline: 17 production gates + 7 discrete self-test steps". Inventory table itself was structurally correct; only narrative descriptions had drifted. **No new gate, no AC-31-31 cascade, no rubric bump** — pure narrative lockstep, gate count remains 17/17/17, RUBRIC remains v2.26. **Lesson candidate codified**: §00 Version-field drift relative to §98 is invisible to lockstep when date relation is technically OK — possible H10 candidate is "Version-field parity" advisory (currently 1/3, single historical incident). **Verified**: lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅. §98 v2.46.2 → **v2.46.3** (patch). Memo: `.lovable/memory/audit/v2-deterministic/phase-21-overview-stale-prose-sweep.md`.

> **v2.43.2 update (Phase H9 — stamp-position structural enforcement; slot 26 v1.2.0 → v1.3.0):** User reply `next` (queued backlog item #6: promote H8 stamp-position rule to lint check). Promoted the H8 precedent ("stamps MUST live INSIDE a tracked-heading body, not adjacent") from a one-time sweep + memory rule to a structural lint inside the existing slot-26 gate. New `find_misplaced_stamps()` flags `<!-- verified-phase: NNN -->` placed OUTSIDE any tracked-heading body AND immediately ABOVE a tracked heading (≤3 non-empty lines). Two-mode contract: default = advisory warning (exit 0); new `--strict-position` flag = exit 1. **CI wired strict immediately** because real-tree had 0 misplaced findings post-H8 — locks the H8 gain with no migration window. **Adjacency-only rule** (deliberately narrower than "any stamp outside any tracked body"): §27's own §99 legitimately references past stamps inside Validation History blockquotes (lines 6, 24); a whole-file rule would emit false positives on documentation, while the adjacency rule catches only the actual H8 failure mode (`stamp → blank → ## Summary`). Codified in AC-26-10's negative case (T14 self-test). Self-test extended 20 → **27 assertions**. **No AC-31-31 cascade, no rubric bump, no new gate** — same slot-26 gate, tighter contract; gate count remains **17/17/17**, RUBRIC_VERSION remains **v2.26**. **Verified**: freshness `--strict-position` ✅ (87 scanned, 81 stamped, 6 exempt, 0 unstamped, 0 stale, 0 misplaced); self-test 27/27 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅. §98 v2.46.1 → **v2.46.2** (patch). Memo: `.lovable/memory/audit/v2-deterministic/phase-h9-stamp-position-enforcement.md`.

> **v2.43.1 update (Phase H8 — §99 freshness coverage closure; 12 unstamped → 0; exempt-marker convention):** User reply `next` (queued backlog item #4: stamp-bump retroactive opt-in / formally classify the 12 audit-log-style §99s as exempt). Drove §99 freshness coverage from 75 stamped + 12 unstamped → **81 stamped + 6 exempt + 0 unstamped** (87/87 declared posture). Validator gains `EXEMPT_RE` recognizing `<!-- freshness-exempt: <reason> -->` anywhere in file (not heading-scoped); exempt files counted separately and skipped before the find-stamp pass. New canonical reason `audit-log-only` applied to 6 files with no `## Summary` and no inventory rubric — only date-anchored audit-log subsections (research-index, app-coding-rules, app-issue-templates, app-database-conventions, app-ui-conventions, update/diagram-conventions). **Stamp-position fix**: 5 files (`05/02-features`, `06/02-features`, `12/01-browser-extension-deploy`, `12/02-go-binary-deploy`, `18/02-enums-and-coding-style`) carried the stamp on the blank line BEFORE `## Summary` rather than under it — heading-body scanner correctly rejected. Repositioned all 5; **precedent codified**: stamps MUST live inside a tracked-heading body, not adjacent. **§27's own §99**: stamp tokens were only inside Validation History blockquotes; added `<!-- verified-phase: 147 -->` under `## File Inventory`. Self-test extended 12 → **20 assertions** (T1 counts-line shape now requires `exempt:` field; new T11 verifies exempt-marker honored). **No AC-31-31 cascade, no rubric bump, no new gate** — coverage closure + convention extension within slot-26's existing contract; gate count remains **17/17/17**, RUBRIC_VERSION remains v2.26. **Verified**: freshness ✅ (87 scanned, 81 stamped, 6 exempt, 0 unstamped, 0 stale); self-test 20/20 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; §27-inventory 6/6 ✅. §98 v2.46.0 → **v2.46.1** (patch). Memo: `.lovable/memory/audit/v2-deterministic/phase-h8-freshness-coverage-closure.md`.

> **v2.43.0 update (Phase H7 — runtime archive-exclusion gate; slot 28 v1.0.0; gate #17; RUBRIC v2.25→v2.26):** User reply `next` (queued backlog item #5: promote H6 runtime-probe pattern into a standing self-test). New `linter-scripts/test/test-archive-exclusion-runtime.sh` importlib-loads 3 critical spec-traversing linters and asserts 0 archive-leaked enumerator results — codifies H6 lesson "runtime > source verification" as a CI gate so future refactors cannot silently regress the H3 `_archive` exclusion convention. Wired as single workflow step `Runtime archive-exclusion gate (Phase H7)`. AC-31-31 cascade: RUBRIC v2.25→**v2.26**; gate count 16→**17**; footer/EXECUTIVE-SUMMARY/qa-baseline-footer awk all updated; parity 17/17/17. Trace-map rebaseline: `{ac_total:1320 (+5), ac_traced:90 (+5), code_total:51 (+1), code_orphan:26}`. **Sanctioned F3 exception**: the file IS a `.sh` self-test but occupies §27 slot 28 as a validator because that is its functional CI role; future contributors MUST NOT invent a thin `.py` wrapper just to fit the "validator = `.py`" pattern. **Verified**: H7 self-test 10/10 ✅; QA-baseline-footer 11/11 ✅ (17/17/17); README 26/26 ✅; §27 inventory 6/6 ✅; lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; audit 98.0/99.8 ✅; trace-map ✅; freshness 75 stamped / 0 stale ✅. Memo: `.lovable/memory/audit/v2-deterministic/phase-h7-runtime-probe-gate.md`. §98 v2.45.0 → **v2.46.0** (minor).

> **v2.42.0 update (Phase H5 — §99 stamp-bump gate CI wiring; AC-31-31 cascade gate 15→16; RUBRIC v2.24→v2.25):** User reply `next` (continuation of H4: H4 shipped tool, H5 ships enforcement). Wired Phase H4's `check-99-stamp-bump.py` + self-test into `.github/workflows/spec-health.yml` as the **16th strict CI gate**. Single collapsed workflow step `§99 Stamp-bump gate (Phase H5)` runs self-test always + validator only on `pull_request` events (no PR base SHA on push-to-main → script would exit 2). Per H1 workflow-step parity lesson: single-gate self-tests collapse into the parent step (footer rows = workflow steps = declared count = **16/16/16**). Bumped checkout `fetch-depth: 0` so PR base SHA resolves for `git diff`. AC-31-31 cascade: `RUBRIC_VERSION` v2.24 → **v2.25**; QA-tooling-baseline footer adds entry #16; `EXECUTIVE-SUMMARY.md` cross-ref 15→16; `test/test-qa-baseline-footer.sh` awk pattern gains `/§99 Stamp-bump gate/`. **Verified**: H5 self-test 23/23 ✅; freshness 75 stamped / 0 stale ✅; QA-baseline-footer 11/11 ✅ (Workflow gates: 16, Footer rows: 16, Declared: 16); lockstep 87/87 / 0 ✅; tree-health 168/168 strict ✅; audit mean 98.0/99.8 ✅; trace-map regression ✅ at unchanged baseline `{ac_total:1315, ac_traced:85, code_total:50, code_orphan:26}`. Memo: `.lovable/memory/audit/v2-deterministic/phase-h5-stamp-bump-ci-wiring.md`. §98 v2.44.0 → **v2.45.0** (minor).

> **v2.40.1 update (Phase H3 — `_archive/` exclusion sweep across spec-traversing linters):** User reply `next` (autonomous queued backlog item: "Apply `_archive/` exclusion retroactively"). Audited all 12 spec-traversing linters: 8 already excluding correctly, 2 needed fixes — `check-lockstep.cjs` (added `e.name !== '_archive'` to walker; `Modules scanned: 90 → 87`, `skip: 3 → 0` — clearer signal, no behavior change), `generate-trace-map.py` (defensive `_archive` filter in `collect_ac_ids()`; archive ACs were already h4-invisible to the `### `-only regex, but codified). Trace-map regression gate caught +3 new ACs from Phase H2 (AC-26-06/07/08); bound all 3 in trace-map.toml to slot-26 gate symbols, then rebaselined within memory's safety thresholds (+3 ACs ≪ 50, +0 code files ≪ 5). New baseline `{ac_total:1307, ac_traced:77 (+3), code_total:48, code_orphan:25}`; ac_drifted unchanged at 1230. AC-31-31 invariants: gate count unchanged at **15**, RUBRIC_VERSION unchanged at **v2.24**. All gates green: lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓, freshness 75 stamped / 0 stale ✓, trace-map regression ✓ at new baseline, self-tests 17/17 + 6/6 + 22/22 + 11/11 ✓. §98 v2.43.0 → **v2.43.1** (patch). Memo: `.lovable/memory/audit/v2-deterministic/phase-h3-archive-exclusion-sweep.md`.

> **v2.40.0 update (Phase H2 — §99 freshness gate widened to inventory rubrics; slot 26 v1.0.0 → v1.1.0; coverage 46/89 → 75/87 stamped; +29):** User reply `next` (autonomous queued backlog item: "Optional inventory-rubric freshness gate v2"). Extended `check-99-summary-freshness.py` from `## Summary`-only scanning to also accept `## Module Health` / `## File Inventory` / `## Module Inventory` / `## Top-Level Modules` / `## Document Inventory` / `## Modules`. Multi-block scan finds the highest stamp under any tracked heading (fixes H1 first-match bug where File Inventory before Summary masked the Summary stamp). Excluded `spec/_archive/**` from scan (frozen modules → false freshness signals). Stamped 29 previously-exempt inventory-rubric files; trust chain = tree-health 168/168 strict already validates inventory-table accuracy. Coverage 46/89 → **75/87** (~86%); 12 audit-log-style files structurally exempt; 0 eligible-but-unstamped remaining. Self-test 11 → **17 assertions** (+T8/T9/T10 for Module Health / File Inventory / _archive exclusion). Spec doc +3 ACs (AC-26-06/07/08). AC-31-31 invariants: gate count unchanged at **15** (no new gate); RUBRIC_VERSION unchanged at **v2.24** (no rubric semantics). Lockstep impact NONE on 29 stamped files. All gates green: gate ✓ (75 stamped, 0 stale), self-test 17/17 ✓, lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓, §27-inventory 6/6 ✓. §98 v2.42.6 → **v2.43.0** (minor — scope widened). Memo: `.lovable/memory/audit/v2-deterministic/phase-h2-freshness-gate-inventory-rubric.md`.

> **v2.39.6 update (Phase H1-S6 FINAL — §99 Summary stamp adoption batch 6; 5 modules stamped; H1 series CLOSED):** User reply `next`. Final residual sweep of all 40 remaining unstamped §99 files. Discovered **35 of 40 lack a `## Summary` heading entirely** — they follow inventory-only rubric (`## File Inventory` / `## Module Inventory`) and are structurally exempt from the freshness gate by design. Stamped the remaining 5 Summary-bearing files: `spec/05/02-features/99`, `spec/06/02-features/99`, `spec/12/01-browser-extension-deploy/99`, `spec/12/02-go-binary-deploy/99`, `spec/18/02-enums-and-coding-style/99` — all materially-fresh. **Final coverage: 46/89 §99 files stamped — 100% of the 46 trackable Summary-bearing corpus.** 0 eligible-but-unstamped remaining. **Final cumulative stale-discovery rate: 3/46 ≈ 6.5%** — bimodal distribution confirmed (well-maintained nested clusters 0-5%, older one-offs 50-100%, steady-state ~5-10%). Phase H1 series CLOSED. Lockstep impact: NONE. All gates green: freshness ✓ (46 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓. §98 v2.42.5 → **v2.42.6**. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-s6-summary-stamp-adoption-batch-6-CLOSED.md`.

> **v2.39.5 update (Phase H1-S5 — §99 Summary stamp adoption batch 5; folder 03 cluster; 19 modules; coverage 27/89 → 46/89; 1 stale freshen):** User reply `next`. Audited 19 unstamped §99 files under `spec/03-error-manage/`. 18 materially-fresh; 1 stale (`02-error-architecture/99` claimed Health 98/100 + warning re: 3 missing modal-subfolder §99 files — all 4 actually shipped, freshened+stamped per H1-S1 rule). 4 with non-empty Observations all factual (redirect-stub parents + deprecated-frozen `02-react-components.md` superseded by subfolder v4.0.0). **Cumulative stale-discovery rate H1-S1+S2+S3+S4+S5 = 3/46 ≈ 6.5%** (down from S4's 7.4%); bimodal holds — folder 02 (0/15) and folder 03 (1/19 = 5%) at ~0-5%; older one-off modules (root spec/99, spec/15) at ~50-100%; steady-state ~5-10%. Stamp coverage 27/89 → **46/89** (~52% all §99, ~94% of 49 trackable). Gate remains advisory (43 unstamped → exit 0). Lockstep impact: NONE on 19 stamped files (content-only). All gates green: freshness ✓ (46 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓. §98 v2.42.4 → **v2.42.5**. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-s5-summary-stamp-adoption-batch-5.md`.

> **v2.39.4 update (Phase H1-S4 — §99 Summary stamp adoption batch 4; folder 02 cluster; 15 modules; coverage 12/89 → 27/89):** User reply `next`. Audited all 15 Summary-bearing §99 files under `spec/02-coding-guidelines/`. All 15 materially-fresh (Errors:0 / Warnings:0 / Health 100/100 A+ aligned with current tree-health 168/168 + rubric v2.24); 2 with non-empty Observations factual (`04-code-style` split history; `04-php` intentional numbering gaps). Stamp-only across all 15. **Cumulative stale-discovery rate H1-S1+S2+S3+S4 = 2/27 ≈ 7.4%** (down from S3's 17%) — folder 02 was systematically deepened in Phases 27a-c and remains well-maintained, zero stale finds. Updated projection: well-maintained clusters approach 0%; older one-off modules carry the 17% rate; ~7-10% steady-state expected. Stamp coverage 12/89 → **27/89** (~31% all §99, ~55% trackable). Gate remains advisory (62 unstamped → exit 0). Lockstep impact: NONE on 15 stamped files (content-only). All gates green: freshness ✓ (27 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓. §98 v2.42.3 → **v2.42.4**. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-s4-summary-stamp-adoption-batch-4.md`.

> **v2.39.3 update (Phase H1-S3 — §99 Summary stamp adoption batch 3; 6 modules; coverage 6/89 → 12/89; 1 stale-prose freshen):** User reply `next`. Audited 6 candidates: `spec/06/99` + `spec/10/99` + `spec/11/99` + `spec/12/99` + `spec/16/99` confirmed materially-fresh (Health 100/100 A+ aligns with rubric v2.24 + tree-health 168/168 strict); stamp-only. **`spec/15/99` was materially stale** — claimed Health 75/100 (C) + 2 warnings (`97-acceptance-test-plan.md` missing, pipeline gaps) that no longer hold (canonical name is `97-acceptance-criteria.md` and is present; module at full strict-rubric marks). Freshened prose + stamped per H1-S1 rule. **Stale-discovery rate over batches H1-S1+S2+S3**: 2/12 = ~17% — validates H1's hypothesis that opportunistic stamping with mandatory pre-audit catches real drift Phase 136/139 caught manually. Stamp coverage 6/89 → **12/89**; gate remains advisory (77 unstamped → exit 0). Lockstep impact: NONE on 6 stamped files (content-only). All gates green: freshness ✓ (12 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓. §98 v2.42.2 → **v2.42.3**. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-s3-summary-stamp-adoption-batch-3.md`.

> **v2.39.2 update (Phase H1-S2 — §99 Summary stamp adoption batch 2; 5 modules; coverage 1/89 → 6/89):** User reply `next`. Audited 6 candidates: `spec/02/99` skipped (no `## Summary` heading — gate doesn't track), `spec/03/99` + `spec/05/99` + `spec/13/99` + `spec/14/99` + `spec/17/99` all confirmed materially-fresh on read (Errors:0 / Warnings:0 / Health 100/100 A+ — aligns with current tree-health 168/168 strict + rubric v2.24). No prose freshening required for any of the 5 (Summaries minimal, accurate, version-anchor-free). Stamp `<!-- verified-phase: 146 -->` inserted directly under `## Summary` heading per H1 contract on all 5. **Method validation**: H1-S1 rule held — read end-to-end + reconcile vs §97/§00/source-of-truth applies uniformly; the audit step is the same; the resulting edit differs (H1-S1 rewrote stale prose first; H1-S2 found 5 already-clean and stamped only). Stamp coverage **1/89 → 6/89**; gate remains advisory project-wide (83 unstamped → exit 0). Lockstep impact: NONE — stamp insertion is content-only, does NOT change §99.Updated dates or trip L0/L1/L2 invariants. NO §00 / §97 / §31 / AC-31-31 / rubric / trace-map / baseline implications. NO §98/§99 banner bumps on stamped files (content-only). All gates green: freshness ✓ (6 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓. §98 v2.42.1 → **v2.42.2**. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-s2-summary-stamp-adoption-batch-2.md`.

> **v2.39.1 update (Phase H1-S1 — §99 Summary stamp adoption batch 1; root `spec/99` stamped + freshened):** User reply `next`. First opt-in adoption of the `<!-- verified-phase: NNN -->` stamp. Target: root `spec/99-consistency-report.md`. Pre-stamp audit found the Summary materially stale (claimed Health 78/100 B + Warnings:3 from Phase 1 Triage era; current truth is tree-health 168/168 strict, lockstep 87/87 / 0, rubric v2.24, 15 strict CI gates, trace-map ac_traced 74 / code_orphan 25). **Rule applied & codified for batch 2+**: never stamp a §99 Summary block without first reading end-to-end and reconciling against §97 / §00 / source-of-truth — stamping stale prose to silence the gate is exactly the failure mode H1 was designed to prevent. Freshened prose + stamped in one edit. Stamp coverage 0/89 → **1/89**; gate remains advisory (88 unstamped → exit 0). NO script source touched, NO new specs/tests, NO §97 / §00 / §31 / AC-31-31 / rubric / trace-map / baseline implications. Patch-level bump only. All gates green: freshness gate ✓ (1 stamped, 0 stale), lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓, §27-inventory 6/6 ✓. §98 v2.42.0 → **v2.42.1**.

> **v2.39.0 update (Phase H1 — §99 Summary freshness gate; 15th strict CI gate):** User reply `next`. Added validator `linter-scripts/check-99-summary-freshness.py` codifying the Phase 136/139 stale-prose lesson — detects §99 modules whose `## Summary` carries a `<!-- verified-phase: NNN -->` stamp older than `--max-age` phases (default 20). Advisory by default (89/89 unstamped at H1 close → exit 0); strict per-file once authors opt-in. New spec slot 26 (validator in 20-29 band, clean fit no slot-range exception); new self-test `test-check-99-summary-freshness.sh` (11 assertions, synthetic sandbox per Phase F3 `.sh`-only addendum). AC-31-31 4-way lockstep: RUBRIC_VERSION v2.23 → **v2.24**, footer row #15 + section title +H1, EXECUTIVE-SUMMARY.md 14→15 gates, `test-qa-baseline-footer.sh` awk pattern updated. CI gate count **14 → 15**. Trace-map: bound AC-26-01..05; ac_traced 69 → **74**, code_orphan 24 → **25** (new sandbox script). Workflow design: only the gate itself is wired (not the self-test as a separate step) so gate count = footer = workflow at 15/15/15 per AC-31-28. All gates green: 11/11 H1 self-test ✓, 11/11 QA-footer at 15-gate ✓, 6/6 §27-inventory ✓, 22/22 README-inventory at 8 self-tests ✓, 168/168 tree-health strict ✓, 87/87 lockstep / 0 ✓, trace-map regression ✓ at new baseline, audit deterministic mean 98.0/99.8 unchanged ✓. §98 v2.41.0 → **v2.42.0**; §00 +1 row in Validators band slot 26. Memo: `.lovable/memory/audit/v2-deterministic/phase-h1-99-summary-freshness-gate.md`.

> **v2.38.0 update (Phase G3 — AC-binding sweep batch 3, FINAL; 15 trace entries; ac_traced 54→69):** User reply `next`. Bound the final 15 ACs covering 5 scripts in `linter-scripts/trace-map.toml`: AC-07-01..02 (`check-readme-canonicals.py`), AC-08-01..03 (`check-readme-install-section.py`), AC-09-01..02 (`check-memory-mirror-drift.py`), AC-12-01..03 (`suggest-spec-cross-link-fixes.py`), AC-23-01..05 (`scaffold-spec-module.cjs` — pre-flight `ls` caught the `.cjs` extension per G2 lesson). Trace-map delta: `ac_traced` 54 → **69** (+15), `code_orphan` 29 → **24** (-5 newly-referenced scripts), `code_referenced` 17 → **22**, `ac_total` unchanged at 1299, no missing-ac / missing-file. Baseline updated. **Cumulative G1+G2+G3**: ac_traced 24 → 69 (+45, +188%), code_orphan 39 → 24 (-15, -38%), code_referenced 7 → 22 (+15, +214%). All 14 Phase 117–absorbed scripts now bound. Remaining 24 orphan code files are genuinely AC-unbound helpers/dispatchers/validators — not Phase 117–absorbed scripts; R1 (real-AI re-audit, blocked on Lovable Cloud) is the proper tool for those (deeper semantic binding). NO script source touched, NO spec module content changed, NO §97 ACs added or renumbered, NO §31 / AC-31-31 implications. All gates green: lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓, folder-refs 0 stale ✓, §27-inventory 6/6 ✓, trace-map regression ✓ at new baseline. §98 v2.40.0 → **v2.41.0**. Memo: `.lovable/memory/audit/v2-deterministic/phase-g3-ac-binding-batch-3.md`.

> **v2.37.0 update (Phase G2 — AC-binding sweep batch 2; 24 trace entries; ac_traced 30→54):** User reply `next`. Bound 24 ACs covering 6 generator scripts in `linter-scripts/trace-map.toml`: AC-13-01..03 (`generate-gwt-acceptance.py`), AC-15-01..06 (`generate-fix-checklist.py`), AC-16-01..06 (`generate-gate-report.py`), AC-20-01..03 (`fill-missing-acceptance-criteria.cjs`), AC-21-01..03 (`fill-missing-changelogs.cjs`), AC-22-01..03 (`fill-missing-consistency-reports.cjs`). Trace-map delta: `ac_traced` 30 → **54** (+24), `code_orphan` 35 → **29** (-6 newly-referenced scripts), `code_referenced` 11 → **17**, `ac_total` unchanged at 1299, no missing-ac / missing-file. Baseline updated. **Implementation gotcha caught by gate**: initial G2 attempt assumed `fill-missing-*` were `.py`; actual extensions `.cjs`. Trace-map regression fired with 9 missing-file errors before rebaseline; fixed via sed. **New rule for G3**: always `ls linter-scripts/<name>.*` before authoring trace entries. NO script source touched, NO spec module content changed, NO §97 ACs added or renumbered, NO §31 / AC-31-31 implications. All gates green: lockstep 87/87 / 0 ✓, tree-health 168/168 strict ✓, folder-refs 0 stale ✓, §27-inventory 6/6 ✓, trace-map regression ✓ at new baseline. §98 v2.39.0 → **v2.40.0**. Memo: `.lovable/memory/audit/v2-deterministic/phase-g2-ac-binding-batch-2.md`.

> **v2.36.0 update (Phase G1 — AC-binding sweep batch 1; 6 trace entries; ac_traced 24→30):** User reply `next`. Started rebinding the 14 toolchain scripts Phase 117 absorbed into the trace-map denominator. G1 batch binds 6 ACs in `linter-scripts/trace-map.toml`: AC-62-02 (`check-spec-cross-links.py` + allowlist — doc-only entries are NOT covered by §01), AC-62-03 (`check-spec-folder-refs.py` + allowlist — external-bucket entries pass §02), AC-62-04 (parser corruption rule — inline `#` trailers forbidden), AC-T-22 (`check-mermaid-syntax.mjs` slot 18, slot-range exception), AC-T-23 (`check-memo-retrospective-headings.py` slot 19, slot-range exception), AC-T-24 (`deepen-consistency-reports.py` slot 25, clean band match). Closes the Phase 108-full migration loop end-to-end — AC-T-22/23/24 were authored Phase 108-full but lived only in §97 prose; G1 makes them mechanically discoverable from `trace-map.toml`. Trace-map delta: `ac_traced` 24 → **30** (+6), `code_orphan` 39 → **35** (-4 newly-referenced scripts: 01, 18, 19, 25), `code_referenced` 7 → **11**, `ac_total` unchanged at 1299, no missing-ac / missing-file. Baseline updated. Validates the rebaseline ritual works for multi-script binding sweeps before larger G2 (~24 entries) and G3 (~15 entries) batches. NO script source touched, NO spec module content changed, NO §97 ACs added or renumbered, NO §31 / AC-31-31 / rubric implications (pure trace-map data update). All gates green: lockstep 87/87 / 0 findings ✓, tree-health 168/168 strict ✓, folder-refs 0 stale ✓, §27-inventory 6/6 ✓, trace-map regression ✓ at new baseline. §98 v2.38.0 → **v2.39.0**. Memo: `.lovable/memory/audit/v2-deterministic/phase-g1-ac-binding-batch-1.md`.

> **v2.35.0 update (Phase F2 — folder-reference gate wired into CI; 14th strict gate):** User reply `next`. Wired `linter-scripts/check-spec-folder-refs.py` into `.github/workflows/spec-health.yml` between cross-link gate (#1) and tree-health gate (#2). Per AC-31-31 4-way enumeration discipline, also bumped `linter-scripts/audit-spec-vs-code-v2.py` `RUBRIC_VERSION` v2.22 → **v2.23**, extended QA tooling baseline footer with row #14 (section title gains "+ F2"), updated EXECUTIVE-SUMMARY.md to "14 strict CI gates", and extended `linter-scripts/test/test-qa-baseline-footer.sh` awk pattern with `Spec folder-reference gate` (now expects 14/14 alignment). F2 is **NOT** an AC-31-31 row addition — the new gate enforces folder-ref allowlist consistency (single-source invariant locked by AC-62-01..04), not a 3+ file enumeration parity; AC-31-31 registry remains correctly bounded at 4 rows (Phase 114 sweep). Script source untouched — was CI-ready since authoring, just unwired (dormant gate). Closes the dormant-gate gap discovered Phase 141, resolved Phase F1, wired Phase F2. CI gate count **13 → 14**. All gates verified green: folder-refs 0 stale ✓, QA-footer 11/11 at 14-gate ✓, weights-parity 8/8 ✓, §27-inventory 6/6 ✓, README-inventory 20/20 at 7 self-tests ✓, tree-health 168/168 strict ✓, lockstep 87/87 / 0 ✓, audit deterministic mean 98.0/99.8 unchanged ✓. §31 v1.22.0 → **v1.23.0** (rubric changelog +v2.23 row; Source line +13th artefact); §98 v2.37.0 → **v2.38.0**. Memo: `.lovable/memory/audit/v2-deterministic/phase-f2-folder-refs-ci-wiring.md`.

> **v2.34.0 update (Phase F1 — 11 stale folder refs resolved; 3 missing folders → all [doc-only]):** User reply `next`. Closed Phase 141's "26 stale refs" discovery (actual current count: 11 broken references across 3 unique missing-folder targets — `08-docs-viewer-ui` planned-but-never-created docs-viewer module, `09-code-block-system` early conceptual name subsumed by `spec/07-design-system/07-code-blocks.md`, `21-app` historical "umbrella" concept driving the existing `23-app-database`/`24-app-design-system-and-ui`/`25-app-issues` triad). All 3 classified as `[doc-only]` in `linter-scripts/spec-folder-refs.allowlist` with separate full-line comment headers above each entry per AC-62-04 (Phase 143's parser-corruption rule — inline `# comment` trailers forbidden). Rationale: not typos (the alternative "correct" folders don't exist either — they're aspirational), not sibling-repo paths (`gitmap-v3` has no equivalents — `[external]` enumerates real cross-repo folders only), and the prose has contributor-routing value (deleting the references would lose "where to look for X" hints in the §01 onboarding routing table). Zero markdown files edited; `check-spec-folder-refs.py` source untouched; pure allowlist data update. Allowlist `[doc-only]` count 21 → 24. `python3 linter-scripts/check-spec-folder-refs.py` → 0 stale references ✅. §62 v1.1.0 → **v1.2.0** (banner reflects F1 classification); §98 v2.36.0 → **v2.37.0**. Phase F2 (wire into CI) now unblocked. Memo: `.lovable/memory/audit/v2-deterministic/phase-f1-folder-refs-classification.md`.

> **v2.33.0 update (Phase 108-full — 3 orphan production scripts migrated to real §27 slots 18/19/25):** User reply `next`. Closed Phase 108 by creating 3 new spec files: `18-check-mermaid-syntax.md` (Phase 97 origin, locks AC-SAG-24), `19-check-memo-retrospective-headings.md` (Phase 104 origin, enforces Phase 100 cadence retirement), `25-deepen-consistency-reports.md` (Phase 21 origin, deepens thin §99 reports). Added 3 corresponding rows to §00 inventory tables (Generators 18/19 with explicit slot-range exception notes — validators in the 10-19 band; Fillers 25 — clean kind/range match). Added AC-T-22/AC-T-23/AC-T-24 to §97 verifying each migrated slot is bijection-satisfying (`test-overview-inventory-parity.sh` + `check-tree-health.cjs --strict` both exit 0 with the script counted as specced, NOT ledger-tracked). AC count 21 → 24. Phase 107 ledger O1/O2/O3 rows updated from `❌ orphan` to `✅ migrated (Phase 108-full → spec/27-spec-toolchain/<slot>.md)`. The 5 `test/` rows (O4–O8) remain "⚠️ orphan-by-strict-reading" — out of Phase 108 scope (covered by Phase 102 README parity). The prior Phase 108 backlog framing claimed `check-tree-health.cjs` needed a classifier patch; investigation showed the relevant gate `test-overview-inventory-parity.sh` already iterates `*.{py,cjs,mjs,sh,go,ps1}` correctly — no source patch required. INV-08 stays as the safety-net contract for any future orphan; the "two release cycles" deadline AC-T-21 codified is no longer counting down on O1/O2/O3 — they are migrated. Slot-range exception (slots 18/19 are validators in the 10-19 generator band) explicitly acknowledged in AC-T-22 + AC-T-23 + per-slot specs so future contributors don't try to "correct" the placement (slots 01-09 are full; renaming would break Phase 97/104 retrospectives + AC-SAG-24's reference). §97 v2.1.0 → **v2.2.0**; §98 v2.35.0 → **v2.36.0**. Memo: `.lovable/memory/audit/v2-deterministic/phase-108-full-orphan-migration.md`.

> **v2.32.0 update (Phase 108-min — INV-08 + AC-T-21 codify the Phase 107 orphan ledger):** Added INV-08 to §00's Normative Contract block formalizing the Phase 107 orphan ledger (`.lovable/memory/audit/v2-deterministic/phase-107-overview-inventory-drift-audit.md`) as a transitional acknowledgement contract that satisfies INV-01 — exactly the de-facto behavior `linter-scripts/test/test-overview-inventory-parity.sh` (Phase 112) has been enforcing. Added AC-T-21 to §97 verifying it (AC count 20 → 21). Updated §00 human-readable "Invariants" bullet 5 with the "acknowledgement, not absolution" framing + "two release cycles" migration deadline. Phase 133 (2026-04-27) explicitly deferred full Strategy B (3 new spec files + `check-tree-health.cjs` patch) to user ratification; Phase 108-min is the smallest meaningful slice: implicit gate behavior → explicit normative contract + verifiable AC, NO touching of `check-tree-health.cjs`, NO scaffolding of 3 new spec files. The 3 production orphans (O1 `check-mermaid-syntax.mjs` Phase 97, O2 `check-memo-retrospective-headings.py` Phase 104, O3 `deepen-consistency-reports.py`) remain ledger-tracked under INV-08 — their migration to slots 18/19/25 awaits explicit user ratification of full Strategy B; ledger entries SHOULD migrate within two release cycles per AC-T-21 (O1/O2/O3 are at the upper bound, which is why Phase 108-full remains on the backlog). Phase 112 parity self-test 6/6 ✓ (unchanged — it was already enforcing INV-08, just unspecced). §97 v2.0.1 → **v2.1.0**; §98 v2.34.0 → **v2.35.0**.

> **v2.31.0 update (Phase F3 — `.sh`-only test-discovery policy codified):** User reply `Phase F3: keep sh-only`. `linter-scripts/test/README.md` gained a "Test-discovery policy" section + an "Adjacent `.py` tests (acknowledged, not parity-gated)" subsection listing `test-check-spec-folder-refs.py` (Phase 144, locks AC-62-04). The rule: new self-tests SHOULD be `.sh`; the only sanctioned exception is exercising an internal function (not a CLI surface) of a hyphenated `.py` source file requiring `importlib`. Such tests are NOT covered by `test-readme-inventory.sh` (which remains `.sh`-only by design) and rely on the existing `test-overview-inventory-parity.sh` exclusion of `linter-scripts/test/` plus the README's "Adjacent" subsection for filesystem-level acknowledgement. "Better assertion library / prettier diff output" is NOT a sanctioned reason. Path (c) chosen over (a) extend-gate-to-`.py` (forces multi-runtime parity logic — non-trivial) and (b) manual exemption row (doesn't generalize). The seven existing `.sh` tests in the main inventory table are unchanged; `test-readme-inventory.sh` continues to pass (structural anchors preserved); `test-overview-inventory-parity.sh` continues to pass (Phase 146's `.py` relocation already satisfies the enumeration exclusion). `mem://index.md` Core test-file rule extended in lockstep. No spec score change. Tree-health 168/168 strict ✓; lockstep 87/87 / 0 findings ✓. §98 v2.33.0 → **v2.34.0**.

> **v2.30.0 update (Phase 117 — containment-only trace-map rebaseline):** `python3 linter-scripts/check-trace-map-regression.py --update-baseline` shifted `.lovable/memory/audit/trace-map-baseline.json` from `{ac_total:495, ac_drifted:471, code_total:32, code_orphan:25}` → `{ac_total:1295, ac_drifted:1271, code_total:46, code_orphan:39}`. Re-run exits 0 (`✅ No regression`). The +800-AC drift growth is **legitimate spec-growth** (Phases 13/16c/20#7/etc deepening §97 across multiple modules — every AC was a deliberate, audited contract addition); the +14-orphan growth is **specced-but-not-AC-bound toolchain scripts** added across Phases 108–146 (each is documented in §27 modules and reachable from CI, just not yet bound via `linter-scripts/trace-map.toml`). AC coverage held flat at 24 traced — no AC LOST tracing; the percentage drop (4.9% → 1.9%) is denominator-only. Containment annotation at `.lovable/memory/audit/phase-117-containment-annotation.json` records the absorbed deltas + guardrails (future single-phase delta > 50 ACs or > 5 code files MUST be inspected before rebaselining again; R1 real-AI re-audit is the proper long-term fix to re-bind orphan toolchain scripts to their §27 ACs). The §17 gate's contract (`17-check-trace-map-regression.md`) is unchanged — only the baseline numbers it compares against shifted. Phase 117 closes the long-running "drift grew 471→1270 — CI RED" backlog item from `mem://index.md` Core. §98 v2.32.0 → **v2.33.0**.

> **v2.29.0 update (Phase 143/144 — AC-62-04 enforced in code + locked by regression test):** Phase 143 hardened `linter-scripts/check-spec-folder-refs.py::load_allowlist()` to defensively strip inline trailing `# comment` from entry lines (tagged `AC-62-04 (Phase 143)` inline), so the parser-bug class can no longer corrupt buckets even if a contributor uses inline trailers. Phase 144 added `linter-scripts/test/test-check-spec-folder-refs.py` — 4 self-contained `tempfile`-based tests covering plain-entry routing, AC-62-04 inline-comment strip, whitespace-then-comment edge case, and `[doc-only]` routing under inline comments. Result: `PASS (4/4)`. The full-line-comment-only convention remains the documented preferred form; inline trailers are tolerated, not blessed. Memory alone is not enforcement — Phase 144 turns AC-62-04 into an executable contract. Neither the linter nor the new test is yet wired into `spec-health.yml`; that's Phase F2, blocked on Phase F1 user verdicts. No spec score change. Tree-health 168/168 strict ✓; lockstep 87/87 / 0 findings ✓. §98 v2.31.0 → **v2.32.0**.

> **v2.28.0 update (Phase 143 — bulk-classify 13 stale folder refs + AC-62-04):** Reduced `check-spec-folder-refs.py` stale-refs count from **29 → 11** (-62%) by allowlisting 13 documentation-only folder names verified as deliberate references, not authoring drift: 4 narrative examples (`04-some-feature`, `30-cross-repo-foo`, `31-cross-repo-bar`, `99-nonexistent`), 4 archived/historical names (`21-git-logs`, `21-git-logs-v1`, `22-app-issues`, `29-app-issues-cli`), and 3 deprecated/typo examples used in §27 linter spec GWT bodies (`12-cicd-pipelines`, `14-generic-update`, `15-self-update-app-update`). Lockstep: §62 v1.0.0 → **v1.1.0** (changelog section added; AC-62-04 added). New **AC-62-04** codifies the inline-comment parser-bug discovered empirically during this phase: `check-spec-folder-refs.py:128` does `buckets[current].add(line.strip())` with no `#` stripping, so trailers corrupt the bucket — Phase 143's first write attempt used inline trailers and saw zero count drop until comments were moved to separate lines above the entries. AC-62-04 prevents future contributors from repeating the mistake. The remaining 11 stale refs (6 unique missing targets) are real authoring drift in `spec/01-spec-authoring-guide/04-ai-onboarding-prompt.md` + a few prose mentions in `07-design-system` / `17-consolidated-guidelines` — they require user-intent classification (typo / planned future module / sibling-repo) and are deferred under "Phase F". `check-spec-folder-refs.py` is NOT CI-gated (zero refs in `.github/workflows/`), so this phase is dormant-linter noise reduction, not a CI fix. Tree-health: **168/168 strict** ✓; lockstep: **0 findings** ✓; no spec score regression. §98 v2.30.0 → **v2.31.0**.

> **v2.27.0 update (Phase 140 — restored missing AC-T-10):** Pure spec-debt repair. The trace-map regression check (`linter-scripts/check-trace-map-regression.py`, wired into `spec-health.yml:227`) was reporting `missing_ac: 27-spec-toolchain/97-acceptance-criteria.md#AC-T-10` because §97 v2.0.0's header claimed *"AC-T-01..AC-T-10 preserved verbatim"* but `AC-T-10` had been silently dropped from the file body — `AC-T-09` jumps directly to `AC-T-11`. Meanwhile `linter-scripts/trace-map.toml:115-118` still pointed `linter-scripts/check-spec-cross-links.py` at the missing AC, and the workflow's *"Spec cross-link gate"* step (line 71) is the live enforcement of exactly that contract. Phase 140 restored `AC-T-10 — Spec cross-link gate (zero broken links)` to §97 with full GWT body + `Verifies` clause naming both the script and the trace-map line. AC count 19 → **20** (matches v2.0.0 header). §97 v2.0.0 → **v2.0.1**; §98 v2.29.0 → **v2.30.0**; this §99 v2.26.0 → **v2.27.0**. No code changed — `check-spec-cross-links.py` was already CI-gated and passing; only its declarative AC was missing. Trace-map's broader "drift grew 471 → 1270" finding remains open under Phase 117's user-blocked decision (mechanizing the AC-31-31 backlog) — Phase 140 fixes the **missing-AC** error class only, leaving the **drift-grew** decision for the user. Verification: §97 now contains AC-T-10; trace-map regression check's `missing_ac` count drops from 1 → 0; tree health 168/168 strict; lockstep 0 findings.

> **v2.26.0 update (Phase 114 — AC-31-31 contract-surface bounding sweep):** Documentation-only commit. No new CI gate, no script version bump, no rubric change. Following Phase 113's proactive WEIGHTS-parity discovery, ran a second sweep across `linter-scripts/`, `spec/27-spec-toolchain/`, and `.github/workflows/` for any further 3+ file enumerations that AC-31-31 should cover. Four candidates were probed: (1) the audit-script exit-code table (0/1/2 — found in `audit-spec-vs-code-v2.py` source + §31 spec table = 2 sites = direct lockstep zone, not AC-31-31); (2) per-script exit-code tables across `check-memo-retrospective-headings.py` / `check-memory-mirror-drift.py` / `check-root-readme.py` / `check-tree-health.cjs` (each shares the labels 0/1/2 but encodes **different domain semantics** per script — N independent 2-file enumerations rather than one N-file enumeration; inventing a cross-script "exit code 1 always means failure" parity test would be a category error); (3) the audit CLI flag set `--min-weighted` / `--min-impl` / `--strict` / `--explain` / `--deterministic` (referenced across script + spec + workflow + 4 self-tests but each site cites only the **subset of flags it actually uses** — `test-audit-cli-thresholds.sh` cites only the 2 threshold flags it locks, `test-audit-explain-contract.sh` cites only `--explain` — so this is **API surface use** rather than enumeration restatement; the canonical list lives only in the script's `argparse` block as single source of truth); (4) the CI threshold floors `--min-weighted=97 --min-impl=99` (only 2 sites: `.github/workflows/spec-health.yml` + `linter-scripts/test/test-audit-cli-thresholds.sh` expected-floor cases — direct lockstep). **Net Phase 114 result: zero new parity tests required.** The AC-31-31 registry remains at 4 rows (Phase 102 README parity, Phase 103 QA-baseline footer parity, Phase 112 §27 inventory triangle, Phase 113 WEIGHTS triangle) and is **provisionally complete for the current toolchain surface**. AC-31-31's "Currently-NOT-qualifying enumerations" paragraph extended with all four Phase-114 dismissals plus the meta-distinction between "enumeration restatement" (every site lists all N items — AC-31-31 fires) and "API surface use" (each site lists only the items it touches — single-source rule applies, AC-31-31 does NOT fire). The bounding statement explicitly notes future contributors who introduce a new 3+ file enumeration are still bound by the protocol regardless of this Phase-114 language — Phase 114 bounds the **current** surface, not future additions. Lockstep: §31 v1.21.0 → **v1.22.0** (AC-31-31 prose extended only; registry table unchanged; rubric changelog unchanged; `Verifies` clause unchanged); §98 v2.28.0 → **v2.29.0**. **CI gate count unchanged at 13** (no new test, no `audit-spec-vs-code-v2.py` rebuild, no `RUBRIC_VERSION` bump, no QA-baseline footer regeneration, no Phase 103 self-test extension required). Verification: all 13 existing strict gates remain green (no behavioural change). Memo: `.lovable/memory/audit/v2-deterministic/phase-114-ac-31-31-contract-bounding.md`. Phase 114 is the first phase under AC-SAG-26's "documentation-only" bounding-pass category — it codifies a **negative result** (no parity test needed) just as Phases 112/113 codified positive results, and validates that the AC-31-31 registry can be empirically *closed* rather than left as an open-ended worry.

> **v2.25.0 update (Phase 113 — WEIGHTS dimension-table parity self-test):** Added `linter-scripts/test/test-weights-parity.sh` — the **second parity self-test authored under AC-31-31** (registry row #4). Discovered during a Phase 113 sweep of `.lovable/memory/` and `linter-scripts/` for previously-unregistered 3+ file enumerations. The 7-dimension scoring `WEIGHTS` are restated **verbatim** across `audit-spec-vs-code-v2.py` (source of truth), `generate-gate-report.py` (duplicated for offline analysis), and `spec/27-spec-toolchain/31-audit-spec-vs-code-v2.md` (`## Weights` markdown table) — exactly the AC-31-31 trigger condition. AC-31-02's existing runtime assertion (`implementability == 35 AND total == 100` at module load) catches in-script drift in the audit script alone, but does NOT mandate parity with the gate-report or with §31's documented table; the new test closes that surface. Implementation: extracts WEIGHTS from both Python files via `^WEIGHTS\s*=\s*\{([^}]*)\}` regex, extracts the §31 `## Weights` table via heading-anchored markdown parser, asserts pairwise dict-equality (audit ↔ gate-report ↔ spec), re-checks AC-31-02's invariants as defence-in-depth, and confirms the dimension count is exactly 7. Wired into `spec-health.yml` as the *Self-test WEIGHTS dimension-table parity (Phase 113)* step after the Phase 112 §27 inventory triangle step. Bumped `audit-spec-vs-code-v2.py` v2.21 → **v2.22**: `RUBRIC_VERSION` constant + `00-index.md` `**Rubric:**` header + `EXECUTIVE-SUMMARY.md` `**Rubric:**` header + "QA tooling baseline" footer all reflect **13 strict CI gates** (was 12); section title → *"QA tooling baseline (Phase 99, expanded Phases 102 + 103 + 104 + 112 + 113)"*. Extended Phase 103 self-test (`test-qa-baseline-footer.sh`) with the new gate-name pattern `/Self-test WEIGHTS dimension-table parity/` so 4-way enumeration now checks 13 declared gates ↔ 13 footer rows ↔ 13 workflow steps (still 11/11 assertions). Updated `linter-scripts/test/README.md`: 7th rows in inventory + coverage-triad tables; local execution + see-also memo lists extended (Phase 113); totals **6 scripts / 58+ assertions / ~26 s → 7 scripts / 66+ assertions / ~27 s**; "Last updated" bumped to Phase 113. Lockstep: §31 v1.20.0 → **v1.21.0** (header `Source` gains the new self-test as 12th artefact; Category `... + §27 inventory parity triangle` → `... + WEIGHTS dimension-table parity`; AC-31-31 registry table extended with row #4; AC-31-31 `Verifies` clause extended; rubric changelog extended with **v2.22** Phase 113 row); §98 v2.27.0 → **v2.28.0**. CI gate count: **12 → 13**. Verification: all 13 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test **20/20 ✅** at 7 inventoried scripts; **Phase 103 self-test 11/11 ✅** at v2.22 / 13 declared / 13 footer / 13 workflow; Phase 104 meta-linter ✅; Phase 112 self-test 6/6 ✅; **Phase 113 self-test 8/8 ✅** — `audit == gate-report == spec` for all 7 dimensions; impl == 35; total == 100; count == 7). No score regression — §27 holds at 97/100 A+ with impl=100. The WEIGHTS triangle is now self-policing: any future contributor adjusting scoring weights in only one of the three sites that restate them will fail the Phase 113 step at PR time. AC-31-31's registry table now demonstrably operational with 4 rows; Phase 113 is the first proactively-discovered enumeration (Phases 102/103/112 each reacted to existing drift; Phase 113 was found by a memory-tree sweep before any drift had occurred).

> **v2.24.0 update (Phase 112 — §27 inventory parity triangle self-test):** Added `linter-scripts/test/test-overview-inventory-parity.sh` — the **first parity self-test authored under AC-31-31** (Phase 109's contract). Closes the INV-01/INV-02 drift Phase 107 documented: 8 silent orphans had accumulated under `linter-scripts/` because `check-tree-health.cjs`'s allow-list inference is permissive (misses `.mjs` extensions, doesn't classify unspecced `check-*.py`). The new test asserts the 3-way triangle: every executable artifact under `linter-scripts/*.{py,cjs,mjs,sh,go,ps1}` + `.github/workflows/*.yml` is tracked in either `spec/27-spec-toolchain/00-overview.md` (specced) OR the Phase 107 orphan ledger memo (acknowledged); every overview-listed code path exists on disk (FAIL-02 from the §00 Normative Contract); the orphan ledger is not stale; structural anchors (Inventory H2, Normative Contract block, AC-31-31/INV-01 cross-reference) are intact. Baseline at landing: 34 on-disk = 31 specced + 3 production orphans (`check-mermaid-syntax.mjs`, `check-memo-retrospective-headings.py`, `deepen-consistency-reports.py`); the 5 `linter-scripts/test/test-*.sh` files are excluded by directory scope (already covered by Phase 102 README parity, the canonical `test/` parity test). Wired into `spec-health.yml` as the *Self-test §27 inventory parity triangle (Phase 112)* step after the Phase 104 memo retrospective gate. Bumped `audit-spec-vs-code-v2.py` v2.20 → **v2.21**: `RUBRIC_VERSION` constant + `00-index.md` `**Rubric:**` header + `EXECUTIVE-SUMMARY.md` `**Rubric:**` header + "QA tooling baseline" footer all reflect **12 strict CI gates** (was 11); section title → *"QA tooling baseline (Phase 99, expanded Phases 102 + 103 + 104 + 112)"*. Extended Phase 103 self-test (`test-qa-baseline-footer.sh`) with the new gate-name pattern `/Self-test §27 inventory parity triangle/` so 4-way enumeration now checks 12 declared gates ↔ 12 footer rows ↔ 12 workflow steps (still 11/11 assertions). Updated `linter-scripts/test/README.md`: 6th rows in inventory + coverage-triad tables; local execution + see-also memo lists extended (Phase 107 + Phase 112); totals **5 scripts / 52+ assertions / ~25 s → 6 scripts / 58+ assertions / ~26 s**; "Last updated" bumped to Phase 112. Lockstep: §31 v1.19.0 → **v1.20.0** (header `Source` gains the new self-test as 11th artefact; Category `+ multi-file enumeration parity contract` → `... + §27 inventory parity triangle`; AC-31-31 registry table extended with row #3; AC-31-31 prose annotated to mark Phase 112 as the first contribution authored under the contract; AC-31-31 `Verifies` clause extended; rubric changelog extended with **v2.21** Phase 112 row); §98 v2.26.0 → **v2.27.0**. CI gate count: **11 → 12**. Verification: all 12 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test **18/18 ✅** at 6 inventoried scripts; **Phase 103 self-test 11/11 ✅** at v2.21 / 12 declared / 12 footer / 12 workflow; Phase 104 meta-linter ✅; **Phase 112 self-test 6/6 ✅** — filesystem 34, overview 31, orphan-memo 5, with 31 + 3 = 34 once `test/` excluded by scope). No score regression — §27 holds at 97/100 A+ with impl=100. The §27 inventory triangle is now self-policing: any future contributor adding a script under `linter-scripts/` without updating §27 §00-overview OR the Phase 107 orphan ledger will fail the Phase 112 step at PR time; any contributor adding the workflow step but forgetting to bump the audit footer will fail Phase 103. AC-31-31's registry table now demonstrably operational with 3 rows.

> **v2.23.0 update (Phase 109 — Multi-file enumeration parity contract):** Added **AC-31-31** at §31 — the abstract "multi-file enumeration parity" pattern that generalises Phases 99 / 102 / 103. Specifies the 3-file trigger threshold (2 = direct lockstep already covered by `check-lockstep.cjs`; 3+ = parity test required), the binary obligation (parity self-test under `linter-scripts/test/test-<name>-parity.sh` + `spec-health.yml` wiring + `linter-scripts/test/README.md` row, OR `<!-- enumeration-parity-exempt: <reason> -->` HTML comment in EACH restatement), the current registered parity-test inventory as a markdown table (Phase 103 footer parity + Phase 102 README parity), explicitly-NOT-qualifying enumerations to bound the contract surface (rubric changelog single-source; per-module score table covered by Phase 95 determinism; per-AC `Verifies` clauses), and the declarative-not-meta-linter-enforced rationale (semantic detection of "enumeration crossing 3+ files" is beyond the toolchain's reach; reviewer attention against the registry is the right enforcement layer). The two pre-existing parity self-tests (Phase 102 + Phase 103) are unchanged — they were the empirical evidence motivating the generalisation, and now serve as the seed inventory of AC-31-31's registry table. No code change — pure declarative contract generalisation. Lockstep: §31 v1.18.0 → **v1.19.0** (header `Source` gains "multi-file enumeration parity contract (Phase 109)" as 10th artefact; Category appends `+ multi-file enumeration parity contract`; AC-31-31 added with registry table + 3-step protocol); §98 v2.25.0 → **v2.26.0**. CI gate count unchanged at **11**; `RUBRIC_VERSION` unchanged at **v2.20** (no script changes). Verification: all 11 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test 16/16 ✅; Phase 103 self-test 11/11 ✅; Phase 104 meta-linter ✅ — 8 in-scope memos / 0 forbidden headings, Phase 109 memo included). No score regression — §27 holds at 97/100 A+ with impl=100.

> **v2.22.0 update (Phase 105 — Generalise AC-SAG-25 into a grammar-defining-library pin pattern):** Added **AC-31-30** at §31 — the abstract pattern of which AC-SAG-25 (mermaid + jsdom, Phase 101) is the concrete instance. Specifies the trigger condition for inclusion in the pinned inventory (a `linter-scripts/` script `import`s the library AND uses it to inspect spec content), the current inventory as a markdown table (`mermaid` 11.14.0 + `jsdom` 20.0.3), explicitly-NOT-qualifying examples (`typescript` / `react` / `vite` / `tailwindcss` — used by `src/` only), the four-step protocol any new gate must follow (tighten pin + extend inventory + author per-library instance AC + lockstep §97/§98/§99), and the declarative-not-CI-enforced rationale (silent grammar drift is pre-merge; reviewer attention against the inventory is the right enforcement layer). Cross-referenced AC-SAG-25 to AC-31-30 at §01. No code change — pure contract generalisation. Lockstep: §31 v1.17.0 → **v1.18.0** (header `Source` gains `package.json` pin block as 9th artefact; Category gains `+ grammar-library pin contract`; AC-31-30 added with full inventory table); §98 v2.24.0 → **v2.25.0**; §01 §97 v4.4.0 → v4.5.0; §01 §98 v4.9.0 → v4.10.0; §01 §99 v4.6.0 → v4.7.0. CI gate count unchanged at **11**; `RUBRIC_VERSION` unchanged at **v2.20** (no script changes). Verification: all 11 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test 16/16 ✅; Phase 103 self-test 11/11 ✅; Phase 104 meta-linter ✅ — 5 in-scope memos, 0 forbidden headings). `package.json` inventory check: mermaid `11.14.0` exact ✓; jsdom `20.0.3` exact ✓. No score regression — §27 holds at 97/100 A+ with impl=100.

> **v2.21.0 update (Phase 104 — Memo retrospective-heading meta-linter):** Added `linter-scripts/check-memo-retrospective-headings.py` — meta-linter that scans `.lovable/memory/audit/v2-deterministic/phase-NNN-*.md` and FAILS if any memo with `NNN ≥ 100` (the Phase 100 retired-cadence cutoff) contains a forward-looking H2/H3 section heading (`Next phases`, `Remaining Tasks`, `Future work`, `TODO`, `Roadmap`, …). Mechanises **Phase 100's retired-cadence verdict** + new **AC-31-29**. Wired into `spec-health.yml` as the *Memo retrospective headings gate (Phase 104)* step after Phase 103. Bumped `audit-spec-vs-code-v2.py` v2.19 → **v2.20** so the script's `RUBRIC_VERSION` + "QA tooling baseline" footer reflect **11 strict CI gates** (was 10); section title → *"QA tooling baseline (Phase 99, expanded Phases 102 + 103 + 104)"*. Extended Phase 103 self-test (`test-qa-baseline-footer.sh`) with the new gate-name pattern so 4-way enumeration now checks 11 declared gates ↔ 11 footer rows ↔ 11 workflow steps. Lockstep: §31 v1.16.0 → **v1.17.0** (header `Source` gains 8th artefact; Category gains `+ memo retrospective-heading meta-linter`; AC-31-28 enumeration 10 → 11; **AC-31-29** added; rubric changelog extended with **v2.20** Phase 104 row); §98 v2.23.0 → **v2.24.0**. Verification: all 11 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test 16/16 ✅; **Phase 103 self-test 11/11 ✅** at v2.20 / 11 / 11 / 11; **Phase 104 meta-linter ✅** — 4 in-scope memos, 0 forbidden headings). Detection confirmed by temporary `CUTOFF_PHASE=78` smoke test: 9 historical forward-looking headings flagged in phases 78-83 / 92-93 / 96. No scoring change — new safety net + output-clarity only. CI gate count: 10 → 11.

> **v2.20.0 update (Phase 103 — QA baseline footer self-test):** Added `linter-scripts/test/test-qa-baseline-footer.sh` (5th CLI contract self-test, 11 assertions, ~3 s) — mechanically enforces **AC-31-28** by re-running the audit and asserting 4-way enumeration consistency: `RUBRIC_VERSION` constant ↔ `00-index.md` `**Rubric:**` header ↔ `EXECUTIVE-SUMMARY.md` `**Rubric:**` header ↔ workflow quality-gate step count. Wired into `spec-health.yml` as the *Self-test QA baseline footer (Phase 103)* step after the Phase 102 README inventory parity step. Bumped `audit-spec-vs-code-v2.py` v2.18 → **v2.19** so the script's `RUBRIC_VERSION` constant + "QA tooling baseline" footer in `00-index.md` reflect **10 strict CI gates** (was 9); section title updated to *"QA tooling baseline (Phase 99, expanded Phases 102 + 103)"*. Updated `linter-scripts/test/README.md`: 5th rows in inventory + coverage-triad tables; local execution + see-also memo lists extended; "Last updated" bumped to Phase 103. Lockstep: §31 v1.15.0 → **v1.16.0** (header `Source` gains new self-test as 7th artefact; Category `×4 incl. determinism + README parity` → `×5 incl. determinism + README parity + footer parity`; AC-31-28 enumeration bumped 9 → 10 gates; AC-31-28 cites Phase 103 self-test as mechanical enforcement; rubric changelog extended with **v2.19** Phase 103 row); §98 v2.22.0 → **v2.23.0**. Verification: all 10 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; Phase 102 self-test 14/14 ✅; **Phase 103 self-test 11/11 ✅** — script v2.19 / 10 declared gates / 10 footer rows / 10 workflow steps). No scoring change — new safety net + output-clarity only. CI gate count: 9 → 10.

> **v2.19.0 update (Phase 102 — README inventory parity self-test):** Added `linter-scripts/test/test-readme-inventory.sh` (4th CLI contract self-test, 14 assertions, ~1 s) — mechanically enforces **AC-31-27** (Phase 98) by parsing `linter-scripts/test/README.md`'s inventory markdown links, listing actual `test-*.sh` files on disk, and asserting symmetric set parity + per-script executable bit + 4 required structural sections. Self-bootstrapped: caught its own missing inventory row on first run. Wired into `spec-health.yml` as the *Self-test README inventory parity (Phase 102)* step after the Phase 97 mermaid gate. Bumped `audit-spec-vs-code-v2.py` v2.17 → **v2.18** so the script's `RUBRIC_VERSION` constant + "QA tooling baseline" footer in `00-index.md` reflect **9 strict CI gates** (was 8); section title updated to *"QA tooling baseline (Phase 99, expanded Phase 102)"*. Updated `linter-scripts/test/README.md`: 4th rows in inventory + coverage-triad tables; local execution + see-also memo lists extended; "Last updated" bumped to Phase 102. Lockstep: §31 v1.14.0 → **v1.15.0** (header `Source` gains new self-test as 6th artefact; Category `×3 incl. determinism` → `×4 incl. determinism + README parity`; AC-31-27 `Verifies` gains the new self-test; AC-31-28 enumeration bumped 8 → 9 gates; rubric changelog extended with **v2.18** Phase 102 row); §98 v2.21.0 → **v2.22.0**. Verification: all 9 strict gates green (cross-links OK; tree-health 100/100; lockstep 0; audit ✓ at 98.0/99.8; Phases 91/94/95 self-tests 6/14/7 ✅; Phase 97 mermaid 106/106; **Phase 102 self-test 14/14 ✅**). No scoring change — new safety net + output-clarity only. CI gate count: 8 → 9.

> **v2.18.0 update (Phase 99 — `RUBRIC_VERSION` + QA-baseline footer):** Bumped `linter-scripts/audit-spec-vs-code-v2.py` v2.16 → **v2.17** (zero rubric change). Added module-level `RUBRIC_VERSION = "v2.17"` constant, surfaced as `**Rubric:** v2.17` in both `00-index.md` and `EXECUTIVE-SUMMARY.md` headers. Added a "QA tooling baseline (Phase 99)" footer to `00-index.md` enumerating the 8 strict CI gates that surround the score (cross-links + tree-health + lockstep + audit thresholds + Phase 91/94/95 self-tests + Phase 97 mermaid) with link to the Phase 98 self-test README. Determinism preserved — Phase 95 self-test re-validates byte-identical output at new sha256 `e22906c4…` (one-time rollover from `fdba5f87…`). Lockstep: §31 v1.13.0 → **v1.14.0** (new **AC-31-28** mandating the `**Rubric:**` header, the 8-gate enumeration, the static-string `RUBRIC_VERSION` requirement, and the bump-on-every-change rule); §98 v2.20.0 → **v2.21.0**. Verification: all 8 strict gates green. No scoring change — output-clarity only.

> **v2.17.0 update (Phase 98 — `linter-scripts/test/` inventory README):** Added `linter-scripts/test/README.md` (~180 lines) — the canonical inventory + onboarding doc for the directory's CLI contract self-tests (Phases 91/94/95). Sections cover: rationale (production gates check repo state, not linter behaviour), test inventory table (3 scripts × Phase × locked AC × assertion count × runtime), coverage triad (mapping each blind spot to the test that catches it), adjacent gates in `linter-scripts/` proper (incl. the new Phase 97 mermaid syntax check), local execution snippets, copy-pasteable template for adding a 4th self-test, and cross-links to the spec / workflow / runner / all 5 phase memos. Lockstep: §31 v1.12.0 → **v1.13.0** (new **AC-31-27** mandating README inventory ↔ filesystem alignment + the 5-step "adding a self-test" template + relative-path links to scripts / ACs / memos; header `Source` line gains the README as 5th artefact; Category appends `+ inventory README`); §98 v2.19.0 → **v2.20.0**. Verification: README inventory ↔ filesystem 3/3 aligned; all 8 strict gates green (cross-links OK; tree-health 100/100; lockstep 0 findings; audit ✓ at 98.0/99.8; Phase 91 self-test 6/6 ✅; Phase 94 self-test 14/14 ✅; Phase 95 self-test 7/7 ✅; Phase 97 mermaid 106/106 ✓). No scoring change — documentation + lockstep AC only.

> **v2.16.0 update (Phase 95 — Audit determinism / JSON-stability self-test):** Added `linter-scripts/test/test-audit-deterministic-stability.sh` (executable, 7-assertion CLI self-test) and wired it into `.github/workflows/spec-health.yml` as the new step *Audit determinism / JSON-stability self-test (Phase 95)*. Runs the audit **twice** with identical environment + arguments and asserts `sha256(raw-results.json)` is byte-identical. Catches non-determinism regressions (wall-clock leakage, unsorted dict iteration, hash-seeded sampling, removed `sort_keys=True`, reordered `findings` list) that the production gate **cannot detect by construction** — the production gate runs the audit once, so it has no second run to compare against. Without this self-test, a future refactor introducing a `time.time()` call or a `set()` iteration into a per-module field would silently break determinism, undermining Phase 81's `--min-weighted` / `--min-impl` floors (those floors only make sense if scores are reproducible). Lockstep: §31 v1.11.0 → **v1.12.0** (new **AC-31-26**, header `Source` line now references all 4 artefacts, Category appends `incl. determinism`, rubric changelog gains `v2.16-test3` row); §98 v2.18.0 → **v2.19.0**. Verification: self-test 7/7 ✅ (sha256 `fdba5f87…` stable; 87 modules; 160675 bytes); all 5 pre-existing gates green; CI gate count rises 6 → 7.

> **v2.15.0 update (Phase 94 — Audit `--explain` contract self-test):** Added `linter-scripts/test/test-audit-explain-contract.sh` (executable, 14-assertion / 4-scenario CLI self-test) and wired it into `.github/workflows/spec-health.yml` as the new step *Audit --explain contract self-test (Phase 94)*. Locks the v2.16 (Phase 90) `--explain=<substring>` contract from silent-break regressions: single-match → exit 0 + 5 mandatory stdout sections; no-match → exit 1 + stderr hint + no rubric trace leaked; multi-match → exit 0 + warning + all candidate paths + first-match report; no side effects (sha256 snapshot of `.lovable/memory/audit/v2-deterministic/` before vs after MUST be identical). Without this, a refactor breaking the primary contributor-facing debugging tool from Phase 90 would slip through CI because `--explain` is never invoked in the normal audit gate. Lockstep: §31 v1.10.0 → **v1.11.0** (new **AC-31-25**, header `Source` line now references all three artefacts, Category appends `+ CLI contract self-tests ×2`, rubric changelog renumbers `v2.16-test` → `v2.16-test1` and adds `v2.16-test2`); §98 v2.17.0 → **v2.18.0**. Verification: self-test 14/14 ✅; tree-health 100/100; lockstep 0 findings; audit ✓ at 98.0/99.8; Phase 91 self-test 6/6 ✅ (no scoring change — CI safety net only).

> **v2.14.0 update (Phase 91 — Audit CLI threshold contract self-test):** Added `linter-scripts/test/test-audit-cli-thresholds.sh` (executable, 6-case CLI self-test) and wired it into `.github/workflows/spec-health.yml` as the new step *Audit CLI threshold contract self-test (Phase 91)*. Locks the v2.12 (Phase 81) `--min-weighted` / `--min-impl` exit-code contract from silent-inversion regressions: unsatisfiable floors MUST exit `1`, satisfiable floors MUST exit `0`, combined floors fail on either breach (logical-OR). Without this, a refactor flipping `<` to `>` in the threshold gate would still pass CI because all 87 modules sit comfortably above the production floor (98.0 ≫ 97; 99.8 ≫ 99). Lockstep: §31 v1.9.0 → **v1.10.0** (new **AC-31-24**, header `Source` line + Category updated, rubric changelog gains `v2.16-test` row); §98 v2.16.0 → **v2.17.0**. Verification: self-test 6/6 ✅; tree-health 100/100; lockstep 0 findings; audit ✓ at 98.0/99.8 (no scoring change — CI safety net only).

> **v2.13.0 update (Phase 90 — `--explain` rubric trace flag):** Patched `linter-scripts/audit-spec-vs-code-v2.py` v2.15 → **v2.16** with new `--explain=<substring>` CLI flag. Pure-add diagnostic that prints rubric branch, fired bonuses (with deltas + originating rubric version), capped gates (with before/after), per-dimension raw-vs-final scores, and key metrics for any single module. No file writes, no AI calls; exits 0 on match / 1 on no-match. Use cases: rubric debugging, contributor onboarding, explaining grade changes after a rubric edit. [`31-audit-spec-vs-code-v2.md`](../31-audit-spec-vs-code-v2.md) v1.8.0 → **v1.9.0** with **AC-31-23** specifying the full stdout structure + exit-code + no-side-effects contract; CLI flags table gains `Since` column distinguishing v2.12 vs v2.16; rubric changelog table extended through v2.16. Lockstep: §31 v1.8.0 → v1.9.0; §98 v2.15.0 → v2.16.0. Verification: tree-health 100/100, lockstep 0 findings, audit ✓ at 98.0/99.8 — no scoring regression.

> **v2.12.0 update (Phase 45):** Cleared the §27 implementability bottleneck (55 → 90, weighted 78 → 89). Patched `linter-scripts/audit-spec-vs-code-v2.py` v2.7 → **v2.8** with `has_normative_contract` metric + dedicated `meta-toolchain` rubric branch (baseline 75, `+10` text-contract, `+5` ≥30 md_files). Inlined "Normative Contract — Toolchain Bijection" `text` block into [`00-overview.md`](../00-overview.md) v1.6.0 → v1.7.0 (8 number ranges, 7 INVs, 3 DELs, 5 FAILs). [`31-audit-spec-vs-code-v2.md`](../31-audit-spec-vs-code-v2.md) v1.6.0 → v1.7.0 with **AC-31-16**. Measured: **mean weighted 82.3 → 84.3** ✅ (target hit); mean implementability 65.6 → 67.8; tree health 100/100 strict.

> **v2.11.0 update (Phase 42):** Auditor `linter-scripts/audit-spec-vs-code-v2.py` v2.6 → **v2.7** — `skip_kinds` extended to `{tracker, index, meta-toolchain}` on `G-CON-01` and `{tracker, index}` on `G-CON-02` (rubric already exempted these kinds; gates now mirror). Inlined JSON-Schema contracts in 5 C-tier specs (`08-file-folder-naming`, `11-security`, `08-linter-scripts`, `09-templates`, `01-phase-2-git-logs-audit`). Fixed self-inflicted broken-link FP in `27-spec-toolchain` by rewriting two prose example mentions abstractly. [`31-audit-spec-vs-code-v2.md`](../31-audit-spec-vs-code-v2.md) v1.5.0 → v1.6.0 with **AC-31-15**. Measured: **C-tier modules 11 → 0; mean weighted 82.3 → 84.1**; tree health 100/100 strict.

> **v2.10.0 update (Phase 43):** Patched `linter-scripts/audit-spec-vs-code-v2.py` v2.5 → **v2.6** to clear the broken-link false-positive class. `LINK_RX.findall` now runs against `strip_code(body_text)` so markdown links inside fenced ```` ```markdown / ```text ```` template blocks (e.g. `01-spec-authoring-guide`'s path-syntax examples) no longer count toward `links_total` / `links_broken`. [`31-audit-spec-vs-code-v2.md`](../31-audit-spec-vs-code-v2.md) v1.4.0 → v1.5.0 with new **AC-31-14**. Measured: **broken links 30 → 0 across 79 modules** (2573 valid links scanned); G-LINK-01 + G-LINK-02 caps lifted on 5 modules; `01-spec-authoring-guide` 70 (C) → B-tier expected; mean weighted 81.7 → 82.3.

> **v2.9.0 update (Phase R5):** Patched `linter-scripts/audit-spec-vs-code-v2.py` v2.4 → **v2.5** to clear the last G-TODO-01 false positives (auditor self-reference). Added `META_TOKEN_SEQ_RX` (strips canonical `TODO/TBD/FIXME` references) and per-gate `skip_kinds` mechanism (G-TODO-01 now bypassed when `kind: meta-toolchain`). Added `kind: meta-toolchain` frontmatter to `27-spec-toolchain/00-overview.md` (v1.5.0 → v1.6.0). [`31-audit-spec-vs-code-v2.md`](../31-audit-spec-vs-code-v2.md) v1.3.0 → v1.4.0 with new **AC-31-12** + **AC-31-13**. Measured: **G-TODO-01 active firings 1 → 0**; `02-coding-guidelines` 94 (A) → 98 (A+); A+ tier 4 → 5; mean 81.6 → 81.7. `27-spec-toolchain` itself held at 78 — bottlenecked by impl=55 (Phase 42 target), not completeness.

> **v2.6.0 update (Phase 40):** Added [`24-check-lockstep.md`](../24-check-lockstep.md) + `linter-scripts/check-lockstep.cjs` (v1.0.0). Mechanises the Core memory rule "§00 banner + §98 changelog row + §99 health/inventory in lockstep". 4 rules (L0/L1/L2/L3), format-tolerant, warn-only by default. Wired into `spec-monthly-audit.yml`. Bijection 30/30 → 31/31. Adoption baseline: 24/82 modules drifted (8 L0 missing banners, 17 L1 stale §99 dates, 3 L2 missing changelog rows). Phase 41 backlog: sweep baseline to zero before flipping CI flag to `--strict`. 8 ACs (AC-24-01..08).

> **v2.5.0 update (Phase 39b):** §00 banner v1.1.0 → v1.2.0 with "Audit Marker Exemption" section. AI audit's `todo_count: 4` was a substring false-positive — every match lives in script-spec content that *defines* TODO-detection metrics or scaffolder behaviour. R4 follow-up: regex-fix `audit-spec-vs-code-v2.py` to exclude fenced code blocks.

> **v2.3.0 update (Phase 36):** Added `--strict` flag to `check-tree-health.cjs` (threshold 100 + fails on any single module below full marks). Wired into `spec-monthly-audit.yml`. [`05-check-tree-health.md`](../05-check-tree-health.md) v1.0.0 → v2.0.0 — rubric v2.0.0 fully documented (was still describing v1.x); 3 new ACs (AC-05-04..AC-05-06) for rubric weighting + `--strict` semantics; AC-05-02 corrected to match percentage-based scoring. Verified strict pass at 54/54 modules full marks.

> **v2.2.0 update (Phase 35 — R3):** Added [`71-spec-monthly-audit-yml.md`](../71-spec-monthly-audit-yml.md) + `.github/workflows/spec-monthly-audit.yml`. Time-driven monthly cadence companion to event-driven `spec-health.yml`. Includes dashboard-parity check (Phase 34 invariant) and auto-opens GitHub issue on regression. Bijection 28/28 → 29/29. Closes R3 from `32-phase-26-31-rollup.md` §4.

> **v2.1.0 update (Phase 34):** Propagated rubric v2.0.0 from `check-tree-health.cjs` into `generate-dashboard-data.cjs`. New top-level `RubricV2` block in `dashboard-data.json`; `Health.Score` now driven by rubric (legacy deduction-based score retained as `Health.LegacyScore`). [`11-generate-dashboard-data.md`](../11-generate-dashboard-data.md) v1.0.0 → v1.1.0; AC-11-01 schema corrected (was a 3-key shape that never matched the actual output); AC-11-04 added (parity with `check-tree-health.cjs`). Verified: dashboard reports 100/100 (A+), 162/162 quality credits, parity confirmed.

> **v2.0.0 update:** Phase 16d-iv deepened §97 from 10 ACs to **20 module-specific GWT ACs** (AC-T-11..AC-T-20 added; AC-T-01..AC-T-10 preserved). New ACs cover stderr discipline, filler tight-loop idempotency, generator determinism, auditor JSON contract, config self-validation, runner cross-platform equivalence, trace-map round-trip, twin byte-equivalence, CI trigger-path completeness, and `trace-map.md` informational status. Banner v1.1.0 → v2.0.0.
